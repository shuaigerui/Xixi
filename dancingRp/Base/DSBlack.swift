
import Foundation

import UIKit

final class DSBlack: UITabBar {

    private enum Layout {
        static let cornerRadius: CGFloat = 28
    }

    private let backgroundContainerView: UIView = {
       var intrinsic2: String! = String(cString: [101,110,117,109,118,97,108,117,101,0], encoding: .utf8)!
    var views_: Float = 5.0
      intrinsic2.append("\((Int(views_ > 390319068.0 || views_ < -390319068.0 ? 43.0 : views_)))")

        let view = UIView()
   repeat {
      views_ /= Swift.max(4, (Float(Int(views_ > 44035421.0 || views_ < -44035421.0 ? 85.0 : views_) >> (Swift.min(4, labs(3))))))
      if views_ == 3661143.0 {
         break
      }
   } while ((views_ - views_) > 5.72 || (views_ - 5.72) > 5.53) && (views_ == 3661143.0)
        view.backgroundColor = .black
        view.isUserInteractionEnabled = false
        return view
    }()

    override init(frame: CGRect) {
        super.init(frame: frame)
        commonInit()
    }

    required init?(coder: NSCoder) {
        super.init(coder: coder)
        commonInit()
    }

    private func commonInit() {
       var rangeG: Float = 5.0
   withUnsafeMutablePointer(to: &rangeG) { pointer in
    
   }
    var spacingC: Bool = false
    _ = spacingC
   if spacingC {
       var argt: Double = 1.0
       var decodedG: String! = String(cString: [112,114,105,110,116,101,114,0], encoding: .utf8)!
       var coverE: Float = 2.0
       var setupe: Int = 5
      withUnsafeMutablePointer(to: &setupe) { pointer in
    
      }
       var instantA: Double = 1.0
       var sectionl: [Any]! = [99, 55]
      withUnsafeMutablePointer(to: &sectionl) { pointer in
    
      }
       var seedT: [Any]! = [24, 45, 50]
      if !sectionl.contains { $0 as? Int == seedT.count } {
          var n_titleI: String! = String(cString: [108,117,109,98,101,114,106,97,99,107,0], encoding: .utf8)!
          var statusy: Bool = true
         seedT.append((1 / (Swift.max(Int(coverE > 219225962.0 || coverE < -219225962.0 ? 82.0 : coverE), 10))))
         n_titleI = "\((Int(coverE > 365206350.0 || coverE < -365206350.0 ? 56.0 : coverE) + (statusy ? 4 : 3)))"
         statusy = seedT.count <= 19
      }
      if argt >= 4.67 {
         decodedG = "\(seedT.count)"
      }
         decodedG = "\((Int(coverE > 33321563.0 || coverE < -33321563.0 ? 85.0 : coverE)))"
          var tabbarY: Double = 5.0
          var registeredg: Bool = false
          var size_aA: String! = String(cString: [114,101,98,97,115,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &size_aA) { pointer in
                _ = pointer.pointee
         }
         decodedG.append("\((Int(tabbarY > 23799283.0 || tabbarY < -23799283.0 ? 75.0 : tabbarY) % (Swift.max(3, seedT.count))))")
         registeredg = argt == 13.69
         size_aA.append("\((Int(coverE > 33908205.0 || coverE < -33908205.0 ? 70.0 : coverE) * Int(argt > 1399637.0 || argt < -1399637.0 ? 64.0 : argt)))")
      for _ in 0 ..< 2 {
         instantA += (Double((String(cString:[71,0], encoding: .utf8)!) == decodedG ? decodedG.count : seedT.count))
      }
      for _ in 0 ..< 3 {
          var documentsl: String! = String(cString: [97,109,116,0], encoding: .utf8)!
          var relativeF: Double = 5.0
         sectionl = [(Int(relativeF > 153571120.0 || relativeF < -153571120.0 ? 48.0 : relativeF))]
         documentsl = "\((Int(relativeF > 365323837.0 || relativeF < -365323837.0 ? 33.0 : relativeF)))"
      }
         seedT = [(Int(instantA > 25382993.0 || instantA < -25382993.0 ? 28.0 : instantA) % (Swift.max(sectionl.count, 6)))]
         seedT = [((String(cString:[82,0], encoding: .utf8)!) == decodedG ? Int(instantA > 94118756.0 || instantA < -94118756.0 ? 12.0 : instantA) : decodedG.count)]
         decodedG.append("\((2 + Int(instantA > 290630379.0 || instantA < -290630379.0 ? 86.0 : instantA)))")
         coverE -= (Float(Int(coverE > 337880750.0 || coverE < -337880750.0 ? 17.0 : coverE) / 3))
       var audioV: Float = 3.0
      for _ in 0 ..< 1 {
         setupe |= 1
      }
          var usedu: String! = String(cString: [99,104,97,110,103,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &usedu) { pointer in
    
         }
          var keptI: String! = String(cString: [97,118,102,109,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &keptI) { pointer in
    
         }
         seedT.append(seedT.count)
         usedu = "\(usedu.count)"
         keptI.append("\(decodedG.count)")
      for _ in 0 ..< 3 {
         coverE /= Swift.max((Float(Int(audioV > 293423410.0 || audioV < -293423410.0 ? 27.0 : audioV))), 3)
      }
      spacingC = 34.2 == rangeG || instantA == 34.2
   }

   while (spacingC) {
      rangeG += (Float(2 / (Swift.max(5, Int(rangeG > 141724513.0 || rangeG < -141724513.0 ? 33.0 : rangeG)))))
      break
   }
        backgroundColor = .clear
        isTranslucent = false
        insertSubview(backgroundContainerView, at: 0)
    }

    override func layoutSubviews() {
       var docsr: Bool = false
       var trimmedw: [String: Any]! = [String(cString: [104,102,121,117,0], encoding: .utf8)!:45, String(cString: [111,105,100,0], encoding: .utf8)!:9]
      withUnsafeMutablePointer(to: &trimmedw) { pointer in
             _ = pointer.pointee
      }
         trimmedw["\(trimmedw.values.count)"] = trimmedw.keys.count & 3
      if trimmedw["\(trimmedw.values.count)"] != nil {
         trimmedw = ["\(trimmedw.values.count)": trimmedw.count]
      }
         trimmedw = ["\(trimmedw.keys.count)": trimmedw.count]
      docsr = !docsr

        super.layoutSubviews()

        backgroundContainerView.frame = bounds
        backgroundContainerView.layer.cornerRadius = Layout.cornerRadius
        backgroundContainerView.layer.maskedCorners = [.layerMinXMinYCorner, .layerMaxXMinYCorner]
        backgroundContainerView.layer.masksToBounds = true

        subviews.forEach { subview in
            guard subview !== backgroundContainerView else { return }
            let cached = NSStringFromClass(type(of: subview))
            if cached.contains("_UIBarBackground") || cached == "_UIBarBackground" {
                subview.isHidden = true
                subview.alpha = 0
            } else {
                subview.isHidden = false
                subview.alpha = 1
            }
        }
    }

    override func sizeThatFits(_ size: CGSize) -> CGSize {
       var feedF: Double = 1.0
   withUnsafeMutablePointer(to: &feedF) { pointer in
    
   }
   if 2.82 >= (feedF + 2.77) && (feedF + 2.77) >= 2.26 {
       var previousk: [String: Any]! = [String(cString: [114,101,119,97,114,100,0], encoding: .utf8)!:UILabel()]
      withUnsafeMutablePointer(to: &previousk) { pointer in
    
      }
       var sandboxC: String! = String(cString: [118,97,100,100,113,0], encoding: .utf8)!
       var configuredJ: String! = String(cString: [115,105,110,107,115,0], encoding: .utf8)!
       var send6: String! = String(cString: [116,111,100,97,121,0], encoding: .utf8)!
       var edity: [Any]! = [95, 41]
      for _ in 0 ..< 1 {
          var work_: String! = String(cString: [115,116,101,97,108,0], encoding: .utf8)!
         sandboxC.append("\(2 | send6.count)")
         work_.append("\(previousk.count)")
      }
         previousk = ["\(previousk.values.count)": sandboxC.count % (Swift.max(10, previousk.count))]
          var mediad: String! = String(cString: [115,116,114,102,116,105,109,101,0], encoding: .utf8)!
          var launchs: Bool = true
          var sendP: Bool = false
         previousk = ["\(previousk.values.count)": mediad.count]
         launchs = send6.count < configuredJ.count
         sendP = !sandboxC.contains("\(sendP)")
      repeat {
         previousk[configuredJ] = configuredJ.count | 1
         if previousk.count == 2640605 {
            break
         }
      } while ((4 & sandboxC.count) < 1) && (previousk.count == 2640605)
      if sandboxC.hasSuffix("\(configuredJ.count)") {
         sandboxC = "\(((String(cString:[106,0], encoding: .utf8)!) == configuredJ ? configuredJ.count : edity.count))"
      }
      repeat {
         edity = [edity.count << (Swift.min(labs(3), 1))]
         if edity.count == 2030117 {
            break
         }
      } while (edity.count == 2030117) && ((configuredJ.count / (Swift.max(3, edity.count))) > 2 || (configuredJ.count / (Swift.max(5, edity.count))) > 2)
      if send6 != String(cString:[75,0], encoding: .utf8)! {
          var peeri: Double = 5.0
          var firstJ: Bool = false
          var datao: Float = 4.0
          var check5: Float = 0.0
         sandboxC.append("\((2 - Int(peeri > 389300902.0 || peeri < -389300902.0 ? 79.0 : peeri)))")
         firstJ = 12 <= sandboxC.count || edity.count <= 12
         datao /= Swift.max(3, (Float(2 | Int(peeri > 28373457.0 || peeri < -28373457.0 ? 48.0 : peeri))))
         check5 += Float(send6.count)
      }
          var diamondse: Int = 2
          var packagesu: Bool = false
          _ = packagesu
          var selected8: String! = String(cString: [100,99,112,114,101,100,0], encoding: .utf8)!
         configuredJ.append("\(((packagesu ? 1 : 2) % (Swift.max(selected8.count, 4))))")
         diamondse ^= 2
      for _ in 0 ..< 2 {
          var personX: String! = String(cString: [115,104,97,114,101,103,114,111,117,112,0], encoding: .utf8)!
          var phrases1: Int = 4
         configuredJ.append("\(sandboxC.count)")
         personX = "\(3 ^ edity.count)"
         phrases1 |= personX.count
      }
       var request3: String! = String(cString: [115,111,114,101,99,101,105,118,101,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
         edity.append(3 << (Swift.min(1, edity.count)))
      }
         configuredJ = "\(3 - request3.count)"
          var subdirectorye: String! = String(cString: [115,104,111,114,116,115,0], encoding: .utf8)!
          _ = subdirectorye
          var clipO: String! = String(cString: [97,108,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &clipO) { pointer in
                _ = pointer.pointee
         }
         edity = [1 + subdirectorye.count]
         clipO = "\(clipO.count)"
         request3.append("\(2 + sandboxC.count)")
      repeat {
          var offseta: String! = String(cString: [99,97,114,111,117,115,101,108,0], encoding: .utf8)!
         edity.append(configuredJ.count)
         offseta.append("\(3 << (Swift.min(3, offseta.count)))")
         if 271045 == edity.count {
            break
         }
      } while (271045 == edity.count) && (5 <= edity.count)
      feedF -= (Double(1 % (Swift.max(2, Int(feedF > 132547928.0 || feedF < -132547928.0 ? 57.0 : feedF)))))
   }

        var normalized = super.sizeThatFits(size)
        if normalized.height < 49 {
            normalized.height = 49
        }
        return normalized
    }
}
