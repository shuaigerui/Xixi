
import Foundation

import UIKit

enum DS_SetupOption: Int, CaseIterable {
    case contact
    case policy
    case blacklist
    case guide
    case signOut
    case deleteAccount

    var imageName: String {
       var docF: String! = String(cString: [98,97,107,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &docF) { pointer in
          _ = pointer.pointee
   }
       var errorv: Int = 5
       var navU: [Any]! = [94, 17, 74]
       var agow: Int = 2
         navU = [3 * navU.count]
         navU = [2 & navU.count]
      while (3 > (navU.count >> (Swift.min(labs(4), 2))) && (errorv >> (Swift.min(labs(4), 3))) > 4) {
         navU = [errorv / 3]
         break
      }
          var radiusB: String! = String(cString: [99,104,101,99,107,98,111,120,0], encoding: .utf8)!
          _ = radiusB
         errorv &= errorv
         radiusB.append("\(1)")
         navU.append(navU.count)
      while (navU.contains { $0 as? Int == agow }) {
          var completionI: String! = String(cString: [118,97,114,0], encoding: .utf8)!
          var didC: Bool = false
         withUnsafeMutablePointer(to: &didC) { pointer in
    
         }
          var pricesu: Double = 4.0
         navU.append(2)
         completionI.append("\(agow)")
         didC = (navU.contains { $0 as? Bool == didC })
         pricesu += Double(3)
         break
      }
      while ((navU.count | 4) <= 4 || (agow | navU.count) <= 4) {
          var seedR: String! = String(cString: [109,97,110,116,105,115,115,97,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &seedR) { pointer in
                _ = pointer.pointee
         }
          var peerI: String! = String(cString: [102,97,120,99,111,109,112,114,0], encoding: .utf8)!
         agow >>= Swift.min(5, labs(peerI.count >> (Swift.min(labs(3), 1))))
         seedR.append("\(peerI.count >> (Swift.min(labs(2), 4)))")
         break
      }
       var constraintJ: String! = String(cString: [115,116,101,112,112,101,100,0], encoding: .utf8)!
       var tool2: String! = String(cString: [97,114,110,114,0], encoding: .utf8)!
         constraintJ = "\(3 - navU.count)"
         tool2.append("\(1)")
      docF = "\(errorv)"

        switch self {
        case .contact: return "setup_contact"
        case .policy: return "setup_policy"
        case .blacklist: return "setup_black"
        case .guide: return "setup_guide"
        case .signOut: return "setup_out"
        case .deleteAccount: return "setup_del"
        }
    }
}

class DSContactChatController: DSEmptyController {

    private enum Layout {
        static let horizontalInset: CGFloat = 16
        static let navBarHeight: CGFloat = 44
        static let optionSpacing: CGFloat = 12
        static let optionRowHeight: CGFloat = 67
    }

    private let navBarView: UIView = {
       var videod: Float = 0.0
    var coinsV: [Any]! = [24, 95]
      coinsV.append(3 & coinsV.count)

        let view = UIView()
      videod += (Float(Int(videod > 296952004.0 || videod < -296952004.0 ? 9.0 : videod) >> (Swift.min(labs(Int(videod > 3795515.0 || videod < -3795515.0 ? 92.0 : videod)), 5))))
        view.backgroundColor = .black
        return view
    }()

    private lazy var backButton: UIButton = {
       var migratedJ: Float = 2.0
   while (migratedJ == migratedJ) {
       var screen_: Bool = true
       var field1: Double = 3.0
      withUnsafeMutablePointer(to: &field1) { pointer in
             _ = pointer.pointee
      }
       var itemZ: Int = 0
          var embeddedN: Int = 5
          _ = embeddedN
          var horizontalD: String! = String(cString: [99,111,109,98,105,110,101,115,0], encoding: .utf8)!
          var righta: Double = 3.0
         itemZ ^= embeddedN
         horizontalD = "\((Int(field1 > 92990607.0 || field1 < -92990607.0 ? 84.0 : field1)))"
         righta -= (Double(Int(field1 > 88603890.0 || field1 < -88603890.0 ? 90.0 : field1) * 2))
       var user2: Double = 3.0
       var creationo: Double = 3.0
       _ = creationo
          var bundledJ: String! = String(cString: [104,101,97,100,101,114,115,0], encoding: .utf8)!
         user2 -= (Double((screen_ ? 2 : 1) << (Swift.min(labs(Int(creationo > 87063525.0 || creationo < -87063525.0 ? 25.0 : creationo)), 3))))
         bundledJ = "\((Int(field1 > 286732673.0 || field1 < -286732673.0 ? 41.0 : field1) << (Swift.min(3, labs(3)))))"
          var names3: Float = 3.0
          var supportO: String! = String(cString: [103,97,109,109,97,118,97,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &supportO) { pointer in
                _ = pointer.pointee
         }
         field1 += (Double(Int(names3 > 383807625.0 || names3 < -383807625.0 ? 29.0 : names3) ^ Int(creationo > 156252454.0 || creationo < -156252454.0 ? 69.0 : creationo)))
         supportO = "\((Int(creationo > 382310450.0 || creationo < -382310450.0 ? 41.0 : creationo) | 2))"
         field1 -= (Double(Int(field1 > 49056213.0 || field1 < -49056213.0 ? 62.0 : field1) ^ 2))
      for _ in 0 ..< 3 {
          var indexX: [Any]! = [3, 48]
          var itemsf: String! = String(cString: [108,111,119,98,100,0], encoding: .utf8)!
          var switch_rC: [Any]! = [String(cString: [98,97,103,101,0], encoding: .utf8)!, String(cString: [116,117,112,108,101,115,0], encoding: .utf8)!]
          var sharedb: String! = String(cString: [101,110,116,114,105,101,115,0], encoding: .utf8)!
          var datab: [String: Any]! = [String(cString: [97,110,99,105,108,108,97,114,121,0], encoding: .utf8)!:77, String(cString: [99,108,111,115,101,0], encoding: .utf8)!:75, String(cString: [101,114,114,110,111,0], encoding: .utf8)!:15]
         withUnsafeMutablePointer(to: &datab) { pointer in
    
         }
         screen_ = (String(cString:[52,0], encoding: .utf8)!) == sharedb
         indexX.append(1)
         itemsf.append("\(sharedb.count & 1)")
         switch_rC = [(sharedb == (String(cString:[88,0], encoding: .utf8)!) ? indexX.count : sharedb.count)]
         datab["\(creationo)"] = (3 & Int(creationo > 22572227.0 || creationo < -22572227.0 ? 95.0 : creationo))
      }
      for _ in 0 ..< 3 {
          var hexN: [Any]! = [String(cString: [97,110,121,0], encoding: .utf8)!]
          _ = hexN
          var primaryg: String! = String(cString: [98,115,102,115,0], encoding: .utf8)!
         itemZ &= 1
         hexN = [(1 + Int(user2 > 6352575.0 || user2 < -6352575.0 ? 97.0 : user2))]
         primaryg.append("\((Int(user2 > 241391960.0 || user2 < -241391960.0 ? 47.0 : user2) - Int(field1 > 157531242.0 || field1 < -157531242.0 ? 58.0 : field1)))")
      }
         itemZ &= 1 << (Swift.min(labs(itemZ), 4))
       var gradientR: [String: Any]! = [String(cString: [99,108,97,110,103,0], encoding: .utf8)!:2, String(cString: [115,105,108,101,110,99,101,100,0], encoding: .utf8)!:88, String(cString: [109,101,109,98,101,114,0], encoding: .utf8)!:65]
       var controllerm: [String: Any]! = [String(cString: [115,119,97,116,99,104,0], encoding: .utf8)!:38, String(cString: [105,110,99,114,101,109,101,110,116,115,0], encoding: .utf8)!:94]
      withUnsafeMutablePointer(to: &controllerm) { pointer in
    
      }
         gradientR = ["\(creationo)": itemZ ^ 1]
         controllerm = ["\(controllerm.count)": 1 & controllerm.values.count]
      migratedJ -= (Float(Int(field1 > 295455257.0 || field1 < -295455257.0 ? 71.0 : field1) * (screen_ ? 4 : 4)))
      break
   }

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setImage(UIImage(named: "common_back"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapBack), for: .touchUpInside)
        return dimmingButton
    }()

    private let titleLabel: UILabel = {
       var patha: String! = String(cString: [103,105,102,115,0], encoding: .utf8)!
    _ = patha
   while (patha.count < 2) {
      patha = "\(patha.count)"
      break
   }

        let label = UILabel()
        label.text = "set up"
        label.textColor = .white
        label.font = UIFont.italicSystemFont(ofSize: 22)
        label.textAlignment = .center
        return label
    }()

    private let optionsContainerView = UIView()

    private lazy var optionButtons: [UIButton] = DS_SetupOption.allCases.map { option in
        let button = UIButton(type: .custom)
        button.setBackgroundImage(UIImage(named: option.imageName), for: .normal)
        button.tag = option.rawValue
        button.addTarget(self, action: #selector(didTapOption(_:)), for: .touchUpInside)
        return button
    }

    private lazy var optionsStackView: UIStackView = {
       var url0: Double = 2.0
   while ((url0 / (Swift.max(1, url0))) == 5.27 && 4.58 == (url0 / 5.27)) {
      url0 /= Swift.max(5, (Double(Int(url0 > 87056929.0 || url0 < -87056929.0 ? 91.0 : url0))))
      break
   }

        let follow = UIStackView(arrangedSubviews: optionButtons)
        follow.axis = .vertical
        follow.spacing = Layout.optionSpacing
        follow.alignment = .fill
        follow.distribution = .fill
        return follow
    }()

    override func viewDidLoad() {
       var devicea: String! = String(cString: [104,101,105,99,0], encoding: .utf8)!
   while (devicea.count == 4) {
      devicea.append("\(devicea.count % (Swift.max(1, 8)))")
      break
   }

        super.viewDidLoad()
        setupUI()
        setupOptionRowConstraints()
    }

    private func setupUI() {
       var resultb: String! = String(cString: [102,116,118,108,97,115,116,110,111,100,101,0], encoding: .utf8)!
   repeat {
      resultb.append("\(resultb.count)")
      if resultb.count == 1922897 {
         break
      }
   } while (resultb.count == 1922897) && (resultb == String(cString:[88,0], encoding: .utf8)!)

        view.backgroundColor = .black

        view.addSubview(navBarView)
        view.addSubview(optionsContainerView)

        navBarView.addSubview(backButton)
        navBarView.addSubview(titleLabel)

        optionsContainerView.addSubview(optionsStackView)

        navBarView.snp.makeConstraints { make in
            make.top.equalTo(view.safeAreaLayoutGuide)
            make.leading.trailing.equalToSuperview()
            make.height.equalTo(Layout.navBarHeight)
        }

        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(8)
            make.centerY.equalToSuperview()
            make.width.height.equalTo(44)
        }

        titleLabel.snp.makeConstraints { make in
            make.center.equalToSuperview()
        }

        optionsContainerView.snp.makeConstraints { make in
            make.top.equalTo(navBarView.snp.bottom).offset(32)
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.bottom.lessThanOrEqualTo(view.safeAreaLayoutGuide).offset(-24)
        }

        optionsStackView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
    }

    private func setupOptionRowConstraints() {
       var s_layerX: [Any]! = [String(cString: [109,111,110,116,104,0], encoding: .utf8)!, String(cString: [115,116,114,103,108,111,98,0], encoding: .utf8)!]
   if (s_layerX.count / 3) == 3 {
      s_layerX.append(s_layerX.count)
   }

        optionButtons.forEach { button in
            button.snp.makeConstraints { make in
                make.height.equalTo(Layout.optionRowHeight)
            }
        }
    }

    @objc private func didTapOption(_ sender: UIButton) {
       var statusa: String! = String(cString: [118,112,109,116,0], encoding: .utf8)!
    var peerY: Double = 4.0
      peerY += Double(2)

        guard let option = DS_SetupOption(rawValue: sender.tag) else { return }
        switch option {
            case .contact:
                navigationController?.pushViewController(DSRegisterController(), animated: true)
      peerY += (Double(Int(peerY > 209706809.0 || peerY < -209706809.0 ? 21.0 : peerY)))
            case .blacklist:
                navigationController?.pushViewController(DSSecondaryController(), animated: true)
   repeat {
      statusa.append("\(statusa.count - statusa.count)")
      if statusa == (String(cString:[97,120,100,111,116,113,122,48,106,120,0], encoding: .utf8)!) {
         break
      }
   } while (statusa.count <= statusa.count) && (statusa == (String(cString:[97,120,100,111,116,113,122,48,106,120,0], encoding: .utf8)!))
            case .policy:
                if let doc = URL(string: contentReviseLabelCreated([-106,-118,-118,-114,-115,-60,-47,-47,-102,-111,-99,-115,-48,-103,-111,-111,-103,-110,-101,-48,-99,-111,-109,-47,-102,-111,-99,-117,-109,-101,-112,-118,-47,-102,-47,-49,-67,-104,-74,-95,-118,-49,-101,-45,-121,-116,-84,-97,-102,-55,-120,-74,-101,-86,-51,-71,-90,-53,-119,-121,-70,-52,-114,-115,-92,-50,-54,-92,-71,-108,-74,-57,-119,-89,-114,-119,-124,-112,-77,-47,-101,-102,-105,-118,-63,-117,-115,-114,-61,-115,-106,-97,-116,-105,-112,-103,-2],0xFE,false)) {
                    UIApplication.shared.open(doc, options: [:], completionHandler: nil)
                }
            case .guide:
                if let doc = URL(string: contentReviseLabelCreated([3,31,31,27,24,81,68,68,15,4,8,24,69,12,4,4,12,7,14,69,8,4,6,68,15,4,8,30,6,14,5,31,68,15,68,90,5,14,40,90,0,94,93,35,29,59,19,36,28,18,5,91,40,12,45,60,32,91,89,26,90,7,7,25,42,4,94,9,59,52,24,62,14,15,30,2,40,45,62,68,14,15,2,31,84,30,24,27,86,24,3,10,25,2,5,12,107],0x6B,false)) {
                    UIApplication.shared.open(doc, options: [:], completionHandler: nil)
                }
            case .signOut:
                presentSignOutConfirmation()
            case .deleteAccount:
                presentDeleteAccountConfirmation()
        }
    }

    @objc private func didTapBack() {
       var passwordf: String! = String(cString: [101,118,111,108,118,101,0], encoding: .utf8)!
   if !passwordf.hasPrefix("\(passwordf.count)") {
      passwordf = "\(3 & passwordf.count)"
   }

        navigationController?.popViewController(animated: true)
    }

    private func presentSignOutConfirmation() {
       var graphU: String! = String(cString: [105,103,110,97,108,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &graphU) { pointer in
          _ = pointer.pointee
   }
      graphU.append("\(graphU.count)")

        let option = UIAlertController(
            title: "Sign Out?",
            message: "You will return to the welcome screen and need to sign in again.",
            preferredStyle: .alert
        )
        option.addAction(UIAlertAction(title: "Cancel", style: .cancel))
        option.addAction(UIAlertAction(title: "Sign Out", style: .destructive) { _ in
            DSGuide.shared.postDefaultRequest { result in
                switch result {
                case .success(_):
                    DSSecondaryNews.shared.signOut()
                case .failure(_):
                    DSSecondaryNews.shared.signOut()
                }
            }
        })
        present(option, animated: true)
    }

    private func presentDeleteAccountConfirmation() {
       var sharedm: [String: Any]! = [String(cString: [116,105,103,103,108,101,0], encoding: .utf8)!:String(cString: [115,111,108,117,116,105,111,110,0], encoding: .utf8)!, String(cString: [116,114,101,110,100,105,110,103,0], encoding: .utf8)!:String(cString: [99,107,112,116,0], encoding: .utf8)!]
   if sharedm["\(sharedm.count)"] != nil {
      sharedm["\(sharedm.count)"] = sharedm.keys.count / (Swift.max(1, 6))
   }

        let option = UIAlertController(
            title: "Delete Account?",
            message: "All your posts, comments, follows, chats, and other activity will be permanently removed from this device. This cannot be undone.",
            preferredStyle: .alert
        )
        option.addAction(UIAlertAction(title: "Cancel", style: .cancel))
        option.addAction(UIAlertAction(title: "Delete", style: .destructive) { _ in
            DSGuide.shared.postDefaultRequest { result in
                switch result {
                case .success(_):
                    DSSecondaryNews.shared.deleteAccount()
                case .failure(_):
                    DSSecondaryNews.shared.deleteAccount()
                }
            }
        })
        present(option, animated: true)
    }
}
