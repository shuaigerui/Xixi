
import Foundation

import UIKit

struct DSTabbarSign {
    let coverImageName: String?
    let avatarImageName: String?
    let userName: String
}

final class DSTeamSecondaryView: UIView {

    private enum Layout {
        static let horizontalInset: CGFloat = 16
    }

    var onCoinShopTapped: (() -> Void)?
    var onReviseTapped: (() -> Void)?
    var onSetupTapped: (() -> Void)?

    private let coverContainerView: UIView = {
       var creationx: [String: Any]! = [String(cString: [100,97,121,0], encoding: .utf8)!:String(cString: [111,118,101,114,105,100,100,101,110,0], encoding: .utf8)!, String(cString: [115,117,112,101,114,103,114,111,117,112,0], encoding: .utf8)!:String(cString: [102,105,101,108,100,0], encoding: .utf8)!]
      creationx = ["\(creationx.count)": creationx.keys.count]

        let view = UIView()
        view.clipsToBounds = true
        view.layer.cornerRadius = 44
        view.layer.maskedCorners = [.layerMinXMaxYCorner, .layerMaxXMaxYCorner]
        return view
    }()

    private let coverImageView: UIImageView = {
       var graphm: Int = 2
    var sel3: String! = String(cString: [111,114,103,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &sel3) { pointer in
          _ = pointer.pointee
   }
   while (2 > sel3.count) {
       var systemQ: String! = String(cString: [107,101,121,102,114,97,109,101,0], encoding: .utf8)!
       var formm: [String: Any]! = [String(cString: [113,112,101,108,100,115,112,0], encoding: .utf8)!:56]
          var ext2: String! = String(cString: [101,120,116,101,110,100,0], encoding: .utf8)!
         systemQ = "\(systemQ.count)"
         ext2.append("\(1)")
         formm = ["\(formm.values.count)": systemQ.count | 2]
      repeat {
          var shopl: [String: Any]! = [String(cString: [102,117,108,108,115,99,114,101,101,110,0], encoding: .utf8)!:19, String(cString: [109,100,116,97,0], encoding: .utf8)!:48, String(cString: [114,105,110,103,116,111,110,101,0], encoding: .utf8)!:26]
          var z_title8: String! = String(cString: [97,115,121,110,99,104,114,111,110,111,117,115,108,121,0], encoding: .utf8)!
          var messagei: Double = 5.0
         formm[systemQ] = (Int(messagei > 181354733.0 || messagei < -181354733.0 ? 77.0 : messagei))
         shopl[z_title8] = z_title8.count + 1
         if 2367563 == formm.count {
            break
         }
      } while (2367563 == formm.count) && (!systemQ.hasPrefix("\(formm.count)"))
         systemQ = "\(3 * systemQ.count)"
          var coordinatory: Bool = false
          var register_vq: Int = 2
          var followw: String! = String(cString: [118,111,114,100,105,112,108,111,109,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &followw) { pointer in
                _ = pointer.pointee
         }
         systemQ.append("\((followw == (String(cString:[118,0], encoding: .utf8)!) ? followw.count : formm.count))")
         coordinatory = !coordinatory
         register_vq |= formm.values.count
          var phraseU: String! = String(cString: [98,105,110,116,101,120,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &phraseU) { pointer in
                _ = pointer.pointee
         }
         formm["\(phraseU)"] = 1 & formm.keys.count
      graphm |= 1
      break
   }

        let imageView = UIImageView()
       var pathsc: Double = 0.0
      withUnsafeMutablePointer(to: &pathsc) { pointer in
    
      }
       var blurQ: [String: Any]! = [String(cString: [100,97,97,108,97,0], encoding: .utf8)!:75, String(cString: [116,101,114,109,105,110,97,116,105,110,103,0], encoding: .utf8)!:45, String(cString: [117,118,109,118,0], encoding: .utf8)!:81]
      withUnsafeMutablePointer(to: &blurQ) { pointer in
             _ = pointer.pointee
      }
      repeat {
         blurQ = ["\(blurQ.count)": 1 & blurQ.count]
         if blurQ.count == 2926399 {
            break
         }
      } while (blurQ.count == 2926399) && (blurQ.keys.contains("\(pathsc)"))
         pathsc -= Double(1)
      while (1.1 >= pathsc) {
          var secondaryl: String! = String(cString: [115,99,111,112,101,100,0], encoding: .utf8)!
          var destinationk: Int = 3
         withUnsafeMutablePointer(to: &destinationk) { pointer in
    
         }
          var prefix_ii2: Double = 0.0
          _ = prefix_ii2
          var valueh: [Any]! = [78, 28]
         withUnsafeMutablePointer(to: &valueh) { pointer in
    
         }
         pathsc += Double(blurQ.count & valueh.count)
         secondaryl = "\((Int(pathsc > 253548844.0 || pathsc < -253548844.0 ? 11.0 : pathsc) / (Swift.max(2, 7))))"
         destinationk |= (Int(prefix_ii2 > 316546582.0 || prefix_ii2 < -316546582.0 ? 34.0 : prefix_ii2) + 3)
         prefix_ii2 -= Double(destinationk)
         break
      }
         blurQ["\(pathsc)"] = (Int(pathsc > 13958099.0 || pathsc < -13958099.0 ? 26.0 : pathsc) * 2)
       var unreadV: Float = 5.0
      if (blurQ.count ^ 4) <= 3 {
          var configM: String! = String(cString: [109,97,105,110,108,105,115,116,0], encoding: .utf8)!
          var costf: Bool = true
          _ = costf
          var register_2iU: String! = String(cString: [108,117,97,0], encoding: .utf8)!
          var pageg: String! = String(cString: [115,101,101,107,0], encoding: .utf8)!
         blurQ = ["\(blurQ.keys.count)": (3 << (Swift.min(5, labs(Int(pathsc > 314755031.0 || pathsc < -314755031.0 ? 59.0 : pathsc)))))]
         configM = "\(register_2iU.count ^ 3)"
         costf = configM == (String(cString:[66,0], encoding: .utf8)!) && blurQ.keys.count == 47
         register_2iU.append("\(2 | pageg.count)")
         pageg = "\(2 % (Swift.max(2, blurQ.keys.count)))"
      }
         unreadV /= Swift.max((Float(Int(pathsc > 261786000.0 || pathsc < -261786000.0 ? 50.0 : pathsc))), 4)
      sel3.append("\((Int(pathsc > 366458180.0 || pathsc < -366458180.0 ? 82.0 : pathsc) + sel3.count))")
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        return imageView
    }()

    private let avatarImageView: UIImageView = {
       var attributesS: Float = 5.0
    _ = attributesS
    var playingS: String! = String(cString: [98,121,114,121,105,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      attributesS += Float(playingS.count ^ 2)
   }

        let imageView = UIImageView()
   for _ in 0 ..< 3 {
      playingS.append("\(playingS.count | playingS.count)")
   }
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        imageView.backgroundColor = UIColor.hex("#3A3A3C")
        imageView.layer.cornerRadius = 56
        imageView.layer.borderWidth = 2
        imageView.layer.borderColor = UIColor.white.cgColor
        return imageView
    }()

    private let nameLabel: UILabel = {
       var reusables: [Any]! = [String(cString: [97,112,99,109,0], encoding: .utf8)!, String(cString: [115,117,112,101,114,118,105,101,119,0], encoding: .utf8)!, String(cString: [109,98,105,110,116,114,97,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &reusables) { pointer in
    
   }
   if (reusables.count | 5) < 4 && (reusables.count | reusables.count) < 5 {
       var total9: Double = 2.0
       _ = total9
       var goldk: String! = String(cString: [99,111,109,112,111,115,101,105,0], encoding: .utf8)!
       var targetQ: [String: Any]! = [String(cString: [99,111,110,116,105,103,117,111,117,115,0], encoding: .utf8)!:5, String(cString: [108,97,110,103,117,97,103,101,115,0], encoding: .utf8)!:81, String(cString: [116,114,97,110,115,105,116,105,111,110,105,110,103,0], encoding: .utf8)!:24]
       var rangeF: Bool = false
       _ = rangeF
       var networkq: [String: Any]! = [String(cString: [103,114,101,101,110,0], encoding: .utf8)!:58, String(cString: [112,97,114,116,105,99,105,112,97,110,116,0], encoding: .utf8)!:87]
       var docE: [Any]! = [85, 18]
         docE = [(targetQ.count << (Swift.min(3, labs((rangeF ? 3 : 2)))))]
          var candidateI: String! = String(cString: [105,115,116,97,112,0], encoding: .utf8)!
         rangeF = networkq["\(total9)"] == nil
         candidateI = "\(candidateI.count)"
      if (total9 * 2.81) > 2.52 {
         total9 += Double(goldk.count << (Swift.min(labs(1), 1)))
      }
         total9 /= Swift.max(Double(networkq.values.count), 2)
          var delay1: String! = String(cString: [100,105,97,108,111,103,117,101,0], encoding: .utf8)!
          _ = delay1
          var contactx: Int = 5
          _ = contactx
         docE.append(contactx)
         delay1.append("\(targetQ.keys.count)")
      repeat {
         targetQ["\(docE.count)"] = targetQ.count
         if 3491234 == targetQ.count {
            break
         }
      } while (targetQ.keys.count <= goldk.count) && (3491234 == targetQ.count)
      if 1 == (targetQ.values.count | 1) || (goldk.count | targetQ.values.count) == 1 {
         targetQ["\(total9)"] = goldk.count
      }
      repeat {
         total9 += (Double(1 ^ Int(total9 > 280349942.0 || total9 < -280349942.0 ? 9.0 : total9)))
         if total9 == 606981.0 {
            break
         }
      } while (networkq.values.contains { $0 as? Double == total9 }) && (total9 == 606981.0)
          var existingM: Double = 3.0
         rangeF = docE.count == 15
         existingM /= Swift.max(3, (Double(docE.count >> (Swift.min(5, labs(Int(total9 > 237456339.0 || total9 < -237456339.0 ? 21.0 : total9)))))))
      if !networkq.keys.contains("\(targetQ.keys.count)") {
          var wnew_yk: Float = 2.0
          var dateg: String! = String(cString: [97,108,108,111,99,97,116,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &dateg) { pointer in
    
         }
          var presetK: [String: Any]! = [String(cString: [100,101,115,99,101,110,100,101,114,0], encoding: .utf8)!:38, String(cString: [112,97,115,115,101,100,0], encoding: .utf8)!:47, String(cString: [115,108,105,99,101,116,121,112,101,0], encoding: .utf8)!:72]
          var emptyr: [Any]! = [69, 3]
         networkq[goldk] = emptyr.count
         wnew_yk -= Float(targetQ.keys.count)
         dateg.append("\(((rangeF ? 3 : 1) << (Swift.min(labs(2), 4))))")
         presetK[dateg] = docE.count % (Swift.max(dateg.count, 1))
      }
       var baseS: Float = 5.0
       _ = baseS
      while ((Double(docE.count) / (Swift.max(4, total9))) < 1.33 && (1.33 / (Swift.max(4, total9))) < 2.30) {
         total9 /= Swift.max((Double(1 + (rangeF ? 4 : 5))), 5)
         break
      }
          var messagesK: Int = 0
          var taskC: Double = 5.0
          _ = taskC
         goldk = "\(((rangeF ? 5 : 4) | Int(baseS > 150249422.0 || baseS < -150249422.0 ? 38.0 : baseS)))"
         messagesK -= 3
         taskC -= Double(messagesK | 2)
         networkq["\(total9)"] = (Int(total9 > 292371209.0 || total9 < -292371209.0 ? 60.0 : total9))
      reusables = [(Int(total9 > 185510157.0 || total9 < -185510157.0 ? 100.0 : total9) % (Swift.max(reusables.count, 6)))]
   }

        let label = UILabel()
        label.textColor = .white
        label.font = .systemFont(ofSize: 20, weight: .semibold)
        return label
    }()

    private lazy var coinShopButton: UIButton = {
       var bnew_89n: Double = 4.0
       var shuffleds: [Any]! = [String(cString: [114,116,99,99,0], encoding: .utf8)!, String(cString: [102,117,110,99,116,105,111,110,115,0], encoding: .utf8)!]
       _ = shuffleds
       var push5: Int = 3
      while (shuffleds.contains { $0 as? Int == push5 }) {
         shuffleds = [push5 + shuffleds.count]
         break
      }
      while (shuffleds.contains { $0 as? Int == push5 }) {
         shuffleds = [push5 & 3]
         break
      }
          var edgesk: Double = 2.0
         shuffleds.append(push5 | 1)
         edgesk += (Double(Int(edgesk > 160858743.0 || edgesk < -160858743.0 ? 53.0 : edgesk)))
      repeat {
          var clips3: Double = 0.0
          _ = clips3
         push5 += 1
         clips3 -= Double(1)
         if 1792551 == push5 {
            break
         }
      } while ((push5 % (Swift.max(shuffleds.count, 4))) == 3) && (1792551 == push5)
          var sectiona: String! = String(cString: [100,108,114,114,0], encoding: .utf8)!
          var statusg: [String: Any]! = [String(cString: [104,97,108,116,0], encoding: .utf8)!:21, String(cString: [115,111,108,105,115,116,101,110,0], encoding: .utf8)!:32, String(cString: [115,115,108,114,111,111,116,115,0], encoding: .utf8)!:51]
         withUnsafeMutablePointer(to: &statusg) { pointer in
    
         }
         push5 /= Swift.max(2, sectiona.count + 1)
         statusg = ["\(shuffleds.count)": shuffleds.count]
         push5 >>= Swift.min(3, labs(push5))
      bnew_89n -= (Double(Int(bnew_89n > 166701830.0 || bnew_89n < -166701830.0 ? 38.0 : bnew_89n) << (Swift.min(shuffleds.count, 3))))

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setBackgroundImage(UIImage(named: "profile_coin"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapCoinShop), for: .touchUpInside)
        return dimmingButton
    }()

    private lazy var reviseButton: UIButton = {
       var priceg: Double = 5.0
      priceg += Double(3)

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setBackgroundImage(UIImage(named: "profile_revise"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapRevise), for: .touchUpInside)
        return dimmingButton
    }()

    private lazy var setupButton: UIButton = {
       var class_4a: [String: Any]! = [String(cString: [117,116,99,116,105,109,101,0], encoding: .utf8)!:String(cString: [103,97,109,101,0], encoding: .utf8)!, String(cString: [105,102,97,100,100,114,115,0], encoding: .utf8)!:String(cString: [101,110,117,109,115,0], encoding: .utf8)!]
    var followe: Int = 2
   withUnsafeMutablePointer(to: &followe) { pointer in
    
   }
      class_4a["\(followe)"] = class_4a.count

        let dimmingButton = UIButton(type: .custom)
   repeat {
      followe ^= followe
      if 3872671 == followe {
         break
      }
   } while (3 == (4 << (Swift.min(2, labs(followe)))) || 4 == (followe << (Swift.min(labs(followe), 3)))) && (3872671 == followe)
        dimmingButton.setBackgroundImage(UIImage(named: "profile_setup"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapSetup), for: .touchUpInside)
        return dimmingButton
    }()

    private let releaseLabel: UILabel = {
       var packagesP: String! = String(cString: [100,101,99,111,100,101,120,0], encoding: .utf8)!
   repeat {
       var readyp: String! = String(cString: [103,108,111,119,0], encoding: .utf8)!
       var indexq: Double = 4.0
       var controlF: String! = String(cString: [97,99,111,110,102,105,103,0], encoding: .utf8)!
       var p_width7: String! = String(cString: [99,102,104,100,0], encoding: .utf8)!
       var dotR: Double = 1.0
         p_width7 = "\(2)"
      repeat {
          var extO: Double = 1.0
          var itemsa: String! = String(cString: [102,108,105,112,112,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &itemsa) { pointer in
                _ = pointer.pointee
         }
         dotR /= Swift.max(Double(2), 1)
         extO *= Double(itemsa.count)
         itemsa = "\((Int(indexq > 110970768.0 || indexq < -110970768.0 ? 99.0 : indexq) % (Swift.max(itemsa.count, 2))))"
         if 1513024.0 == dotR {
            break
         }
      } while (dotR > indexq) && (1513024.0 == dotR)
         p_width7.append("\(1)")
      repeat {
         dotR -= Double(1 | p_width7.count)
         if dotR == 3482722.0 {
            break
         }
      } while (5.43 < dotR) && (dotR == 3482722.0)
      while ((controlF.count * 5) < 2 || (dotR / (Swift.max(Double(controlF.count), 8))) < 2.7) {
          var blacklistedg: [String: Any]! = [String(cString: [101,110,99,111,100,97,98,108,101,115,0], encoding: .utf8)!:96, String(cString: [98,108,117,114,97,121,0], encoding: .utf8)!:8, String(cString: [114,101,109,111,118,97,108,0], encoding: .utf8)!:32]
          var group4: String! = String(cString: [115,108,111,112,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &group4) { pointer in
                _ = pointer.pointee
         }
          var n_playerQ: Double = 2.0
         withUnsafeMutablePointer(to: &n_playerQ) { pointer in
                _ = pointer.pointee
         }
          var rightj: String! = String(cString: [117,112,112,101,114,0], encoding: .utf8)!
         dotR /= Swift.max((Double(rightj == (String(cString:[68,0], encoding: .utf8)!) ? group4.count : rightj.count)), 1)
         blacklistedg[p_width7] = p_width7.count % 1
         n_playerQ /= Swift.max(1, Double(blacklistedg.values.count * 2))
         break
      }
      for _ in 0 ..< 3 {
         p_width7 = "\(((String(cString:[80,0], encoding: .utf8)!) == readyp ? readyp.count : p_width7.count))"
      }
      for _ in 0 ..< 1 {
         readyp.append("\(p_width7.count)")
      }
         p_width7.append("\(readyp.count)")
          var roomk: [Any]! = [96, 65, 87]
          _ = roomk
          var indicesj: Bool = true
         withUnsafeMutablePointer(to: &indicesj) { pointer in
    
         }
          var linesq: String! = String(cString: [97,116,111,109,105,99,115,0], encoding: .utf8)!
          _ = linesq
         p_width7.append("\(((indicesj ? 5 : 1)))")
         roomk = [(Int(indexq > 16776155.0 || indexq < -16776155.0 ? 52.0 : indexq))]
         linesq.append("\(((String(cString:[87,0], encoding: .utf8)!) == p_width7 ? p_width7.count : Int(dotR > 151563716.0 || dotR < -151563716.0 ? 94.0 : dotR)))")
         readyp.append("\(1)")
      for _ in 0 ..< 2 {
          var cellV: Bool = true
          var room5: Double = 1.0
          var graph8: String! = String(cString: [97,121,117,118,108,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &graph8) { pointer in
    
         }
         controlF.append("\(3)")
         cellV = dotR <= Double(readyp.count)
         room5 -= Double(graph8.count / (Swift.max(4, readyp.count)))
         graph8 = "\((3 - Int(room5 > 302999143.0 || room5 < -302999143.0 ? 42.0 : room5)))"
      }
         controlF = "\(2)"
      repeat {
         controlF = "\(3)"
         if controlF == (String(cString:[110,120,50,108,57,110,116,55,116,0], encoding: .utf8)!) {
            break
         }
      } while ((controlF.count >> (Swift.min(labs(3), 5))) > 2 && (3 + Int(indexq > 47725693.0 || indexq < -47725693.0 ? 63.0 : indexq)) > 4) && (controlF == (String(cString:[110,120,50,108,57,110,116,55,116,0], encoding: .utf8)!))
      if readyp.count >= 4 {
         dotR -= Double(controlF.count / (Swift.max(3, readyp.count)))
      }
         controlF.append("\((Int(dotR > 23266742.0 || dotR < -23266742.0 ? 41.0 : dotR)))")
      packagesP.append("\(controlF.count >> (Swift.min(labs(2), 2)))")
      if (String(cString:[109,54,52,55,101,52,122,0], encoding: .utf8)!) == packagesP {
         break
      }
   } while (packagesP.count == 3) && ((String(cString:[109,54,52,55,101,52,122,0], encoding: .utf8)!) == packagesP)

        let label = UILabel()
        label.text = "release"
        label.textColor = .white
        label.font = UIFont.italicSystemFont(ofSize: 20)
        return label
    }()

    override init(frame: CGRect) {
        super.init(frame: frame)
        backgroundColor = .black
        setupUI()
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    func configure(with user: DSContact) {
       var v_manager_: Double = 3.0
    var switch_2cQ: Int = 1
      switch_2cQ += (switch_2cQ << (Swift.min(labs(Int(v_manager_ > 141959505.0 || v_manager_ < -141959505.0 ? 85.0 : v_manager_)), 2)))

   if (v_manager_ + v_manager_) <= 4.14 || (v_manager_ + v_manager_) <= 4.14 {
       var attributes6: Float = 3.0
       var reportJ: Double = 3.0
       var updatedA: String! = String(cString: [119,105,112,101,0], encoding: .utf8)!
       var table2: Bool = true
       _ = table2
       var suggesteda: String! = String(cString: [111,112,101,114,97,110,100,0], encoding: .utf8)!
      while (!table2 && 5 >= suggesteda.count) {
          var submitG: [String: Any]! = [String(cString: [105,110,100,101,102,105,110,105,116,101,108,121,0], encoding: .utf8)!:UILabel()]
          _ = submitG
          var used0: String! = String(cString: [116,104,114,101,97,100,110,97,109,101,0], encoding: .utf8)!
         table2 = attributes6 > 31.98 && reportJ > 31.98
         submitG["\(reportJ)"] = (Int(reportJ > 358109488.0 || reportJ < -358109488.0 ? 66.0 : reportJ) & (table2 ? 5 : 3))
         used0.append("\((Int(reportJ > 120402970.0 || reportJ < -120402970.0 ? 31.0 : reportJ)))")
         break
      }
         updatedA = "\((suggesteda == (String(cString:[106,0], encoding: .utf8)!) ? suggesteda.count : (table2 ? 1 : 5)))"
         reportJ /= Swift.max((Double(updatedA == (String(cString:[55,0], encoding: .utf8)!) ? updatedA.count : (table2 ? 3 : 3))), 2)
      if (2.31 - attributes6) < 1.90 || (suggesteda.count - Int(attributes6 > 4199775.0 || attributes6 < -4199775.0 ? 10.0 : attributes6)) < 2 {
         attributes6 += Float(3)
      }
       var navf: Double = 4.0
       _ = navf
       var textN: Double = 0.0
       _ = textN
       var feedw: String! = String(cString: [97,116,116,97,99,104,109,101,110,116,115,0], encoding: .utf8)!
       var textW: Float = 2.0
       _ = textW
       var auto_rU: Float = 4.0
         auto_rU /= Swift.max(4, (Float(Int(navf > 113756671.0 || navf < -113756671.0 ? 83.0 : navf) + Int(attributes6 > 368993871.0 || attributes6 < -368993871.0 ? 32.0 : attributes6))))
      repeat {
         auto_rU -= (Float(feedw.count ^ Int(attributes6 > 228052972.0 || attributes6 < -228052972.0 ? 84.0 : attributes6)))
         if 4095208.0 == auto_rU {
            break
         }
      } while (4095208.0 == auto_rU) && (Float(navf) >= auto_rU)
         textW -= (Float(1 << (Swift.min(labs(Int(auto_rU > 102711150.0 || auto_rU < -102711150.0 ? 23.0 : auto_rU)), 5))))
      while (auto_rU > Float(feedw.count)) {
         feedw.append("\((Int(navf > 51014348.0 || navf < -51014348.0 ? 82.0 : navf) << (Swift.min(labs(2), 3))))")
         break
      }
       var poolK: String! = String(cString: [102,108,105,103,104,116,0], encoding: .utf8)!
       _ = poolK
       var normalu: String! = String(cString: [120,108,97,98,101,108,104,101,105,103,104,116,0], encoding: .utf8)!
       var publishZ: String! = String(cString: [99,111,109,112,97,114,97,116,111,114,0], encoding: .utf8)!
       var blackh: String! = String(cString: [115,116,97,116,117,115,111,114,0], encoding: .utf8)!
       _ = blackh
      repeat {
         blackh = "\(3 << (Swift.min(4, feedw.count)))"
         if 567325 == blackh.count {
            break
         }
      } while (567325 == blackh.count) && (updatedA.count > blackh.count)
       var min_fa: Bool = false
       var selV: Bool = true
         textN /= Swift.max((Double(Int(textW > 123829686.0 || textW < -123829686.0 ? 95.0 : textW) ^ Int(attributes6 > 8568438.0 || attributes6 < -8568438.0 ? 75.0 : attributes6))), 4)
         poolK = "\((Int(reportJ > 325509787.0 || reportJ < -325509787.0 ? 45.0 : reportJ) / 3))"
         normalu.append("\((Int(textN > 330319580.0 || textN < -330319580.0 ? 71.0 : textN) % 2))")
         publishZ = "\((1 % (Swift.max(5, Int(reportJ > 195359002.0 || reportJ < -195359002.0 ? 20.0 : reportJ)))))"
         min_fa = 94 > feedw.count
         selV = updatedA.count < suggesteda.count
      v_manager_ -= (Double(2 + Int(reportJ > 187828653.0 || reportJ < -187828653.0 ? 6.0 : reportJ)))
   }
        nameLabel.text = user.userName
        let configured = DSSecondaryNews.shared.avatarImage(for: user) ?? UserData.image(for: user.avatarUrl)
        coverImageView.image = configured
        avatarImageView.image = configured
    }

    func configure(with info: DSTabbarSign) {
       var videoy: String! = String(cString: [122,111,110,101,0], encoding: .utf8)!
   while (videoy.count < 1) {
      videoy = "\(videoy.count)"
      break
   }

        nameLabel.text = info.userName
        let configured = UserData.image(for: info.avatarImageName ?? info.coverImageName)
        coverImageView.image = configured
        avatarImageView.image = configured
    }

    private func setupUI() {
       var footeru: Double = 1.0
   withUnsafeMutablePointer(to: &footeru) { pointer in
    
   }
      footeru -= Double(2)

        addSubview(coverContainerView)
        coverContainerView.addSubview(coverImageView)
        addSubview(avatarImageView)
        addSubview(nameLabel)
        addSubview(coinShopButton)
        addSubview(reviseButton)
        addSubview(setupButton)
        addSubview(releaseLabel)

        coverContainerView.snp.makeConstraints { make in
            make.top.leading.trailing.equalToSuperview()
            make.height.equalTo(350)
        }

        coverImageView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        avatarImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalTo(coverContainerView.snp.bottom).offset(-56)
            make.width.height.equalTo(112)
        }

        nameLabel.snp.makeConstraints { make in
            make.leading.equalTo(avatarImageView.snp.trailing).offset(9)
            make.trailing.lessThanOrEqualToSuperview().inset(Layout.horizontalInset)
            make.top.equalTo(coverContainerView.snp.bottom)
        }

        coinShopButton.snp.makeConstraints { make in
            make.top.equalTo(avatarImageView.snp.bottom).offset(14)
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.height.equalTo(80)
        }

        reviseButton.snp.makeConstraints { make in
            make.top.equalTo(coinShopButton.snp.bottom).offset(14)
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
            make.trailing.equalTo(setupButton.snp.leading).offset(-7)
            make.height.equalTo(80)
        }

        setupButton.snp.makeConstraints { make in
            make.centerY.width.height.equalTo(reviseButton)
            make.trailing.equalToSuperview().inset(Layout.horizontalInset)
        }

        releaseLabel.snp.makeConstraints { make in
            make.top.equalTo(reviseButton.snp.bottom).offset(16)
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
        }
    }

    @objc private func didTapCoinShop() {
       var completionA: Double = 0.0
   while (completionA < completionA) {
      completionA += (Double(2 & Int(completionA > 345254074.0 || completionA < -345254074.0 ? 82.0 : completionA)))
      break
   }

        onCoinShopTapped?()
    }

    @objc private func didTapRevise() {
       var phrase6: [String: Any]! = [String(cString: [119,97,108,107,0], encoding: .utf8)!:78, String(cString: [117,115,105,110,103,0], encoding: .utf8)!:99]
    _ = phrase6
    var editI: String! = String(cString: [114,111,117,110,100,110,101,115,115,0], encoding: .utf8)!
   repeat {
       var screenD: Double = 3.0
       var safed: Float = 3.0
      withUnsafeMutablePointer(to: &safed) { pointer in
             _ = pointer.pointee
      }
      while (3.55 < (3.89 + safed) || 1.36 < (3.89 + safed)) {
         screenD -= (Double(Int(safed > 212660758.0 || safed < -212660758.0 ? 83.0 : safed)))
         break
      }
         screenD -= (Double(Int(safed > 387097331.0 || safed < -387097331.0 ? 69.0 : safed)))
      for _ in 0 ..< 1 {
          var scrollk: String! = String(cString: [104,121,115,116,101,114,101,115,105,115,0], encoding: .utf8)!
          _ = scrollk
         safed /= Swift.max(4, (Float(Int(screenD > 192076694.0 || screenD < -192076694.0 ? 47.0 : screenD) >> (Swift.min(3, labs(2))))))
         scrollk.append("\((Int(screenD > 301196245.0 || screenD < -301196245.0 ? 50.0 : screenD)))")
      }
       var ready0: String! = String(cString: [115,109,106,112,101,103,0], encoding: .utf8)!
       _ = ready0
         screenD /= Swift.max((Double(Int(safed > 169836846.0 || safed < -169836846.0 ? 1.0 : safed) - 2)), 3)
      while (safed < Float(ready0.count)) {
         safed /= Swift.max((Float(ready0.count & Int(screenD > 265363245.0 || screenD < -265363245.0 ? 56.0 : screenD))), 4)
         break
      }
      phrase6 = ["\(phrase6.values.count)": 3]
      if 2323669 == phrase6.count {
         break
      }
   } while (editI.count > 4) && (2323669 == phrase6.count)
   repeat {
      editI.append("\(editI.count)")
      if editI == (String(cString:[117,106,109,101,105,99,49,117,0], encoding: .utf8)!) {
         break
      }
   } while (editI == (String(cString:[117,106,109,101,105,99,49,117,0], encoding: .utf8)!)) && (3 > (editI.count << (Swift.min(labs(1), 5))) || 1 > (editI.count << (Swift.min(4, phrase6.count))))

        onReviseTapped?()
    }

    @objc private func didTapSetup() {
       var generatorL: String! = String(cString: [114,97,105,116,0], encoding: .utf8)!
    var trailing1: Double = 1.0
      trailing1 += Double(1)
   while (generatorL.count <= (Int(trailing1 > 253026014.0 || trailing1 < -253026014.0 ? 45.0 : trailing1))) {
      trailing1 += Double(2)
      break
   }

   repeat {
      generatorL.append("\(1 | generatorL.count)")
      if generatorL.count == 1358454 {
         break
      }
   } while (!generatorL.hasSuffix("\(generatorL.count)")) && (generatorL.count == 1358454)
        onSetupTapped?()
    }
}
