
import Foundation

import UIKit
import Toast_Swift

class DSRegisterController: DSEmptyController {

    private enum Layout {
        static let horizontalInset: CGFloat = 24
        static let navBarHeight: CGFloat = 44
        static let accentColor = UIColor(hex: "#7327C8")
    }

    static let supportEmail = "Xixifeadback@gmail.com"

    private let navBarView: UIView = {
       var packagea: String! = String(cString: [121,98,108,111,99,107,0], encoding: .utf8)!
    _ = packagea
    var secondaryy: [Any]! = [63.0]
    _ = secondaryy
      secondaryy = [3]

        let view = UIView()
   if packagea == String(cString:[78,0], encoding: .utf8)! {
      packagea = "\((packagea == (String(cString:[121,0], encoding: .utf8)!) ? packagea.count : packagea.count))"
   }
        view.backgroundColor = .black
        return view
    }()

    private lazy var backButton: UIButton = {
       var pushC: String! = String(cString: [121,101,108,108,111,119,0], encoding: .utf8)!
       var cornerO: String! = String(cString: [100,105,108,97,116,101,0], encoding: .utf8)!
       var bundledU: [Any]! = [32, 0]
       var candidateB: Float = 2.0
       var processedy: Double = 1.0
       var scrollK: Double = 4.0
      if 3.57 == (2.95 * scrollK) {
         scrollK -= Double(1 - bundledU.count)
      }
      for _ in 0 ..< 3 {
         scrollK -= (Double(Int(processedy > 322991869.0 || processedy < -322991869.0 ? 63.0 : processedy)))
      }
      if (processedy + scrollK) >= 1.25 {
         processedy -= (Double(Int(scrollK > 196626082.0 || scrollK < -196626082.0 ? 25.0 : scrollK)))
      }
      repeat {
         scrollK += Double(1)
         if 2268950.0 == scrollK {
            break
         }
      } while (2268950.0 == scrollK) && ((candidateB - 5.58) == 4.49 || 3.45 == (5.58 + scrollK))
         processedy /= Swift.max(Double(3), 5)
      while (4.88 == (3.82 + scrollK)) {
         bundledU = [(Int(processedy > 143905127.0 || processedy < -143905127.0 ? 84.0 : processedy))]
         break
      }
      if cornerO.contains("\(scrollK)") {
         scrollK /= Swift.max((Double(Int(candidateB > 159385522.0 || candidateB < -159385522.0 ? 45.0 : candidateB))), 5)
      }
          var ask7: Double = 4.0
          var checkh: String! = String(cString: [116,97,112,102,105,108,116,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &checkh) { pointer in
                _ = pointer.pointee
         }
         candidateB -= (Float(bundledU.count & Int(ask7 > 3333738.0 || ask7 < -3333738.0 ? 39.0 : ask7)))
         checkh = "\(1)"
      pushC = "\(bundledU.count & 3)"

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setImage(UIImage(named: "common_back"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapBack), for: .touchUpInside)
        return dimmingButton
    }()

    private let titleLabel: UILabel = {
       var addU: Float = 3.0
       var segmenty: String! = String(cString: [108,101,116,115,0], encoding: .utf8)!
       _ = segmenty
       var min_pU: String! = String(cString: [100,101,115,116,114,111,121,101,100,0], encoding: .utf8)!
       var min_z8: String! = String(cString: [105,108,108,105,113,97,0], encoding: .utf8)!
         segmenty.append("\((segmenty == (String(cString:[116,0], encoding: .utf8)!) ? min_pU.count : segmenty.count))")
      repeat {
         min_pU.append("\(2)")
         if min_pU.count == 1648048 {
            break
         }
      } while (segmenty.hasSuffix(min_pU)) && (min_pU.count == 1648048)
       var message0: String! = String(cString: [115,110,97,107,101,0], encoding: .utf8)!
       _ = message0
         min_pU.append("\(segmenty.count << (Swift.min(min_pU.count, 3)))")
      for _ in 0 ..< 1 {
          var creatione: String! = String(cString: [98,101,105,103,110,101,116,0], encoding: .utf8)!
          var commentatorsQ: String! = String(cString: [112,111,112,117,112,0], encoding: .utf8)!
          _ = commentatorsQ
          var daten: Float = 3.0
          var goldS: [Any]! = [4, 20, 77]
         min_pU.append("\(3 >> (Swift.min(3, min_pU.count)))")
         creatione.append("\((1 / (Swift.max(2, Int(daten > 197326247.0 || daten < -197326247.0 ? 29.0 : daten)))))")
         commentatorsQ = "\(goldS.count)"
         daten /= Swift.max((Float(Int(daten > 381619498.0 || daten < -381619498.0 ? 21.0 : daten))), 4)
         goldS = [min_z8.count]
      }
      while (min_z8.hasSuffix(message0)) {
         message0 = "\(segmenty.count - 2)"
         break
      }
      for _ in 0 ..< 1 {
          var urlsc: Double = 3.0
          var cornerK: String! = String(cString: [98,97,115,101,0], encoding: .utf8)!
          var argv: String! = String(cString: [99,101,108,102,0], encoding: .utf8)!
          var observerD: String! = String(cString: [105,110,116,101,103,114,97,108,0], encoding: .utf8)!
          _ = observerD
          var removeh: Bool = true
         min_pU = "\(cornerK.count - argv.count)"
         urlsc += (Double((String(cString:[112,0], encoding: .utf8)!) == min_z8 ? message0.count : min_z8.count))
         observerD = "\(1 * min_z8.count)"
         removeh = observerD.count == 48
      }
         min_pU.append("\(3 >> (Swift.min(1, min_z8.count)))")
         message0 = "\(segmenty.count)"
      addU /= Swift.max(Float(2), 3)

        let label = UILabel()
        label.text = "Contact Us"
        label.textColor = .white
        label.font = UIFont.italicSystemFont(ofSize: 22)
        label.textAlignment = .center
        return label
    }()

    private let iconImageView: UIImageView = {
       var firstK: Float = 2.0
    _ = firstK
   for _ in 0 ..< 1 {
       var supportZ: [Any]! = [99, 38]
       var mailp: String! = String(cString: [99,111,110,116,97,105,110,101,100,0], encoding: .utf8)!
       var maskD: Double = 3.0
       var index4: Float = 2.0
      withUnsafeMutablePointer(to: &index4) { pointer in
    
      }
      repeat {
         maskD -= Double(supportZ.count)
         if maskD == 2361217.0 {
            break
         }
      } while (3.14 >= (4.27 * maskD) || 1.29 >= (4.27 - maskD)) && (maskD == 2361217.0)
         mailp.append("\((Int(maskD > 193820050.0 || maskD < -193820050.0 ? 16.0 : maskD)))")
       var graphT: String! = String(cString: [97,116,116,101,109,116,115,0], encoding: .utf8)!
         supportZ = [graphT.count - supportZ.count]
      while ((index4 * 5.96) > 4.53 || 2 > (graphT.count % 4)) {
         graphT = "\(mailp.count)"
         break
      }
         mailp.append("\(3 >> (Swift.min(3, supportZ.count)))")
      repeat {
         graphT = "\((Int(index4 > 274173320.0 || index4 < -274173320.0 ? 53.0 : index4)))"
         if graphT.count == 3910139 {
            break
         }
      } while (graphT.count == 3910139) && (!graphT.contains("\(index4)"))
      for _ in 0 ..< 2 {
         mailp.append("\(3)")
      }
         index4 -= Float(1 - mailp.count)
      for _ in 0 ..< 2 {
         index4 /= Swift.max((Float(mailp == (String(cString:[70,0], encoding: .utf8)!) ? supportZ.count : mailp.count)), 4)
      }
      while (supportZ.count > 4) {
          var docS: [Any]! = [79, 17]
          _ = docS
          var storedm: [String: Any]! = [String(cString: [122,95,50,53,0], encoding: .utf8)!:74, String(cString: [109,99,108,109,115,0], encoding: .utf8)!:100]
         index4 -= Float(docS.count ^ graphT.count)
         storedm = ["\(storedm.values.count)": 3 | storedm.count]
         break
      }
         graphT = "\(graphT.count ^ 2)"
      firstK /= Swift.max(2, (Float(supportZ.count ^ Int(firstK > 371328007.0 || firstK < -371328007.0 ? 54.0 : firstK))))
   }

        let config = UIImage.SymbolConfiguration(pointSize: 56, weight: .medium)
        let image = UIImage(systemName: "envelope.fill", withConfiguration: config)
        let imageView = UIImageView(image: image)
        imageView.tintColor = Layout.accentColor
        imageView.contentMode = .scaleAspectFit
        return imageView
    }()

    private let descriptionLabel: UILabel = {
       var friend_oi: String! = String(cString: [109,111,100,101,109,118,99,111,115,116,0], encoding: .utf8)!
   if friend_oi.count == 5 {
      friend_oi = "\(friend_oi.count / (Swift.max(2, 6)))"
   }

        let label = UILabel()
        label.text = "If you have questions, feedback, or need assistance, please contact us by email."
        label.textColor = UIColor.white.withAlphaComponent(0.75)
        label.font = .systemFont(ofSize: 16, weight: .regular)
        label.textAlignment = .center
        label.numberOfLines = 0
        return label
    }()

    private let emailCardView: UIView = {
       var iconm: String! = String(cString: [99,115,114,105,100,0], encoding: .utf8)!
   repeat {
       var personI: [Any]! = [93, 62, 86]
       var indexK: String! = String(cString: [115,119,97,112,112,101,114,0], encoding: .utf8)!
       _ = indexK
       var layoutu: Double = 3.0
       var loading4: String! = String(cString: [109,109,120,101,120,116,0], encoding: .utf8)!
       var checkM: String! = String(cString: [117,110,101,100,105,116,97,98,108,101,0], encoding: .utf8)!
       _ = checkM
      while (indexK.hasSuffix("\(layoutu)")) {
         indexK.append("\((2 | Int(layoutu > 308003847.0 || layoutu < -308003847.0 ? 50.0 : layoutu)))")
         break
      }
       var currentZ: String! = String(cString: [121,117,118,112,97,99,107,101,100,0], encoding: .utf8)!
       var messagesT: String! = String(cString: [100,97,112,112,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var config1: Double = 5.0
          _ = config1
         layoutu += (Double(Int(layoutu > 256547866.0 || layoutu < -256547866.0 ? 24.0 : layoutu) >> (Swift.min(labs(3), 1))))
         config1 /= Swift.max(1, Double(loading4.count / 3))
      }
      if !checkM.hasSuffix(loading4) {
         checkM.append("\(((String(cString:[52,0], encoding: .utf8)!) == currentZ ? messagesT.count : currentZ.count))")
      }
      repeat {
         checkM.append("\(((String(cString:[110,0], encoding: .utf8)!) == currentZ ? personI.count : currentZ.count))")
         if checkM.count == 2772663 {
            break
         }
      } while (loading4 == String(cString:[108,0], encoding: .utf8)!) && (checkM.count == 2772663)
      repeat {
         personI.append(2)
         if 2131546 == personI.count {
            break
         }
      } while (2131546 == personI.count) && (personI.contains { $0 as? Double == layoutu })
       var arg7: [Any]! = [35, 12]
         layoutu += Double(indexK.count / 2)
      while (messagesT.contains(indexK)) {
          var httpu: Double = 0.0
          var delaym: Float = 0.0
          var max_obO: String! = String(cString: [112,114,101,100,105,99,116,111,114,0], encoding: .utf8)!
          var remoteG: [String: Any]! = [String(cString: [97,99,101,110,99,0], encoding: .utf8)!:[70, 67, 51]]
         withUnsafeMutablePointer(to: &remoteG) { pointer in
    
         }
         messagesT.append("\(currentZ.count)")
         httpu += Double(currentZ.count + personI.count)
         delaym -= Float(remoteG.count - 1)
         max_obO.append("\(((String(cString:[90,0], encoding: .utf8)!) == max_obO ? remoteG.values.count : max_obO.count))")
         break
      }
         messagesT.append("\(personI.count << (Swift.min(loading4.count, 5)))")
          var mode0: Float = 3.0
         checkM = "\(loading4.count % (Swift.max(2, 9)))"
         mode0 /= Swift.max(Float(currentZ.count), 5)
       var http8: Float = 4.0
      repeat {
          var encodede: String! = String(cString: [100,117,109,112,112,97,99,107,101,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &encodede) { pointer in
                _ = pointer.pointee
         }
          var min_s9: [String: Any]! = [String(cString: [112,101,97,107,0], encoding: .utf8)!:36, String(cString: [112,97,101,116,104,0], encoding: .utf8)!:2]
          _ = min_s9
         loading4.append("\(indexK.count ^ messagesT.count)")
         encodede.append("\(arg7.count * encodede.count)")
         min_s9["\(layoutu)"] = 2
         if 1712041 == loading4.count {
            break
         }
      } while (currentZ.count <= loading4.count) && (1712041 == loading4.count)
       var tracksL: String! = String(cString: [98,114,105,100,103,101,0], encoding: .utf8)!
       var networkm: String! = String(cString: [107,101,121,101,100,0], encoding: .utf8)!
       _ = networkm
         arg7 = [indexK.count]
         http8 -= (Float(Int(layoutu > 353958119.0 || layoutu < -353958119.0 ? 70.0 : layoutu) % 1))
         tracksL = "\(currentZ.count / (Swift.max(7, personI.count)))"
         networkm = "\((Int(layoutu > 296067345.0 || layoutu < -296067345.0 ? 17.0 : layoutu) ^ 2))"
      iconm = "\((Int(layoutu > 303524776.0 || layoutu < -303524776.0 ? 80.0 : layoutu) | 1))"
      if iconm == (String(cString:[53,95,54,48,52,95,49,117,97,104,0], encoding: .utf8)!) {
         break
      }
   } while (iconm == (String(cString:[53,95,54,48,52,95,49,117,97,104,0], encoding: .utf8)!)) && (iconm.count == 4)

        let view = UIView()
        view.backgroundColor = UIColor(white: 0.12, alpha: 1)
        view.layer.cornerRadius = 16
        view.layer.borderWidth = 1
        view.layer.borderColor = UIColor.white.withAlphaComponent(0.12).cgColor
        return view
    }()

    private let emailTitleLabel: UILabel = {
       var namesa: String! = String(cString: [101,115,116,105,109,97,116,105,110,103,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &namesa) { pointer in
    
   }
   while (namesa.count >= 3) {
      namesa.append("\(namesa.count ^ 3)")
      break
   }

        let label = UILabel()
        label.text = "Email"
        label.textColor = UIColor.white.withAlphaComponent(0.6)
        label.font = .systemFont(ofSize: 14, weight: .medium)
        label.textAlignment = .center
        return label
    }()

    private lazy var emailLabel: UILabel = {
       var seeded4: Double = 5.0
    var dimmingg: Bool = false
   while (dimmingg) {
       var shopp: Int = 1
          var audior: Double = 4.0
          var decodeda: Double = 2.0
         shopp >>= Swift.min(3, labs(1))
         audior += (Double(Int(audior > 277893603.0 || audior < -277893603.0 ? 81.0 : audior) % (Swift.max(8, Int(decodeda > 217265006.0 || decodeda < -217265006.0 ? 32.0 : decodeda)))))
         decodeda -= (Double(Int(decodeda > 147571899.0 || decodeda < -147571899.0 ? 59.0 : decodeda) << (Swift.min(3, labs(shopp)))))
       var f_imageY: Double = 5.0
         shopp -= shopp
         f_imageY /= Swift.max((Double(Int(f_imageY > 379584619.0 || f_imageY < -379584619.0 ? 4.0 : f_imageY) + 2)), 2)
      seeded4 -= Double(3 << (Swift.min(labs(shopp), 2)))
      break
   }

        let placeholderLabel = UILabel()
       var anew_yhS: Double = 4.0
       var groupA: [String: Any]! = [String(cString: [102,117,114,116,104,101,114,0], encoding: .utf8)!:true]
      withUnsafeMutablePointer(to: &groupA) { pointer in
             _ = pointer.pointee
      }
       var nextZ: Bool = false
         anew_yhS /= Swift.max(Double(groupA.values.count), 5)
         groupA = ["\(groupA.values.count)": (groupA.count | (nextZ ? 2 : 1))]
      if (groupA.count * 4) >= 3 && (groupA.count / (Swift.max(4, 5))) >= 5 {
         anew_yhS -= Double(2)
      }
      while ((3.56 + anew_yhS) > 3.8 && (anew_yhS + 3.56) > 4.8) {
         anew_yhS *= (Double(1 ^ Int(anew_yhS > 148120492.0 || anew_yhS < -148120492.0 ? 55.0 : anew_yhS)))
         break
      }
          var roomu: String! = String(cString: [115,109,97,108,108,0], encoding: .utf8)!
          var n_productsh: [String: Any]! = [String(cString: [113,117,97,100,0], encoding: .utf8)!:84, String(cString: [99,101,108,116,0], encoding: .utf8)!:73]
         withUnsafeMutablePointer(to: &n_productsh) { pointer in
    
         }
          var ready_: Int = 1
         withUnsafeMutablePointer(to: &ready_) { pointer in
                _ = pointer.pointee
         }
         nextZ = ready_ <= 70
         roomu = "\(2 * roomu.count)"
         n_productsh["\(nextZ)"] = (3 ^ Int(anew_yhS > 260748481.0 || anew_yhS < -260748481.0 ? 25.0 : anew_yhS))
       var remoteK: String! = String(cString: [115,101,99,107,101,121,0], encoding: .utf8)!
       var thumbnail7: String! = String(cString: [114,101,115,111,108,118,101,114,115,0], encoding: .utf8)!
       var coinsW: String! = String(cString: [105,109,103,117,116,105,108,115,0], encoding: .utf8)!
       _ = coinsW
       var blueh: Bool = true
      if thumbnail7.count <= groupA.keys.count {
          var leading9: String! = String(cString: [100,114,97,119,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &leading9) { pointer in
                _ = pointer.pointee
         }
          var exit2: [String: Any]! = [String(cString: [115,104,97,108,108,0], encoding: .utf8)!:3.0]
          _ = exit2
          var verticalj: String! = String(cString: [118,101,114,116,105,99,97,108,0], encoding: .utf8)!
         thumbnail7.append("\(groupA.values.count)")
         leading9 = "\(((nextZ ? 4 : 4) << (Swift.min(groupA.count, 5))))"
         exit2 = ["\(exit2.keys.count)": verticalj.count]
         verticalj.append("\(((String(cString:[82,0], encoding: .utf8)!) == coinsW ? (blueh ? 5 : 3) : coinsW.count))")
      }
         remoteK = "\(((blueh ? 1 : 2) - 1))"
      dimmingg = (!dimmingg ? !nextZ : !dimmingg)
        placeholderLabel.text = Self.supportEmail
        placeholderLabel.textColor = Layout.accentColor
        placeholderLabel.font = .systemFont(ofSize: 17, weight: .semibold)
        placeholderLabel.textAlignment = .center
        placeholderLabel.numberOfLines = 0
        placeholderLabel.isUserInteractionEnabled = true
        let presenting = UITapGestureRecognizer(target: self, action: #selector(didTapSendEmail))
        placeholderLabel.addGestureRecognizer(presenting)
        return placeholderLabel
    }()

    private lazy var copyButton: UIButton = {
       var bluej: String! = String(cString: [114,101,112,108,97,99,101,100,0], encoding: .utf8)!
   if bluej.count > 4 {
      bluej = "\(3)"
   }

        let dimmingButton = UIButton(type: .system)
        dimmingButton.setTitle("Copy Email", for: .normal)
        dimmingButton.setTitleColor(.white, for: .normal)
        dimmingButton.titleLabel?.font = .systemFont(ofSize: 17, weight: .semibold)
        dimmingButton.backgroundColor = UIColor(white: 0.2, alpha: 1)
        dimmingButton.layer.cornerRadius = 26
        dimmingButton.addTarget(self, action: #selector(didTapCopyEmail), for: .touchUpInside)
        return dimmingButton
    }()


    override func viewDidLoad() {
       var followB: String! = String(cString: [106,111,117,114,110,97,108,110,97,109,101,0], encoding: .utf8)!
    var a_managery: Float = 2.0
    _ = a_managery
       var hoursE: String! = String(cString: [97,108,112,110,0], encoding: .utf8)!
         hoursE.append("\(hoursE.count)")
      while (hoursE.count <= 1) {
         hoursE = "\(1)"
         break
      }
          var accentv: String! = String(cString: [103,101,110,101,114,97,116,101,0], encoding: .utf8)!
         hoursE = "\(accentv.count | 2)"
      a_managery -= Float(hoursE.count)
      a_managery /= Swift.max((Float(Int(a_managery > 167608742.0 || a_managery < -167608742.0 ? 32.0 : a_managery))), 2)

      followB.append("\(followB.count * followB.count)")
        super.viewDidLoad()
        setupUI()
    }

    private func setupUI() {
       var launchu: [String: Any]! = [String(cString: [120,99,111,100,101,0], encoding: .utf8)!:92, String(cString: [119,112,101,110,100,105,110,103,0], encoding: .utf8)!:5, String(cString: [99,113,117,101,117,101,0], encoding: .utf8)!:25]
   withUnsafeMutablePointer(to: &launchu) { pointer in
          _ = pointer.pointee
   }
      launchu = ["\(launchu.values.count)": 3]

        view.backgroundColor = .black

        view.addSubview(navBarView)
        view.addSubview(iconImageView)
        view.addSubview(descriptionLabel)
        view.addSubview(emailCardView)
        view.addSubview(copyButton)

        navBarView.addSubview(backButton)
        navBarView.addSubview(titleLabel)

        emailCardView.addSubview(emailTitleLabel)
        emailCardView.addSubview(emailLabel)

        navBarView.snp.makeConstraints { make in
            make.top.equalTo(view.safeAreaLayoutGuide)
            make.leading.trailing.equalToSuperview()
            make.height.equalTo(Layout.navBarHeight)
        }

        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(8)
            make.centerY.equalToSuperview()
            make.width.height.equalTo(44)
        }

        titleLabel.snp.makeConstraints { make in
            make.center.equalToSuperview()
        }

        iconImageView.snp.makeConstraints { make in
            make.top.equalTo(navBarView.snp.bottom).offset(48)
            make.centerX.equalToSuperview()
            make.width.height.equalTo(72)
        }

        descriptionLabel.snp.makeConstraints { make in
            make.top.equalTo(iconImageView.snp.bottom).offset(24)
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
        }

        emailCardView.snp.makeConstraints { make in
            make.top.equalTo(descriptionLabel.snp.bottom).offset(32)
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
        }

        emailTitleLabel.snp.makeConstraints { make in
            make.top.equalToSuperview().offset(20)
            make.leading.trailing.equalToSuperview().inset(16)
        }

        emailLabel.snp.makeConstraints { make in
            make.top.equalTo(emailTitleLabel.snp.bottom).offset(8)
            make.leading.trailing.equalToSuperview().inset(16)
            make.bottom.equalToSuperview().offset(-20)
        }

        copyButton.snp.makeConstraints { make in
            make.top.equalTo(emailCardView.snp.bottom).offset(28)
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.height.equalTo(52)
        }
    }

    @objc private func didTapBack() {
       var sessionu: Int = 5
    _ = sessionu
      sessionu ^= 3 * sessionu

        navigationController?.popViewController(animated: true)
    }

    @objc private func didTapCopyEmail() {
       var mergedw: String! = String(cString: [98,101,108,111,110,103,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      mergedw.append("\(mergedw.count * 3)")
   }

        UIPasteboard.general.string = Self.supportEmail
        view.makeToast("Email copied")
    }

    @objc private func didTapSendEmail() {
       var phraseD: String! = String(cString: [108,111,119,0], encoding: .utf8)!
    _ = phraseD
      phraseD = "\((phraseD == (String(cString:[111,0], encoding: .utf8)!) ? phraseD.count : phraseD.count))"

        let error = Self.supportEmail.addingPercentEncoding(withAllowedCharacters: .urlQueryAllowed) ?? Self.supportEmail
        guard let url = URL(string: "mailto:\(error)"),
              UIApplication.shared.canOpenURL(url) else {
            view.makeToast("Unable to open Mail app")
            return
        }
        UIApplication.shared.open(url)
    }
}
