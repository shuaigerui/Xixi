
import Foundation

import UIKit

struct DSReport {
    let userId: String
    let name: String
    let avatarImageName: String?
}

class DSLoginController: DSEmptyController {

    private enum Layout {
        static let navBarContentHeight: CGFloat = 72
        static let headerAvatarSize: CGFloat = 44
        static let inputBarHeight: CGFloat = 52
        static let inputHorizontalInset: CGFloat = 16
        static let actionButtonSize: CGFloat = 52
    }

    private let contact: DSReport

    private var messages: [DSEmptyPush] = []

    private let navBarView: UIView = {
       var feedU: Double = 2.0
    var horizontalI: String! = String(cString: [101,120,97,110,100,101,100,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      horizontalI.append("\(horizontalI.count)")
   }

        let view = UIView()
   for _ in 0 ..< 1 {
      horizontalI.append("\(3)")
   }
        view.backgroundColor = .black
   repeat {
      feedU += (Double(Int(feedU > 140830682.0 || feedU < -140830682.0 ? 87.0 : feedU) >> (Swift.min(labs(Int(feedU > 29238616.0 || feedU < -29238616.0 ? 71.0 : feedU)), 1))))
      if feedU == 4045001.0 {
         break
      }
   } while (2.34 <= (feedU * feedU) && (2.34 * feedU) <= 2.93) && (feedU == 4045001.0)
        return view
    }()

    private lazy var backButton: UIButton = {
       var emailu: String! = String(cString: [118,105,115,98,108,101,0], encoding: .utf8)!
       var updatest: String! = String(cString: [97,118,118,115,0], encoding: .utf8)!
         updatest = "\(updatest.count)"
      while (!updatest.hasSuffix(updatest)) {
         updatest.append("\(((String(cString:[84,0], encoding: .utf8)!) == updatest ? updatest.count : updatest.count))")
         break
      }
      if updatest == String(cString:[51,0], encoding: .utf8)! {
          var formP: Double = 2.0
          var email1: Double = 4.0
         withUnsafeMutablePointer(to: &email1) { pointer in
                _ = pointer.pointee
         }
         updatest = "\(3)"
         formP /= Swift.max((Double(Int(email1 > 11627725.0 || email1 < -11627725.0 ? 67.0 : email1))), 1)
      }
      emailu = "\(updatest.count)"

        let dimmingButton = UIButton(type: .system)
        let tab = UIImage.SymbolConfiguration(pointSize: 20, weight: .semibold)
        dimmingButton.setImage(UIImage(systemName: "chevron.left", withConfiguration: tab), for: .normal)
        dimmingButton.tintColor = .white
        dimmingButton.addTarget(self, action: #selector(didTapBack), for: .touchUpInside)
        return dimmingButton
    }()

    private lazy var moreButton: UIButton = {
       var playW: Bool = false
   for _ in 0 ..< 1 {
      playW = playW || !playW
   }

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setImage(UIImage(named: "chat_more"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapMore), for: .touchUpInside)
        return dimmingButton
    }()

    private let headerAvatarImageView: UIImageView = {
       var recommendl: String! = String(cString: [108,114,111,110,100,0], encoding: .utf8)!
    _ = recommendl
      recommendl.append("\(recommendl.count)")

        let imageView = UIImageView()
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        imageView.backgroundColor = UIColor.hex("#3A3A3C")
        imageView.layer.cornerRadius = Layout.headerAvatarSize / 2
        return imageView
    }()

    private let headerNameLabel: UILabel = {
       var decodedl: String! = String(cString: [112,105,99,116,117,114,101,0], encoding: .utf8)!
    _ = decodedl
   repeat {
      decodedl.append("\(2 >> (Swift.min(4, decodedl.count)))")
      if 1669475 == decodedl.count {
         break
      }
   } while (1669475 == decodedl.count) && (2 < decodedl.count)

        let label = UILabel()
        label.textColor = .white
        label.font = .systemFont(ofSize: 16, weight: .semibold)
        label.textAlignment = .center
        return label
    }()

    private lazy var tableView: UITableView = {
       var capture3: String! = String(cString: [102,105,110,97,108,100,0], encoding: .utf8)!
       var hex8: String! = String(cString: [106,114,101,118,100,99,116,0], encoding: .utf8)!
       _ = hex8
       var partsP: String! = String(cString: [109,106,112,101,103,101,110,99,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &partsP) { pointer in
    
      }
         partsP = "\((hex8 == (String(cString:[108,0], encoding: .utf8)!) ? hex8.count : partsP.count))"
      for _ in 0 ..< 2 {
          var d_centerS: String! = String(cString: [118,101,110,117,101,0], encoding: .utf8)!
          _ = d_centerS
          var existingo: String! = String(cString: [101,109,117,108,97,116,111,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &existingo) { pointer in
                _ = pointer.pointee
         }
          var constraintg: String! = String(cString: [98,105,110,100,101,114,0], encoding: .utf8)!
          var radius4: String! = String(cString: [97,99,99,0], encoding: .utf8)!
          var timeoutx: Bool = true
         hex8 = "\(2)"
         d_centerS = "\(radius4.count << (Swift.min(labs(2), 4)))"
         existingo = "\(existingo.count)"
         constraintg = "\(radius4.count * 1)"
         timeoutx = d_centerS == (String(cString:[50,0], encoding: .utf8)!) && 97 > constraintg.count
      }
          var networkU: Bool = true
          var symbolf: String! = String(cString: [115,101,114,118,105,99,101,0], encoding: .utf8)!
          var componentsx: Int = 1
          _ = componentsx
         partsP.append("\(3)")
         networkU = hex8 == (String(cString:[117,0], encoding: .utf8)!)
         symbolf = "\(symbolf.count)"
         componentsx |= componentsx / 1
         partsP = "\(1)"
         hex8 = "\((hex8 == (String(cString:[83,0], encoding: .utf8)!) ? partsP.count : hex8.count))"
      for _ in 0 ..< 2 {
          var loaderD: Double = 4.0
          var formatterD: Bool = true
          var observer2: [Any]! = [4.0]
          var linkC: Bool = true
          var attributedf: [Any]! = [String(cString: [105,110,116,114,97,120,104,117,102,0], encoding: .utf8)!, String(cString: [97,103,101,110,116,115,0], encoding: .utf8)!]
         hex8 = "\(((linkC ? 2 : 1)))"
         loaderD -= Double(attributedf.count >> (Swift.min(labs(2), 2)))
         observer2 = [((String(cString:[111,0], encoding: .utf8)!) == hex8 ? attributedf.count : hex8.count)]
      }
      capture3 = "\(hex8.count % 2)"

        let tableView = UITableView(frame: .zero, style: .plain)
        tableView.backgroundColor = .black
        tableView.separatorStyle = .none
        tableView.showsVerticalScrollIndicator = false
        tableView.estimatedRowHeight = 80
        tableView.rowHeight = UITableView.automaticDimension
        tableView.keyboardDismissMode = .interactive
        tableView.dataSource = self
        tableView.delegate = self
        tableView.register(
            DSRommManagerCell.self,
            forCellReuseIdentifier: DSRommManagerCell.reuseIdentifier
        )
        return tableView
    }()

    private let inputBarView: UIView = {
       var parts4: Double = 4.0
   withUnsafeMutablePointer(to: &parts4) { pointer in
          _ = pointer.pointee
   }
   if (Double(2 * Int(parts4))) == 1.84 {
       var buttonsJ: String! = String(cString: [97,117,116,104,114,111,114,105,122,97,116,105,111,110,0], encoding: .utf8)!
       _ = buttonsJ
       var peerJ: Bool = true
      withUnsafeMutablePointer(to: &peerJ) { pointer in
             _ = pointer.pointee
      }
       var headerO: [String: Any]! = [String(cString: [105,108,108,117,109,105,110,97,116,105,111,110,0], encoding: .utf8)!:39, String(cString: [104,111,117,114,0], encoding: .utf8)!:66]
      if buttonsJ.contains("\(headerO.keys.count)") {
         buttonsJ = "\(3)"
      }
          var httpt: String! = String(cString: [111,112,101,114,97,116,105,110,103,0], encoding: .utf8)!
          var displayw: String! = String(cString: [106,111,121,102,117,108,0], encoding: .utf8)!
         peerJ = displayw.count >= 95
         httpt = "\(((String(cString:[121,0], encoding: .utf8)!) == buttonsJ ? (peerJ ? 1 : 2) : buttonsJ.count))"
         headerO[buttonsJ] = buttonsJ.count - headerO.count
         headerO = [buttonsJ: (1 / (Swift.max(2, (peerJ ? 5 : 5))))]
       var indicatorv: String! = String(cString: [97,115,112,101,99,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &indicatorv) { pointer in
    
      }
         headerO["\(peerJ)"] = (headerO.values.count << (Swift.min(3, labs((peerJ ? 5 : 1)))))
       var processedZ: String! = String(cString: [111,110,102,105,103,117,114,97,116,105,111,110,0], encoding: .utf8)!
          var alertQ: Bool = true
         withUnsafeMutablePointer(to: &alertQ) { pointer in
    
         }
          var destinationV: Double = 4.0
          _ = destinationV
         peerJ = destinationV <= Double(buttonsJ.count)
         alertQ = buttonsJ.count < 36
          var controllerQ: String! = String(cString: [99,111,115,105,103,110,97,116,111,114,121,0], encoding: .utf8)!
         processedZ = "\((processedZ == (String(cString:[54,0], encoding: .utf8)!) ? processedZ.count : headerO.values.count))"
         controllerQ.append("\((indicatorv == (String(cString:[53,0], encoding: .utf8)!) ? headerO.keys.count : indicatorv.count))")
      parts4 /= Swift.max(4, Double(1))
   }

        let view = UIView()
        view.backgroundColor = .black
        return view
    }()

    private lazy var videoButton: UIButton = {
       var urlc: String! = String(cString: [98,99,100,117,105,110,116,0], encoding: .utf8)!
    var controlY: String! = String(cString: [97,115,105,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &controlY) { pointer in
    
   }
      controlY = "\(urlc.count)"

        let dimmingButton = UIButton(type: .custom)
      urlc = "\(controlY.count >> (Swift.min(labs(2), 4)))"
        dimmingButton.setImage(UIImage(named: "chat_video"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapVideo), for: .touchUpInside)
        return dimmingButton
    }()

    private let inputBackgroundView: UIView = {
       var arg9: String! = String(cString: [115,111,108,118,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &arg9) { pointer in
    
   }
      arg9 = "\(arg9.count)"

        let view = UIView()
        view.backgroundColor = UIColor.hex("#2C2C2E")
        view.layer.cornerRadius = 26
        view.clipsToBounds = true
        return view
    }()

    private lazy var messageTextField: UITextField = {
       var contactf: [Any]! = [29, 30, 64]
   withUnsafeMutablePointer(to: &contactf) { pointer in
    
   }
      contactf = [3]

        let person = UITextField()
        person.attributedPlaceholder = NSAttributedString(
            string: "Enter what you want to send",
            attributes: [
                .foregroundColor: UIColor.white.withAlphaComponent(0.45),
                .font: UIFont.systemFont(ofSize: 15, weight: .regular)
            ]
        )
        person.textColor = .white
        person.font = .systemFont(ofSize: 15, weight: .regular)
        person.returnKeyType = .send
        person.delegate = self
        return person
    }()

    private lazy var sendButton: UIButton = {
       var publishr: Bool = false
   withUnsafeMutablePointer(to: &publishr) { pointer in
    
   }
      publishr = !publishr || !publishr

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setImage(UIImage(named: "common_send"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapSend), for: .touchUpInside)
        return dimmingButton
    }()

    init(contact: DSReport) {
        self.contact = contact
        super.init(nibName: nil, bundle: nil)
    }

    convenience init() {
        self.init(contact: DSReport(userId: "u_003", name: "Beach", avatarImageName: nil))
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    override func viewDidLoad() {
       var itemz: Float = 3.0
   for _ in 0 ..< 1 {
      itemz += (Float(Int(itemz > 258916028.0 || itemz < -258916028.0 ? 20.0 : itemz)))
   }

        super.viewDidLoad()
        applyContact()
        setupUI()
        reloadMessages()
    }

    override func viewWillAppear(_ animated: Bool) {
       var micH: Double = 1.0
      micH -= Double(3)

        super.viewWillAppear(animated)
        markConversationReadIfNeeded()
    }

    private var currentUserId: String? {
       var bubbleo: Double = 1.0
       var final_xZ: Int = 2
      withUnsafeMutablePointer(to: &final_xZ) { pointer in
             _ = pointer.pointee
      }
       var rangel: String! = String(cString: [117,110,115,104,105,102,116,0], encoding: .utf8)!
       var subtitle7: Double = 5.0
          var tnewsi: Bool = false
         final_xZ -= 3
         tnewsi = subtitle7 >= 77.19
         rangel.append("\(final_xZ)")
      for _ in 0 ..< 2 {
          var containerG: Int = 4
          _ = containerG
          var radiusf: String! = String(cString: [100,105,102,102,101,114,101,110,99,101,115,0], encoding: .utf8)!
          _ = radiusf
          var scriptY: Float = 1.0
          var insetP: [Any]! = [45, 8]
          var registeredg: Int = 2
          _ = registeredg
         rangel.append("\(radiusf.count)")
         containerG /= Swift.max(1 << (Swift.min(4, radiusf.count)), 2)
         scriptY += (Float(1 + Int(scriptY > 15182251.0 || scriptY < -15182251.0 ? 40.0 : scriptY)))
         insetP = [radiusf.count | 1]
         registeredg += 3
      }
      repeat {
         rangel.append("\(((String(cString:[76,0], encoding: .utf8)!) == rangel ? rangel.count : final_xZ))")
         if rangel.count == 1468847 {
            break
         }
      } while (rangel.count == 1468847) && (rangel.count == 2)
       var u_countv: Float = 4.0
       _ = u_countv
      if u_countv <= Float(rangel.count) {
          var bundledO: String! = String(cString: [105,116,101,114,97,116,111,114,0], encoding: .utf8)!
          _ = bundledO
          var headerk: Double = 5.0
         withUnsafeMutablePointer(to: &headerk) { pointer in
    
         }
          var workF: String! = String(cString: [116,114,97,110,115,102,101,114,114,101,100,0], encoding: .utf8)!
          var r_managerp: Bool = true
          _ = r_managerp
         u_countv /= Swift.max((Float(Int(u_countv > 64351736.0 || u_countv < -64351736.0 ? 45.0 : u_countv))), 3)
         bundledO.append("\(((String(cString:[56,0], encoding: .utf8)!) == workF ? workF.count : Int(u_countv > 340966678.0 || u_countv < -340966678.0 ? 76.0 : u_countv)))")
         headerk /= Swift.max((Double(Int(headerk > 18997765.0 || headerk < -18997765.0 ? 6.0 : headerk))), 1)
         r_managerp = Float(workF.count) <= u_countv
      }
         rangel.append("\(rangel.count)")
      repeat {
         subtitle7 -= Double(3)
         if subtitle7 == 115969.0 {
            break
         }
      } while (subtitle7 == 115969.0) && ((subtitle7 / 1.68) <= 4.38 || 1.68 <= (subtitle7 / (Swift.max(5, Double(u_countv)))))
      for _ in 0 ..< 1 {
         rangel = "\(final_xZ)"
      }
      bubbleo /= Swift.max(4, (Double(Int(subtitle7 > 88341536.0 || subtitle7 < -88341536.0 ? 51.0 : subtitle7) & final_xZ)))

            return DSSecondaryNews.shared.user?.userId
    }

    private var meAvatarPath: String? {
       var inseti: Int = 3
    _ = inseti
    var updated6: Int = 1
   withUnsafeMutablePointer(to: &updated6) { pointer in
          _ = pointer.pointee
   }
      updated6 &= inseti % 2
   if (inseti | 2) > 4 || 1 > (updated6 | 2) {
      inseti /= Swift.max(4, updated6 >> (Swift.min(labs(inseti), 3)))
   }

            return DSSecondaryNews.shared.user?.avatarUrl
    }

    private func reloadMessages() {
       var exitP: String! = String(cString: [114,101,103,105,115,116,101,114,0], encoding: .utf8)!
   repeat {
      exitP.append("\(exitP.count)")
      if 4171977 == exitP.count {
         break
      }
   } while (exitP == String(cString:[99,0], encoding: .utf8)! || exitP.count == 1) && (4171977 == exitP.count)

        guard let currentUserId else {
            messages = []
            tableView.reloadData()
            return
        }
        messages = DS_ChatStore.messages(currentUserId: currentUserId, peerUserId: contact.userId)
        tableView.reloadData()
        scrollToBottom(animated: false)
    }

    private func markConversationReadIfNeeded() {
       var h_productsu: Double = 0.0
      h_productsu -= (Double(Int(h_productsu > 294835108.0 || h_productsu < -294835108.0 ? 60.0 : h_productsu) << (Swift.min(3, labs(1)))))

        guard let currentUserId else { return }
        DS_ChatStore.markConversationRead(currentUserId: currentUserId, peerUserId: contact.userId)
    }

    private func applyContact() {
       var release_m6M: String! = String(cString: [112,114,101,100,105,99,116,105,111,110,0], encoding: .utf8)!
    _ = release_m6M
   repeat {
       var asky: String! = String(cString: [99,111,109,109,111,110,0], encoding: .utf8)!
       var clipw: [Any]! = [String(cString: [117,108,97,119,0], encoding: .utf8)!, String(cString: [97,100,111,112,116,101,100,0], encoding: .utf8)!, String(cString: [115,116,111,119,0], encoding: .utf8)!]
      repeat {
          var aspectE: Int = 4
          var barH: Bool = false
         withUnsafeMutablePointer(to: &barH) { pointer in
                _ = pointer.pointee
         }
          var bundledu: String! = String(cString: [116,114,97,110,115,108,97,116,105,111,110,0], encoding: .utf8)!
         clipw.append(asky.count * bundledu.count)
         aspectE /= Swift.max((asky == (String(cString:[66,0], encoding: .utf8)!) ? aspectE : asky.count), 2)
         barH = barH || 46 == aspectE
         if 1639619 == clipw.count {
            break
         }
      } while (4 >= (3 + asky.count)) && (1639619 == clipw.count)
      for _ in 0 ..< 3 {
         clipw.append(clipw.count)
      }
         clipw.append(asky.count)
          var accentz: Int = 1
          var poole: String! = String(cString: [97,119,97,121,0], encoding: .utf8)!
         asky.append("\(clipw.count ^ 2)")
         accentz &= 1
         poole.append("\(accentz % (Swift.max(10, asky.count)))")
         asky.append("\(clipw.count * asky.count)")
         clipw.append(clipw.count)
      release_m6M.append("\(clipw.count)")
      if release_m6M == (String(cString:[112,103,116,107,0], encoding: .utf8)!) {
         break
      }
   } while (release_m6M != String(cString:[72,0], encoding: .utf8)! || release_m6M.count > 2) && (release_m6M == (String(cString:[112,103,116,107,0], encoding: .utf8)!))

        headerNameLabel.text = contact.name
        headerAvatarImageView.image = UserData.image(for: contact.avatarImageName)
    }

    private func setupUI() {
       var tabbar5: String! = String(cString: [115,112,101,101,100,111,109,101,116,101,114,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      tabbar5 = "\(tabbar5.count)"
   }

        view.backgroundColor = .black

        view.addSubview(navBarView)
        view.addSubview(tableView)
        view.addSubview(inputBarView)

        navBarView.addSubview(backButton)
        navBarView.addSubview(moreButton)
        navBarView.addSubview(headerAvatarImageView)
        navBarView.addSubview(headerNameLabel)

        inputBarView.addSubview(videoButton)
        inputBarView.addSubview(inputBackgroundView)
        inputBarView.addSubview(sendButton)
        inputBackgroundView.addSubview(messageTextField)

        navBarView.snp.makeConstraints { make in
            make.top.leading.trailing.equalToSuperview()
            make.bottom.equalTo(view.safeAreaLayoutGuide.snp.top).offset(Layout.navBarContentHeight)
        }

        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(8)
            make.bottom.equalToSuperview().inset(8)
            make.width.height.equalTo(44)
        }

        moreButton.snp.makeConstraints { make in
            make.trailing.equalToSuperview().inset(8)
            make.centerY.equalTo(backButton)
            make.width.height.equalTo(44)
        }

        headerAvatarImageView.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.top.equalTo(view.safeAreaLayoutGuide).offset(4)
            make.width.height.equalTo(Layout.headerAvatarSize)
        }

        headerNameLabel.snp.makeConstraints { make in
            make.top.equalTo(headerAvatarImageView.snp.bottom).offset(6)
            make.centerX.equalToSuperview()
            make.leading.greaterThanOrEqualTo(backButton.snp.trailing).offset(8)
            make.trailing.lessThanOrEqualTo(moreButton.snp.leading).offset(-8)
        }

        inputBarView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview()
            make.bottom.equalTo(view.safeAreaLayoutGuide)
            make.height.equalTo(Layout.inputBarHeight + 16)
        }

        videoButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.inputHorizontalInset)
            make.centerY.equalToSuperview()
            make.width.height.equalTo(Layout.actionButtonSize)
        }

        sendButton.snp.makeConstraints { make in
            make.trailing.equalToSuperview().inset(Layout.inputHorizontalInset)
            make.centerY.equalToSuperview()
            make.width.height.equalTo(Layout.actionButtonSize)
        }

        inputBackgroundView.snp.makeConstraints { make in
            make.leading.equalTo(videoButton.snp.trailing).offset(12)
            make.trailing.equalTo(sendButton.snp.leading).offset(-12)
            make.centerY.equalToSuperview()
            make.height.equalTo(Layout.inputBarHeight)
        }

        messageTextField.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(16)
            make.centerY.equalToSuperview()
        }

        tableView.snp.makeConstraints { make in
            make.top.equalTo(navBarView.snp.bottom)
            make.leading.trailing.equalToSuperview()
            make.bottom.equalTo(inputBarView.snp.top)
        }
    }

    private func scrollToBottom(animated: Bool) {
       var tabbarr: String! = String(cString: [115,101,99,116,111,114,0], encoding: .utf8)!
       var actionK: String! = String(cString: [108,97,121,111,117,116,115,0], encoding: .utf8)!
       _ = actionK
       var usersB: Int = 3
      withUnsafeMutablePointer(to: &usersB) { pointer in
             _ = pointer.pointee
      }
       var lived: Float = 4.0
      for _ in 0 ..< 2 {
         actionK = "\(usersB / (Swift.max(actionK.count, 2)))"
      }
          var sectionz: [String: Any]! = [String(cString: [112,97,121,101,101,0], encoding: .utf8)!:70, String(cString: [110,117,109,101,114,97,108,115,0], encoding: .utf8)!:58]
          var editM: [String: Any]! = [String(cString: [115,105,112,104,97,115,104,0], encoding: .utf8)!:5, String(cString: [105,100,101,116,0], encoding: .utf8)!:18]
          var fetchedq: String! = String(cString: [114,101,112,108,105,99,97,116,101,100,0], encoding: .utf8)!
         usersB += 2 >> (Swift.min(4, labs(usersB)))
         sectionz[fetchedq] = editM.keys.count
         editM = ["\(usersB)": (Int(lived > 43358667.0 || lived < -43358667.0 ? 48.0 : lived))]
         fetchedq = "\(3 << (Swift.min(5, sectionz.keys.count)))"
       var b_imageB: [Any]! = [1, 55, 42]
       _ = b_imageB
      while (4 <= (b_imageB.count * 4)) {
         b_imageB = [(Int(lived > 288545429.0 || lived < -288545429.0 ? 1.0 : lived))]
         break
      }
      for _ in 0 ..< 1 {
          var firstx: Float = 1.0
         b_imageB = [(actionK == (String(cString:[51,0], encoding: .utf8)!) ? actionK.count : usersB)]
         firstx /= Swift.max(Float(usersB), 4)
      }
         lived -= Float(3 * b_imageB.count)
         b_imageB.append((Int(lived > 99498516.0 || lived < -99498516.0 ? 83.0 : lived) % 2))
         b_imageB = [usersB / 3]
       var shopo: String! = String(cString: [115,101,116,102,105,101,108,100,0], encoding: .utf8)!
       var permissions7: String! = String(cString: [100,105,115,97,112,112,101,97,114,101,100,0], encoding: .utf8)!
       _ = permissions7
         shopo.append("\(b_imageB.count)")
         permissions7.append("\((shopo == (String(cString:[70,0], encoding: .utf8)!) ? shopo.count : usersB))")
      tabbarr.append("\((1 ^ Int(lived > 202182378.0 || lived < -202182378.0 ? 90.0 : lived)))")

        guard !messages.isEmpty else { return }
        let radius = IndexPath(row: messages.count - 1, section: 0)
        tableView.scrollToRow(at: radius, at: .bottom, animated: animated)
    }

    @objc private func didTapBack() {
       var diamondsJ: String! = String(cString: [114,116,99,100,0], encoding: .utf8)!
    _ = diamondsJ
      diamondsJ.append("\(2 * diamondsJ.count)")

        navigationController?.popViewController(animated: true)
    }

    @objc private func didTapMore() {
       var membere: String! = String(cString: [99,111,110,115,116,114,117,99,116,111,114,0], encoding: .utf8)!
   while (membere.contains("\(membere.count)")) {
      membere.append("\(membere.count)")
      break
   }

        ds_presentBlacklistConfirmation(peerUserId: contact.userId, peerName: contact.name) { [weak self] in
            self?.navigationController?.popViewController(animated: true)
        }
    }

    @objc private func didTapVideo() {
       var loadingC: [String: Any]! = [String(cString: [115,109,115,0], encoding: .utf8)!:String(cString: [99,117,100,97,0], encoding: .utf8)!, String(cString: [101,118,105,99,116,105,111,110,0], encoding: .utf8)!:String(cString: [106,112,101,103,108,115,0], encoding: .utf8)!, String(cString: [116,101,115,115,101,108,97,116,101,0], encoding: .utf8)!:String(cString: [114,111,113,118,105,100,101,111,0], encoding: .utf8)!]
       var indexB: String! = String(cString: [118,112,105,116,120,102,109,0], encoding: .utf8)!
       var cnewsJ: [Any]! = [10, 22, 57]
         indexB.append("\(indexB.count & cnewsJ.count)")
      for _ in 0 ..< 1 {
         indexB = "\(cnewsJ.count + 3)"
      }
      for _ in 0 ..< 1 {
          var ownQ: Double = 5.0
          _ = ownQ
         indexB.append("\(((String(cString:[109,0], encoding: .utf8)!) == indexB ? indexB.count : cnewsJ.count))")
         ownQ -= Double(indexB.count)
      }
         indexB = "\(3)"
         cnewsJ.append(indexB.count)
      repeat {
          var trimmedb: Int = 5
          var decoded2: String! = String(cString: [105,115,100,105,97,99,114,105,116,105,99,0], encoding: .utf8)!
          var captureZ: Bool = false
          var mailt: Double = 0.0
          _ = mailt
         indexB.append("\((Int(mailt > 157210955.0 || mailt < -157210955.0 ? 43.0 : mailt) ^ (captureZ ? 2 : 4)))")
         trimmedb &= cnewsJ.count << (Swift.min(indexB.count, 5))
         decoded2 = "\((indexB.count << (Swift.min(4, labs((captureZ ? 4 : 1))))))"
         if indexB == (String(cString:[55,119,102,103,118,115,107,51,110,0], encoding: .utf8)!) {
            break
         }
      } while (indexB == (String(cString:[55,119,102,103,118,115,107,51,110,0], encoding: .utf8)!)) && (indexB.count < 2)
      loadingC = ["\(loadingC.values.count)": loadingC.count]

        guard ds_guardMutualFollowForChat(peerUserId: contact.userId) else { return }
        let avatarQ = DSListController(
            peerUserId: contact.userId,
            peerName: contact.name,
            peerAvatarPath: contact.avatarImageName
        )
        navigationController?.pushViewController(avatarQ, animated: true)
    }

    @objc private func didTapSend() {
       var docsu: String! = String(cString: [102,117,116,101,120,0], encoding: .utf8)!
    var migratedJ: String! = String(cString: [97,105,110,116,105,110,103,0], encoding: .utf8)!
   repeat {
       var candidated: [String: Any]! = [String(cString: [111,117,116,102,105,108,101,0], encoding: .utf8)!:true]
       var priceq: Double = 0.0
       var size_e0f: String! = String(cString: [103,101,111,98,116,97,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &size_e0f) { pointer in
    
      }
      while (4.37 < priceq) {
         candidated[size_e0f] = candidated.count ^ size_e0f.count
         break
      }
      repeat {
         size_e0f.append("\(2)")
         if 1373521 == size_e0f.count {
            break
         }
      } while ((Int(priceq > 392421977.0 || priceq < -392421977.0 ? 70.0 : priceq)) == size_e0f.count) && (1373521 == size_e0f.count)
         size_e0f = "\(candidated.values.count)"
         size_e0f = "\(candidated.keys.count)"
         size_e0f = "\((Int(priceq > 372186814.0 || priceq < -372186814.0 ? 88.0 : priceq)))"
      if 4.20 > (1.54 * priceq) && (1.54 * priceq) > 4.11 {
         priceq -= Double(candidated.keys.count)
      }
      for _ in 0 ..< 1 {
         size_e0f.append("\(candidated.count)")
      }
      repeat {
          var senderw: String! = String(cString: [105,110,116,101,114,112,111,108,97,116,111,114,115,0], encoding: .utf8)!
          var verticalC: String! = String(cString: [103,101,116,110,112,97,115,115,101,115,0], encoding: .utf8)!
         size_e0f = "\(size_e0f.count)"
         senderw = "\(1)"
         verticalC = "\(verticalC.count << (Swift.min(labs(3), 3)))"
         if 3233309 == size_e0f.count {
            break
         }
      } while (3233309 == size_e0f.count) && (1 <= (5 * candidated.values.count))
      while ((priceq - 3.77) < 1.42 || 4 < (1 + size_e0f.count)) {
         priceq /= Swift.max(3, Double(1))
         break
      }
      docsu.append("\((size_e0f == (String(cString:[70,0], encoding: .utf8)!) ? candidated.count : size_e0f.count))")
      if 2704111 == docsu.count {
         break
      }
   } while (2704111 == docsu.count) && (!migratedJ.contains(docsu))

        
        DSGuide.shared.postDefaultRequest(isShow: false) { result in
      docsu.append("\(migratedJ.count & 1)")
            switch result {
            case .success(_):
                self.sendAction()
            case .failure(_):
                self.sendAction()
            }
        }
    }
    
    private func sendAction(){
       var didv: String! = String(cString: [101,120,112,114,101,115,115,0], encoding: .utf8)!
      didv = "\(didv.count / (Swift.max(didv.count, 10)))"

        
        let handling = messageTextField.text?.trimmingCharacters(in: .whitespacesAndNewlines) ?? ""
        guard !handling.isEmpty, let currentUserId else { return }

        DS_ChatStore.appendMessage(
            currentUserId: currentUserId,
            contact: contact,
            sender: .me,
            text: handling
        )
        messageTextField.text = nil
        reloadMessages()
        scrollToBottom(animated: true)
    }
}

extension DSLoginController: UITableViewDataSource {


    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
       var toggler: Int = 4
       var normalL: Float = 2.0
      withUnsafeMutablePointer(to: &normalL) { pointer in
    
      }
       var size_3T: String! = String(cString: [103,114,97,100,105,101,110,116,0], encoding: .utf8)!
       var conversationsD: String! = String(cString: [112,114,111,118,105,100,105,110,103,0], encoding: .utf8)!
         conversationsD = "\(((String(cString:[75,0], encoding: .utf8)!) == size_3T ? Int(normalL > 216570375.0 || normalL < -216570375.0 ? 37.0 : normalL) : size_3T.count))"
          var trailingK: Double = 3.0
          _ = trailingK
          var delayN: Double = 1.0
          _ = delayN
         conversationsD.append("\(size_3T.count)")
         trailingK /= Swift.max(Double(conversationsD.count), 5)
         delayN -= (Double(Int(trailingK > 29500741.0 || trailingK < -29500741.0 ? 51.0 : trailingK) + Int(delayN > 6753599.0 || delayN < -6753599.0 ? 9.0 : delayN)))
      repeat {
          var appearanceN: [Any]! = [92, 87, 38]
          var pickero: String! = String(cString: [102,105,108,116,101,114,115,0], encoding: .utf8)!
         size_3T = "\((conversationsD == (String(cString:[112,0], encoding: .utf8)!) ? pickero.count : conversationsD.count))"
         appearanceN = [3 << (Swift.min(5, conversationsD.count))]
         if 881796 == size_3T.count {
            break
         }
      } while (881796 == size_3T.count) && ((size_3T.count * Int(normalL > 323557112.0 || normalL < -323557112.0 ? 86.0 : normalL)) <= 2 && 5.9 <= (Float(size_3T.count) * normalL))
      for _ in 0 ..< 3 {
         size_3T = "\((size_3T == (String(cString:[88,0], encoding: .utf8)!) ? size_3T.count : Int(normalL > 199554471.0 || normalL < -199554471.0 ? 20.0 : normalL)))"
      }
          var first3: String! = String(cString: [104,111,114,105,122,0], encoding: .utf8)!
         conversationsD = "\((Int(normalL > 95840970.0 || normalL < -95840970.0 ? 72.0 : normalL)))"
         first3.append("\(2 * first3.count)")
      toggler /= Swift.max(((String(cString:[53,0], encoding: .utf8)!) == size_3T ? size_3T.count : Int(normalL > 233486612.0 || normalL < -233486612.0 ? 49.0 : normalL)), 3)

        guard let cell = tableView.dequeueReusableCell(
            withIdentifier: DSRommManagerCell.reuseIdentifier,
            for: indexPath
        ) as? DSRommManagerCell else {
            return UITableViewCell()
        }
        cell.configure(
            with: messages[indexPath.row],
            peerAvatarPath: contact.avatarImageName,
            meAvatarPath: meAvatarPath
        )
        return cell
    }


    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
       var package9: Double = 4.0
   repeat {
      package9 += Double(2)
      if 1994495.0 == package9 {
         break
      }
   } while (package9 == package9) && (1994495.0 == package9)

return         messages.count
    }
}

extension DSLoginController: UITableViewDelegate {
}

extension DSLoginController: UITextFieldDelegate {


    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
       var mergedI: Float = 0.0
    var datek: Double = 5.0
    _ = datek
       var tappedi: String! = String(cString: [105,110,102,105,110,105,116,121,0], encoding: .utf8)!
         tappedi.append("\(tappedi.count ^ 3)")
       var durationO: String! = String(cString: [115,117,98,118,105,101,119,115,0], encoding: .utf8)!
       _ = durationO
       var avatarm: String! = String(cString: [100,101,108,97,121,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &avatarm) { pointer in
             _ = pointer.pointee
      }
      if avatarm != String(cString:[74,0], encoding: .utf8)! {
         durationO.append("\(durationO.count)")
      }
      datek -= (Double(Int(mergedI > 177210831.0 || mergedI < -177210831.0 ? 48.0 : mergedI) ^ 3))

   while ((Double(Int(datek) / (Swift.max(2, 1)))) < 2.29) {
       var providerS: [Any]! = [89, 10, 100]
       _ = providerS
       var loader3: String! = String(cString: [101,120,112,111,115,117,114,101,0], encoding: .utf8)!
         loader3 = "\(providerS.count)"
         providerS.append(providerS.count)
         providerS.append(3 / (Swift.max(6, loader3.count)))
      while ((2 % (Swift.max(6, providerS.count))) < 1 && (loader3.count % 2) < 2) {
         loader3.append("\(2)")
         break
      }
         loader3.append("\(loader3.count)")
          var priceh: Int = 4
          _ = priceh
         loader3 = "\((loader3 == (String(cString:[112,0], encoding: .utf8)!) ? loader3.count : providerS.count))"
         priceh %= Swift.max(4, ((String(cString:[86,0], encoding: .utf8)!) == loader3 ? priceh : loader3.count))
      mergedI /= Swift.max((Float(Int(datek > 43678795.0 || datek < -43678795.0 ? 21.0 : datek))), 5)
      break
   }
        didTapSend()
        return true
    }
}
