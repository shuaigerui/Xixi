
import Foundation

import UIKit

enum DS_ChatRoomMessageSender {
    case peer
    case me
}

struct DSEmptyPush {
    let sender: DS_ChatRoomMessageSender
    let text: String
}

final class DSRommManagerCell: UITableViewCell {

    static let reuseIdentifier = "DSRommManagerCell"

    private enum Layout {
        static let avatarSize: CGFloat = 36
        static let bubbleCornerRadius: CGFloat = 12
        static let horizontalInset: CGFloat = 16
        static let bubbleMaxWidthRatio: CGFloat = 0.72
    }

    private let peerContainerView = UIView()
    private let meContainerView = UIView()

    private let peerAvatarImageView: UIImageView = {
       var rommA: String! = String(cString: [101,97,114,108,121,0], encoding: .utf8)!
    var createdq: String! = String(cString: [112,114,101,112,114,111,103,114,97,109,109,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &createdq) { pointer in
          _ = pointer.pointee
   }
      createdq.append("\(3)")

        let imageView = UIImageView()
      createdq = "\(rommA.count | createdq.count)"
        imageView.contentMode = .scaleAspectFill
   for _ in 0 ..< 3 {
      rommA = "\(2)"
   }
        imageView.clipsToBounds = true
        imageView.backgroundColor = UIColor.hex("#3A3A3C")
        imageView.layer.cornerRadius = Layout.avatarSize / 2
        return imageView
    }()

    private let meAvatarImageView: UIImageView = {
       var b_managerT: String! = String(cString: [106,100,99,111,101,102,99,116,0], encoding: .utf8)!
    var nameO: Float = 3.0
   withUnsafeMutablePointer(to: &nameO) { pointer in
          _ = pointer.pointee
   }
      b_managerT = "\(b_managerT.count)"

        let imageView = UIImageView()
      b_managerT.append("\(3)")
        imageView.contentMode = .scaleAspectFill
      nameO -= (Float(Int(nameO > 37467578.0 || nameO < -37467578.0 ? 78.0 : nameO) + 2))
        imageView.clipsToBounds = true
        imageView.backgroundColor = UIColor.hex("#3A3A3C")
        imageView.layer.cornerRadius = Layout.avatarSize / 2
        return imageView
    }()

    private let peerBubbleView: UIView = {
       var seedR: String! = String(cString: [100,101,99,105,109,97,108,0], encoding: .utf8)!
    var cleanede: Double = 4.0
   for _ in 0 ..< 3 {
      cleanede /= Swift.max(5, Double(1 & seedR.count))
   }

        let view = UIView()
       var containerI: Float = 4.0
       _ = containerI
      for _ in 0 ..< 1 {
         containerI /= Swift.max((Float(Int(containerI > 210305770.0 || containerI < -210305770.0 ? 91.0 : containerI) / (Swift.max(Int(containerI > 316956316.0 || containerI < -316956316.0 ? 71.0 : containerI), 5)))), 5)
      }
      for _ in 0 ..< 2 {
         containerI -= (Float(Int(containerI > 313017455.0 || containerI < -313017455.0 ? 42.0 : containerI) >> (Swift.min(3, labs(3)))))
      }
      repeat {
         containerI -= (Float(Int(containerI > 226988888.0 || containerI < -226988888.0 ? 21.0 : containerI) >> (Swift.min(5, labs(1)))))
         if 190726.0 == containerI {
            break
         }
      } while (containerI >= containerI) && (190726.0 == containerI)
      cleanede *= (Double(3 & Int(containerI > 97801608.0 || containerI < -97801608.0 ? 5.0 : containerI)))
        view.backgroundColor = UIColor.hex("#B19DFF")
   while (seedR.hasPrefix(seedR)) {
       var hoursa: String! = String(cString: [99,97,118,115,105,100,99,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &hoursa) { pointer in
             _ = pointer.pointee
      }
       var fieldh: String! = String(cString: [109,97,116,105,99,0], encoding: .utf8)!
       var hasB: [String: Any]! = [String(cString: [112,114,101,115,115,117,114,101,0], encoding: .utf8)!:60, String(cString: [100,101,110,111,105,115,101,102,105,108,116,101,114,0], encoding: .utf8)!:100]
       var coinsQ: String! = String(cString: [114,98,115,112,0], encoding: .utf8)!
       _ = coinsQ
      while (coinsQ.count >= hasB.count) {
          var playinga: String! = String(cString: [99,111,110,115,116,97,110,116,0], encoding: .utf8)!
          var presentingZ: String! = String(cString: [98,117,114,115,116,121,0], encoding: .utf8)!
          var removedM: Bool = false
          var selj: [Any]! = [89, 69]
          var removedf: String! = String(cString: [105,111,101,114,114,110,111,109,101,109,0], encoding: .utf8)!
         coinsQ.append("\(selj.count % 1)")
         playinga = "\(3)"
         presentingZ.append("\(2 >> (Swift.min(1, selj.count)))")
         removedM = selj.count == 97
         removedf = "\(selj.count)"
         break
      }
       var completionz: Double = 2.0
       var capturep: Double = 0.0
         completionz /= Swift.max(3, (Double(Int(completionz > 67525379.0 || completionz < -67525379.0 ? 15.0 : completionz) % (Swift.max(3, 8)))))
      repeat {
          var hasg: String! = String(cString: [117,110,115,101,108,101,99,116,0], encoding: .utf8)!
         coinsQ.append("\((hasB.values.count / (Swift.max(2, Int(completionz > 166069308.0 || completionz < -166069308.0 ? 21.0 : completionz)))))")
         hasg.append("\(3)")
         if coinsQ == (String(cString:[115,122,53,116,105,110,121,99,107,115,0], encoding: .utf8)!) {
            break
         }
      } while (!coinsQ.hasSuffix("\(hoursa.count)")) && (coinsQ == (String(cString:[115,122,53,116,105,110,121,99,107,115,0], encoding: .utf8)!))
      for _ in 0 ..< 1 {
         completionz /= Swift.max(2, Double(1))
      }
         hasB = ["\(capturep)": (Int(capturep > 160077100.0 || capturep < -160077100.0 ? 64.0 : capturep) * 3)]
          var aspect7: String! = String(cString: [110,97,110,112,97,0], encoding: .utf8)!
          var personA: String! = String(cString: [109,101,109,100,98,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &personA) { pointer in
    
         }
          var formatterv: String! = String(cString: [119,109,97,100,97,116,97,0], encoding: .utf8)!
         completionz += Double(hasB.count)
         aspect7 = "\((fieldh == (String(cString:[65,0], encoding: .utf8)!) ? fieldh.count : hasB.count))"
         personA.append("\(hasB.count * 3)")
         formatterv = "\(personA.count * fieldh.count)"
       var usedE: String! = String(cString: [105,110,112,117,116,0], encoding: .utf8)!
       var constraintZ: String! = String(cString: [99,109,97,99,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &constraintZ) { pointer in
             _ = pointer.pointee
      }
          var coinsY: Float = 4.0
         capturep /= Swift.max(1, Double(3 & constraintZ.count))
         coinsY /= Swift.max(5, Float(3 << (Swift.min(1, hoursa.count))))
       var bundledo: Double = 4.0
         fieldh.append("\(1 << (Swift.min(5, coinsQ.count)))")
         completionz /= Swift.max(Double(constraintZ.count + 1), 3)
         usedE = "\(hasB.values.count + 1)"
         bundledo -= (Double(1 | Int(completionz > 30068897.0 || completionz < -30068897.0 ? 80.0 : completionz)))
      seedR = "\(hoursa.count)"
      break
   }
        view.layer.cornerRadius = Layout.bubbleCornerRadius
        view.clipsToBounds = true
        return view
    }()

    private let meBubbleView: UIView = {
       var timeoutI: Double = 5.0
    _ = timeoutI
       var taskg: Int = 1
       var keysX: String! = String(cString: [115,116,114,105,112,112,101,100,0], encoding: .utf8)!
       _ = keysX
       var bottomW: Float = 1.0
       var k_titleZ: String! = String(cString: [97,109,101,120,0], encoding: .utf8)!
         taskg -= keysX.count | taskg
       var clipso: String! = String(cString: [99,111,108,100,0], encoding: .utf8)!
       _ = clipso
       var followu: String! = String(cString: [114,103,98,120,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &followu) { pointer in
    
      }
      repeat {
         taskg += followu.count | 1
         if 210519 == taskg {
            break
         }
      } while (Int(bottomW) <= taskg) && (210519 == taskg)
         clipso = "\(k_titleZ.count)"
         clipso.append("\(followu.count << (Swift.min(labs(3), 2)))")
      while (4 >= followu.count) {
          var check1: String! = String(cString: [118,102,105,108,116,101,114,0], encoding: .utf8)!
          var data_: Double = 4.0
          var storedU: Int = 4
          var names3: Bool = true
         followu.append("\(1 - k_titleZ.count)")
         check1 = "\(keysX.count / (Swift.max(3, 2)))"
         data_ += Double(taskg)
         storedU &= followu.count | clipso.count
         names3 = (taskg << (Swift.min(keysX.count, 2))) <= 47
         break
      }
      for _ in 0 ..< 3 {
         taskg %= Swift.max(3, followu.count * keysX.count)
      }
      for _ in 0 ..< 2 {
         taskg ^= (Int(bottomW > 46064297.0 || bottomW < -46064297.0 ? 76.0 : bottomW))
      }
      timeoutI += Double(taskg)

        let view = UIView()
        view.backgroundColor = UIColor.hex("#9D4EDD")
        view.layer.cornerRadius = Layout.bubbleCornerRadius
        view.clipsToBounds = true
        return view
    }()

    private let peerMessageLabel: UILabel = {
       var guideZ: Double = 1.0
      guideZ /= Swift.max(1, (Double(Int(guideZ > 177977569.0 || guideZ < -177977569.0 ? 69.0 : guideZ) | Int(guideZ > 260729910.0 || guideZ < -260729910.0 ? 54.0 : guideZ))))

        let label = UILabel()
        label.textColor = .white
        label.font = .systemFont(ofSize: 15, weight: .regular)
        label.numberOfLines = 0
        return label
    }()

    private let meMessageLabel: UILabel = {
       var c_positiong: String! = String(cString: [101,102,102,101,99,116,105,118,101,108,121,0], encoding: .utf8)!
      c_positiong.append("\(c_positiong.count)")

        let label = UILabel()
        label.textColor = .white
        label.font = .systemFont(ofSize: 15, weight: .regular)
        label.numberOfLines = 0
        return label
    }()

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        setupUI()
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    func configure(
        with message: DSEmptyPush,
        peerAvatarPath: String?,
        meAvatarPath: String?
    ) {
       var handlinge: String! = String(cString: [117,105,100,115,0], encoding: .utf8)!
      handlinge.append("\((handlinge == (String(cString:[87,0], encoding: .utf8)!) ? handlinge.count : handlinge.count))")

        let lines = message.sender == .peer
        peerContainerView.isHidden = !lines
        meContainerView.isHidden = lines

        peerAvatarImageView.image = UserData.image(for: peerAvatarPath)
        meAvatarImageView.image = UserData.image(for: meAvatarPath)

        if lines {
            peerMessageLabel.text = message.text
        } else {
            meMessageLabel.text = message.text
        }
    }

    private func setupUI() {
       var effect6: Bool = true
    var top4: Int = 0
   withUnsafeMutablePointer(to: &top4) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var visibilityd: String! = String(cString: [114,101,111,114,100,101,114,101,100,0], encoding: .utf8)!
       var display6: Float = 0.0
          var timestampX: String! = String(cString: [102,97,107,101,0], encoding: .utf8)!
          var namesk: String! = String(cString: [108,115,112,114,0], encoding: .utf8)!
         display6 -= (Float((String(cString:[122,0], encoding: .utf8)!) == visibilityd ? visibilityd.count : namesk.count))
         timestampX = "\(2 % (Swift.max(8, timestampX.count)))"
      if (3.79 / (Swift.max(5, display6))) < 4.22 || 3 < (visibilityd.count % 3) {
         display6 += (Float(Int(display6 > 311875196.0 || display6 < -311875196.0 ? 38.0 : display6) * 3))
      }
       var remoteg: String! = String(cString: [99,97,108,108,108,0], encoding: .utf8)!
         remoteg.append("\(1 | visibilityd.count)")
      while (display6 <= Float(visibilityd.count)) {
          var pickerA: Double = 1.0
          var trailingb: String! = String(cString: [114,101,110,100,101,114,97,98,108,101,0], encoding: .utf8)!
         display6 -= (Float(2 >> (Swift.min(5, labs(Int(pickerA > 172702811.0 || pickerA < -172702811.0 ? 69.0 : pickerA))))))
         trailingb = "\((Int(display6 > 108389115.0 || display6 < -108389115.0 ? 76.0 : display6) & Int(pickerA > 388870874.0 || pickerA < -388870874.0 ? 4.0 : pickerA)))"
         break
      }
          var timestampi: String! = String(cString: [99,110,97,109,101,0], encoding: .utf8)!
         remoteg = "\((timestampi == (String(cString:[67,0], encoding: .utf8)!) ? timestampi.count : visibilityd.count))"
      effect6 = 12.33 > display6
      if effect6 ? !effect6 : effect6 {
         break
      }
   } while (effect6 ? !effect6 : effect6) && (5 > (top4 / (Swift.max(5, 5))) || !effect6)

      top4 /= Swift.max(5, 1 >> (Swift.min(3, labs(top4))))
        selectionStyle = .none
        backgroundColor = .clear
        contentView.backgroundColor = .clear

        contentView.addSubview(peerContainerView)
        contentView.addSubview(meContainerView)

        peerContainerView.addSubview(peerAvatarImageView)
        peerContainerView.addSubview(peerBubbleView)
        peerBubbleView.addSubview(peerMessageLabel)

        meContainerView.addSubview(meAvatarImageView)
        meContainerView.addSubview(meBubbleView)
        meBubbleView.addSubview(meMessageLabel)

        peerContainerView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        meContainerView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        peerAvatarImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalToSuperview().inset(8)
            make.width.height.equalTo(Layout.avatarSize)
            make.bottom.lessThanOrEqualToSuperview().inset(8)
        }

        peerBubbleView.snp.makeConstraints { make in
            make.leading.equalTo(peerAvatarImageView.snp.trailing).offset(10)
            make.top.equalTo(peerAvatarImageView)
            make.trailing.lessThanOrEqualToSuperview().inset(Layout.horizontalInset)
            make.width.lessThanOrEqualTo(contentView.snp.width).multipliedBy(Layout.bubbleMaxWidthRatio)
            make.bottom.equalToSuperview().inset(8)
        }

        peerMessageLabel.snp.makeConstraints { make in
            make.edges.equalToSuperview().inset(UIEdgeInsets(top: 12, left: 14, bottom: 12, right: 14))
        }

        meAvatarImageView.snp.makeConstraints { make in
            make.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalToSuperview().inset(8)
            make.width.height.equalTo(Layout.avatarSize)
            make.bottom.lessThanOrEqualToSuperview().inset(8)
        }

        meBubbleView.snp.makeConstraints { make in
            make.trailing.equalTo(meAvatarImageView.snp.leading).offset(-10)
            make.top.equalTo(meAvatarImageView)
            make.leading.greaterThanOrEqualToSuperview().inset(Layout.horizontalInset)
            make.width.lessThanOrEqualTo(contentView.snp.width).multipliedBy(Layout.bubbleMaxWidthRatio)
            make.bottom.equalToSuperview().inset(8)
        }

        meMessageLabel.snp.makeConstraints { make in
            make.edges.equalToSuperview().inset(UIEdgeInsets(top: 12, left: 14, bottom: 12, right: 14))
        }
    }
}
