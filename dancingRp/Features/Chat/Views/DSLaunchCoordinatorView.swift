
import Foundation

import UIKit

enum DS_ChatTab: Int, CaseIterable {
    case chat
    case friend
    case ask

    var normalImageName: String {
       var modityR: String! = String(cString: [117,110,113,117,97,110,116,105,122,101,0], encoding: .utf8)!
    var didR: Int = 2
      didR >>= Swift.min(3, labs(didR))

        switch self {
        case .chat: return "seg_chat"
        case .friend: return "seg_friend"
        case .ask: return "seg_ask"
        }
       var tracksB: [Any]! = [100, 10, 58]
      withUnsafeMutablePointer(to: &tracksB) { pointer in
             _ = pointer.pointee
      }
       var content4: Double = 2.0
      while (4 >= (tracksB.count | 5)) {
         tracksB = [tracksB.count]
         break
      }
      while ((Int(content4 > 186861301.0 || content4 < -186861301.0 ? 9.0 : content4)) <= tracksB.count) {
          var normalizedK: Double = 4.0
         tracksB = [tracksB.count]
         normalizedK /= Swift.max((Double(1 >> (Swift.min(labs(Int(content4 > 256000699.0 || content4 < -256000699.0 ? 98.0 : content4)), 5)))), 2)
         break
      }
      if 4 == (tracksB.count / (Swift.max(8, Int(content4 > 262585987.0 || content4 < -262585987.0 ? 17.0 : content4)))) || (content4 / (Swift.max(3.40, 9))) == 1.74 {
         content4 -= Double(tracksB.count)
      }
         tracksB = [tracksB.count]
          var workY: String! = String(cString: [115,116,105,99,107,101,114,112,97,99,107,0], encoding: .utf8)!
          var medias: String! = String(cString: [99,111,110,99,97,116,100,101,99,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &medias) { pointer in
                _ = pointer.pointee
         }
          var coinsO: String! = String(cString: [104,101,120,98,105,110,0], encoding: .utf8)!
          _ = coinsO
         content4 -= Double(3)
         workY = "\((1 >> (Swift.min(1, labs(Int(content4 > 19050894.0 || content4 < -19050894.0 ? 53.0 : content4))))))"
         medias = "\(workY.count)"
         coinsO = "\(coinsO.count)"
       var pathk: String! = String(cString: [102,117,108,108,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &pathk) { pointer in
             _ = pointer.pointee
      }
       var tap9: String! = String(cString: [111,110,121,120,100,0], encoding: .utf8)!
         pathk = "\((3 / (Swift.max(1, Int(content4 > 254771721.0 || content4 < -254771721.0 ? 64.0 : content4)))))"
         tap9 = "\((pathk == (String(cString:[77,0], encoding: .utf8)!) ? pathk.count : tap9.count))"
      didR |= 3
       var accentJ: Double = 1.0
      withUnsafeMutablePointer(to: &accentJ) { pointer in
    
      }
      for _ in 0 ..< 1 {
         accentJ /= Swift.max(2, (Double(Int(accentJ > 312872655.0 || accentJ < -312872655.0 ? 10.0 : accentJ))))
      }
          var live0: String! = String(cString: [115,116,114,116,111,100,0], encoding: .utf8)!
         accentJ /= Swift.max(3, Double(2))
         live0.append("\((Int(accentJ > 178446554.0 || accentJ < -178446554.0 ? 86.0 : accentJ) % (Swift.max(3, 7))))")
          var subtitleE: String! = String(cString: [116,111,117,99,104,101,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &subtitleE) { pointer in
                _ = pointer.pointee
         }
         accentJ -= Double(subtitleE.count)
      modityR = "\(modityR.count)"
    }

    var selectedImageName: String {
       var namesi: Int = 2
   withUnsafeMutablePointer(to: &namesi) { pointer in
    
   }
    var scriptP: [Any]! = [81.0]
   withUnsafeMutablePointer(to: &scriptP) { pointer in
          _ = pointer.pointee
   }
       var avatarsz: String! = String(cString: [108,105,98,115,104,105,110,101,0], encoding: .utf8)!
       var peerz: Double = 1.0
       _ = peerz
      repeat {
         avatarsz = "\(3)"
         if (String(cString:[112,110,108,102,122,56,0], encoding: .utf8)!) == avatarsz {
            break
         }
      } while ((String(cString:[112,110,108,102,122,56,0], encoding: .utf8)!) == avatarsz) && ((avatarsz.count / (Swift.max(7, Int(peerz > 39874983.0 || peerz < -39874983.0 ? 61.0 : peerz)))) <= 5 && 5 <= (5 ^ avatarsz.count))
         avatarsz = "\(avatarsz.count)"
      for _ in 0 ..< 3 {
         avatarsz = "\((3 | Int(peerz > 372219003.0 || peerz < -372219003.0 ? 7.0 : peerz)))"
      }
         avatarsz.append("\(3)")
      repeat {
          var modityG: String! = String(cString: [99,111,109,112,101,110,115,97,116,105,111,110,0], encoding: .utf8)!
          _ = modityG
          var durationZ: Int = 5
          _ = durationZ
          var containerU: String! = String(cString: [102,97,97,110,100,99,116,0], encoding: .utf8)!
          var constraintR: String! = String(cString: [113,117,111,116,101,100,0], encoding: .utf8)!
          var chatV: [Any]! = [String(cString: [97,108,105,103,110,101,100,0], encoding: .utf8)!, String(cString: [99,111,112,105,101,100,0], encoding: .utf8)!]
          _ = chatV
         avatarsz.append("\((Int(peerz > 258141540.0 || peerz < -258141540.0 ? 58.0 : peerz)))")
         modityG.append("\((constraintR == (String(cString:[89,0], encoding: .utf8)!) ? constraintR.count : chatV.count))")
         durationZ += 3
         containerU.append("\(constraintR.count)")
         chatV.append((Int(peerz > 371469979.0 || peerz < -371469979.0 ? 70.0 : peerz)))
         if avatarsz.count == 465331 {
            break
         }
      } while (avatarsz.count == 465331) && (4.95 < (peerz - Double(avatarsz.count)))
      repeat {
         peerz /= Swift.max(3, (Double(Int(peerz > 375896747.0 || peerz < -375896747.0 ? 87.0 : peerz))))
         if peerz == 3969915.0 {
            break
         }
      } while ((peerz - 5.0) < 1.43) && (peerz == 3969915.0)
      scriptP.append(namesi)

        switch self {
        case .chat: return "seg_chat_sel"
        case .friend: return "seg_friend_sel"
        case .ask: return "seg_ask_sel"
        }
      namesi -= namesi & namesi
    }
}

final class DSLaunchCoordinatorView: UIView {

    private enum Layout {
        static let horizontalInset: CGFloat = 16
        static let titleHeight: CGFloat = 32
        static let segmentHeight: CGFloat = 40
        static let segmentWidth: CGFloat = 82
    }

    var onTabSelected: ((DS_ChatTab) -> Void)?

    private(set) var selectedTab: DS_ChatTab = .chat

    private let titleLabel: UILabel = {
       var presentingQ: Double = 2.0
    var encodedC: Float = 4.0
    _ = encodedC
   while ((Double(2 - Int(presentingQ))) > 3.57) {
       var friend_g0r: String! = String(cString: [97,108,108,111,119,0], encoding: .utf8)!
       var updatedY: String! = String(cString: [113,115,118,100,101,105,110,116,0], encoding: .utf8)!
       var bubble0: Bool = true
       var vnewsv: Bool = false
       var shop8: Bool = false
         shop8 = (vnewsv ? shop8 : !vnewsv)
      for _ in 0 ..< 2 {
          var a_playerz: String! = String(cString: [97,98,115,116,114,97,99,116,0], encoding: .utf8)!
          var worky: String! = String(cString: [97,112,112,108,121,0], encoding: .utf8)!
          var pathss: [Any]! = [[UILabel(frame:CGRect.zero)]]
          var storedp: [Any]! = [32]
          var pricesW: String! = String(cString: [100,105,103,101,115,116,98,121,111,98,106,0], encoding: .utf8)!
          _ = pricesW
         bubble0 = worky == friend_g0r
         a_playerz.append("\(updatedY.count * 1)")
         pathss = [3 << (Swift.min(2, a_playerz.count))]
         storedp.append(pricesW.count)
         pricesW.append("\(((bubble0 ? 4 : 2) & 1))")
      }
       var date7: Bool = false
          var keysU: Int = 5
          _ = keysU
         shop8 = !date7
         keysU &= ((date7 ? 3 : 3))
       var postsD: String! = String(cString: [100,114,97,105,110,0], encoding: .utf8)!
       var blur3: String! = String(cString: [97,117,116,111,99,111,114,114,0], encoding: .utf8)!
          var relativeV: String! = String(cString: [100,101,116,101,99,116,105,110,103,0], encoding: .utf8)!
         friend_g0r.append("\(relativeV.count)")
      if friend_g0r.count < updatedY.count {
         updatedY.append("\(((shop8 ? 1 : 3)))")
      }
          var greenA: String! = String(cString: [117,110,112,97,99,107,0], encoding: .utf8)!
          _ = greenA
          var intrinsicM: String! = String(cString: [112,97,114,115,101,114,115,0], encoding: .utf8)!
          _ = intrinsicM
         vnewsv = shop8
         greenA.append("\(((bubble0 ? 3 : 2)))")
         intrinsicM = "\(3 | intrinsicM.count)"
          var cacheA: [String: Any]! = [String(cString: [97,108,109,111,115,116,0], encoding: .utf8)!:[String(cString: [112,114,111,106,101,99,116,101,100,0], encoding: .utf8)!:31, String(cString: [115,109,111,111,116,104,101,100,0], encoding: .utf8)!:5]]
         postsD = "\(1 ^ updatedY.count)"
         cacheA = ["\(date7)": 3]
          var componentO: Double = 4.0
          var extraF: String! = String(cString: [116,97,112,112,101,100,0], encoding: .utf8)!
          _ = extraF
          var timestampk: Double = 3.0
          _ = timestampk
         friend_g0r = "\(3)"
         componentO /= Swift.max((Double((bubble0 ? 1 : 4) << (Swift.min(extraF.count, 5)))), 4)
         extraF.append("\(1)")
         timestampk -= Double(2 & postsD.count)
         blur3 = "\(((String(cString:[77,0], encoding: .utf8)!) == friend_g0r ? friend_g0r.count : updatedY.count))"
      if postsD.count <= 3 {
         postsD.append("\(friend_g0r.count)")
      }
      for _ in 0 ..< 2 {
         blur3.append("\(((date7 ? 3 : 4)))")
      }
         updatedY = "\(1)"
       var attributesw: Bool = false
      withUnsafeMutablePointer(to: &attributesw) { pointer in
    
      }
         attributesw = friend_g0r.count <= updatedY.count
      encodedC += Float(2)
      break
   }

        let label = UILabel()
       var formC: String! = String(cString: [115,101,110,100,101,114,115,0], encoding: .utf8)!
       _ = formC
       var optiont: String! = String(cString: [102,108,117,115,104,0], encoding: .utf8)!
         optiont.append("\((optiont == (String(cString:[106,0], encoding: .utf8)!) ? optiont.count : formC.count))")
         optiont = "\(optiont.count)"
      while (formC == optiont) {
          var placeholderM: String! = String(cString: [105,112,100,111,112,100,0], encoding: .utf8)!
         optiont.append("\(formC.count)")
         placeholderM = "\(placeholderM.count * formC.count)"
         break
      }
      repeat {
         optiont = "\((optiont == (String(cString:[55,0], encoding: .utf8)!) ? formC.count : optiont.count))"
         if optiont == (String(cString:[49,52,95,108,51,107,54,0], encoding: .utf8)!) {
            break
         }
      } while (optiont == (String(cString:[49,52,95,108,51,107,54,0], encoding: .utf8)!)) && (formC == String(cString:[78,0], encoding: .utf8)!)
          var main_bG: Double = 0.0
         formC = "\(optiont.count)"
         main_bG /= Swift.max(Double(1), 3)
         optiont = "\((formC == (String(cString:[121,0], encoding: .utf8)!) ? optiont.count : formC.count))"
      presentingQ -= (Double(Int(encodedC > 166323959.0 || encodedC < -166323959.0 ? 42.0 : encodedC)))
        label.text = "Friend"
        label.textColor = .white
        label.font = .systemFont(ofSize: 20, weight: .medium)
        return label
    }()

    private lazy var segmentStackView: UIStackView = {
       var confirmK: Int = 1
    var passwordx: String! = String(cString: [112,114,111,109,112,116,0], encoding: .utf8)!
   repeat {
      passwordx = "\(confirmK % 2)"
      if passwordx == (String(cString:[54,113,53,0], encoding: .utf8)!) {
         break
      }
   } while (passwordx == (String(cString:[54,113,53,0], encoding: .utf8)!)) && (passwordx.count <= 2)

        let follow = UIStackView(arrangedSubviews: [chatButton, friendButton, askButton])
       var pickerc: String! = String(cString: [116,101,120,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &pickerc) { pointer in
             _ = pointer.pointee
      }
       var trimmedu: [String: Any]! = [String(cString: [115,105,122,101,108,101,115,115,0], encoding: .utf8)!:25, String(cString: [111,102,111,114,109,97,116,0], encoding: .utf8)!:69]
       var cancele: String! = String(cString: [109,99,111,109,112,0], encoding: .utf8)!
          var fittedf: String! = String(cString: [101,110,116,105,114,101,108,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &fittedf) { pointer in
    
         }
         pickerc = "\(3 | trimmedu.keys.count)"
         fittedf = "\(3)"
         trimmedu[pickerc] = trimmedu.count
         pickerc = "\(2 >> (Swift.min(3, trimmedu.count)))"
         trimmedu["\(cancele)"] = cancele.count & 2
      repeat {
          var removedX: [String: Any]! = [String(cString: [112,114,101,100,105,99,116,105,111,110,115,0], encoding: .utf8)!:String(cString: [119,97,115,0], encoding: .utf8)!, String(cString: [118,97,114,105,97,110,99,101,120,104,0], encoding: .utf8)!:String(cString: [115,117,109,115,113,0], encoding: .utf8)!, String(cString: [102,114,97,109,101,98,117,102,102,101,114,115,0], encoding: .utf8)!:String(cString: [114,101,115,101,116,0], encoding: .utf8)!]
         trimmedu = ["\(trimmedu.values.count)": pickerc.count - 3]
         removedX[pickerc] = 1
         if 3525600 == trimmedu.count {
            break
         }
      } while (3525600 == trimmedu.count) && ((3 * pickerc.count) > 1 && 3 > (3 * trimmedu.count))
         cancele.append("\(pickerc.count ^ 3)")
         trimmedu = [cancele: pickerc.count]
       var normalF: Double = 2.0
       var visibilityc: Double = 0.0
      withUnsafeMutablePointer(to: &visibilityc) { pointer in
    
      }
         pickerc = "\(pickerc.count % (Swift.max(3, 5)))"
         normalF -= (Double(pickerc.count | Int(visibilityc > 126860529.0 || visibilityc < -126860529.0 ? 60.0 : visibilityc)))
         visibilityc -= Double(cancele.count & 1)
      passwordx = "\(cancele.count % 3)"
        follow.axis = .horizontal
      confirmK |= confirmK / 3
        follow.spacing = 20
        follow.alignment = .center
        follow.distribution = .fill
        return follow
    }()

    private lazy var chatButton = makeSegmentButton(for: .chat)
    private lazy var friendButton = makeSegmentButton(for: .friend)
    private lazy var askButton = makeSegmentButton(for: .ask)

    private lazy var segmentButtons: [UIButton] = [chatButton, friendButton, askButton]

    override init(frame: CGRect) {
        super.init(frame: frame)
        backgroundColor = .black
        setupUI()
        updateTabSelection(.chat)
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    override var intrinsicContentSize: CGSize {
       var remove6: String! = String(cString: [115,119,105,112,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &remove6) { pointer in
          _ = pointer.pointee
   }
    var layoutU: String! = String(cString: [114,101,112,114,101,112,97,114,101,0], encoding: .utf8)!
      layoutU = "\(1)"
   if !remove6.hasSuffix(layoutU) {
      remove6 = "\(2)"
   }

            return CGSize(width: UIView.noIntrinsicMetric, height: 116)
    }

    func updateTabSelection(_ tab: DS_ChatTab) {
       var tapB: Float = 0.0
   withUnsafeMutablePointer(to: &tapB) { pointer in
          _ = pointer.pointee
   }
   while ((tapB * 2.60) == 5.6 && (2.60 * tapB) == 1.90) {
      tapB /= Swift.max((Float(Int(tapB > 220035539.0 || tapB < -220035539.0 ? 47.0 : tapB) + Int(tapB > 154621857.0 || tapB < -154621857.0 ? 98.0 : tapB))), 1)
      break
   }

        selectedTab = tab
        segmentButtons.enumerated().forEach { index, button in
            let bottom = DS_ChatTab(rawValue: index) ?? .chat
            button.isSelected = bottom == tab
        }
    }

    private func setupUI() {
       var suggested3: Bool = false
      suggested3 = !suggested3

        addSubview(titleLabel)
        addSubview(segmentStackView)

        titleLabel.snp.makeConstraints { make in
            make.leading.top.equalToSuperview().inset(Layout.horizontalInset)
            make.height.equalTo(25)
        }

        segmentStackView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalTo(titleLabel.snp.bottom).offset(16)
            make.height.equalTo(Layout.segmentHeight)
            make.bottom.equalToSuperview().inset(12)
        }

        segmentButtons.forEach { button in
            button.snp.makeConstraints { make in
                make.width.equalTo(Layout.segmentWidth)
                make.height.equalTo(Layout.segmentHeight)
            }
        }

        chatButton.addTarget(self, action: #selector(didTapChat), for: .touchUpInside)
        friendButton.addTarget(self, action: #selector(didTapFriend), for: .touchUpInside)
        askButton.addTarget(self, action: #selector(didTapAsk), for: .touchUpInside)
    }

    private func makeSegmentButton(for tab: DS_ChatTab) -> UIButton {
       var vertical5: Float = 5.0
    var productw: String! = String(cString: [109,111,109,101,110,116,97,114,121,0], encoding: .utf8)!
   while ((5 * productw.count) <= 1) {
       var configuredf: Float = 0.0
         configuredf += (Float(Int(configuredf > 340118387.0 || configuredf < -340118387.0 ? 28.0 : configuredf) * 1))
      if configuredf > 3.11 {
          var hoursA: Bool = true
         configuredf -= (Float(Int(configuredf > 332069716.0 || configuredf < -332069716.0 ? 80.0 : configuredf) | 2))
         hoursA = 81.21 > configuredf || hoursA
      }
      while ((5.41 + configuredf) > 4.78 && (configuredf + configuredf) > 5.41) {
          var destinationk: [Any]! = [String(cString: [117,110,100,101,114,114,117,110,0], encoding: .utf8)!, String(cString: [103,97,105,110,115,0], encoding: .utf8)!, String(cString: [114,101,116,97,105,110,115,0], encoding: .utf8)!]
         configuredf -= Float(3)
         destinationk.append(destinationk.count)
         break
      }
      vertical5 -= (Float(Int(configuredf > 390250062.0 || configuredf < -390250062.0 ? 60.0 : configuredf)))
      break
   }

      vertical5 -= Float(productw.count)
        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setImage(UIImage(named: tab.normalImageName), for: .normal)
        dimmingButton.setImage(UIImage(named: tab.selectedImageName), for: .selected)
        dimmingButton.imageView?.contentMode = .scaleAspectFit
        dimmingButton.tag = tab.rawValue
        return dimmingButton
    }

    @objc private func didTapChat() {
       var clearu: String! = String(cString: [102,100,99,116,0], encoding: .utf8)!
    _ = clearu
       var formatterk: Double = 3.0
      for _ in 0 ..< 2 {
         formatterk /= Swift.max((Double(2 | Int(formatterk > 25402310.0 || formatterk < -25402310.0 ? 44.0 : formatterk))), 4)
      }
          var encodedI: Double = 4.0
         formatterk -= (Double(Int(encodedI > 47312428.0 || encodedI < -47312428.0 ? 94.0 : encodedI)))
      while (formatterk >= formatterk) {
         formatterk -= (Double(Int(formatterk > 234522685.0 || formatterk < -234522685.0 ? 28.0 : formatterk) ^ 1))
         break
      }
      clearu.append("\(clearu.count | 2)")

        selectTab(.chat)
    }

    @objc private func didTapFriend() {
       var optionsx: String! = String(cString: [112,114,101,100,111,119,110,108,111,97,100,0], encoding: .utf8)!
       var unreadZ: [Any]! = [3, 11, 76]
       var mediaQ: [String: Any]! = [String(cString: [98,109,111,100,101,0], encoding: .utf8)!:String(cString: [116,119,105,108,105,103,104,116,0], encoding: .utf8)!, String(cString: [97,100,100,101,100,0], encoding: .utf8)!:String(cString: [109,107,118,109,117,120,101,114,116,121,112,101,115,0], encoding: .utf8)!]
          var ownE: String! = String(cString: [115,112,101,99,116,105,109,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &ownE) { pointer in
                _ = pointer.pointee
         }
          var candidateI: String! = String(cString: [98,97,116,99,104,101,115,0], encoding: .utf8)!
          _ = candidateI
          var handlingc: Bool = true
         unreadZ = [((handlingc ? 3 : 4))]
         ownE = "\(unreadZ.count / (Swift.max(3, 1)))"
         candidateI.append("\(2)")
          var randomh: String! = String(cString: [99,104,101,99,107,108,105,110,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &randomh) { pointer in
    
         }
         unreadZ = [randomh.count + 3]
         mediaQ["\(unreadZ.count)"] = mediaQ.keys.count
      repeat {
         mediaQ["\(unreadZ.count)"] = unreadZ.count
         if mediaQ.count == 1632790 {
            break
         }
      } while (mediaQ.count == 1632790) && ((mediaQ.values.count % (Swift.max(3, 1))) > 1 || (mediaQ.values.count % (Swift.max(3, 4))) > 4)
       var documentsb: [String: Any]! = [String(cString: [105,115,115,117,101,115,0], encoding: .utf8)!:String(cString: [119,97,118,101,115,121,110,116,104,0], encoding: .utf8)!, String(cString: [99,111,110,102,105,103,117,114,97,116,105,111,110,115,0], encoding: .utf8)!:String(cString: [100,115,116,115,117,98,0], encoding: .utf8)!]
         mediaQ = ["\(documentsb.values.count)": documentsb.count]
      optionsx = "\(optionsx.count)"

        selectTab(.friend)
    }

    @objc private func didTapAsk() {
       var tappeda: [String: Any]! = [String(cString: [112,108,97,99,101,109,97,114,107,115,0], encoding: .utf8)!:false]
    _ = tappeda
    var relativeN: [Any]! = [79, 81]
   withUnsafeMutablePointer(to: &relativeN) { pointer in
    
   }
    var description_0yK: String! = String(cString: [114,101,118,101,97,108,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &description_0yK) { pointer in
          _ = pointer.pointee
   }
   if tappeda.keys.contains("\(relativeN.count)") {
      relativeN.append(tappeda.values.count / 1)
   }
      description_0yK.append("\(description_0yK.count | relativeN.count)")

        selectTab(.ask)
    }

    private func selectTab(_ tab: DS_ChatTab) {
       var exit8: String! = String(cString: [120,109,112,101,103,0], encoding: .utf8)!
    var sendG: [Any]! = [16, 32]
   repeat {
      exit8.append("\(sendG.count)")
      if exit8 == (String(cString:[97,99,119,0], encoding: .utf8)!) {
         break
      }
   } while ((sendG.count * exit8.count) > 1 && (sendG.count * 1) > 5) && (exit8 == (String(cString:[97,99,119,0], encoding: .utf8)!))

        guard selectedTab != tab else { return }
        updateTabSelection(tab)
        onTabSelected?(tab)
    }
}
