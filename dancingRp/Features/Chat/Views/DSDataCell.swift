
import Foundation

import UIKit

struct DSCoverRegisterItem {
    let userId: String
    let avatarImageName: String?
    let name: String
}

final class DSDataCell: UITableViewCell {

    static let reuseIdentifier = "DSDataCell"

    private enum Layout {
        static let horizontalInset: CGFloat = 16
        static let rowHeight: CGFloat = 64
        static let avatarSize: CGFloat = 44
        static let spacing: CGFloat = 8
        static let cardCornerRadius: CGFloat = 24
    }

    var onChatTapped: (() -> Void)?

    private let infoCardView: UIView = {
       var layout6: Double = 4.0
       var bottomM: Bool = false
       var reda: [Any]! = [50, 79, 22]
       var tappedY: String! = String(cString: [99,97,110,99,101,108,108,101,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &tappedY) { pointer in
    
      }
      while ((reda.count % (Swift.max(tappedY.count, 3))) > 1) {
          var phrasesy: Bool = false
          var docp: Bool = false
          var o_width5: [String: Any]! = [String(cString: [102,97,100,101,0], encoding: .utf8)!:87, String(cString: [97,112,112,101,110,100,97,108,108,0], encoding: .utf8)!:79, String(cString: [111,112,117,115,108,97,99,105,110,103,0], encoding: .utf8)!:10]
         tappedY = "\((3 >> (Swift.min(2, labs((bottomM ? 1 : 4))))))"
         phrasesy = tappedY.count > 5 && !bottomM
         docp = o_width5["\(phrasesy)"] != nil
         o_width5 = ["\(phrasesy)": (tappedY.count | (phrasesy ? 5 : 1))]
         break
      }
      repeat {
          var delayP: Int = 1
          var delete_9m: Bool = false
          _ = delete_9m
          var createdC: String! = String(cString: [115,99,104,101,109,97,115,0], encoding: .utf8)!
          var config1: String! = String(cString: [100,101,99,105,100,101,0], encoding: .utf8)!
          var fullV: String! = String(cString: [99,112,105,97,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &fullV) { pointer in
    
         }
         bottomM = (fullV.count - delayP) < 48
         delete_9m = (31 > ((delete_9m ? 31 : fullV.count) * fullV.count))
         createdC = "\((tappedY == (String(cString:[70,0], encoding: .utf8)!) ? tappedY.count : (bottomM ? 1 : 5)))"
         config1 = "\(1)"
         if bottomM ? !bottomM : bottomM {
            break
         }
      } while (bottomM ? !bottomM : bottomM) && (4 < tappedY.count)
          var sourceO: Bool = true
         tappedY.append("\(tappedY.count)")
          var v_centerL: String! = String(cString: [115,116,111,114,121,98,111,97,114,100,0], encoding: .utf8)!
          var publishh: Bool = true
          var sendf: Float = 2.0
          _ = sendf
         reda = [((bottomM ? 1 : 4) << (Swift.min(labs(1), 2)))]
         v_centerL = "\(((publishh ? 4 : 3) - Int(sendf > 296109092.0 || sendf < -296109092.0 ? 99.0 : sendf)))"
         publishh = !publishh || reda.count <= 37
         sendf /= Swift.max(1, Float(2))
          var editd: [Any]! = [36.0]
         bottomM = (String(cString:[117,0], encoding: .utf8)!) == tappedY || editd.count == 49
          var partsy: [String: Any]! = [String(cString: [112,105,99,107,101,114,0], encoding: .utf8)!:26.0]
          var pendingB: Double = 4.0
          var camera0: Int = 4
         reda = [partsy.count]
         pendingB /= Swift.max(Double(camera0 << (Swift.min(tappedY.count, 5))), 1)
         camera0 ^= partsy.count
         tappedY = "\(1 + reda.count)"
         bottomM = (reda.contains { $0 as? Bool == bottomM })
         tappedY = "\(3 / (Swift.max(7, reda.count)))"
      layout6 -= Double(2 << (Swift.min(3, tappedY.count)))

        let view = UIView()
        view.backgroundColor = .white
        view.layer.cornerRadius = Layout.cardCornerRadius
        view.clipsToBounds = true
        return view
    }()

    private let avatarImageView: UIImageView = {
       var tapE: String! = String(cString: [114,101,113,117,101,115,116,0], encoding: .utf8)!
   while (tapE.hasPrefix(tapE)) {
      tapE = "\(tapE.count / (Swift.max(3, 4)))"
      break
   }

        let imageView = UIImageView()
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        imageView.backgroundColor = UIColor.hex("#E8E8E8")
        imageView.layer.cornerRadius = Layout.avatarSize / 2
        return imageView
    }()

    private let nameLabel: UILabel = {
       var timestampF: Double = 5.0
    _ = timestampF
   while (4.88 > (timestampF / (Swift.max(2.34, 4))) && 4.71 > (timestampF / 2.34)) {
       var processedo: Bool = false
       _ = processedo
       var homeO: String! = String(cString: [118,108,99,115,0], encoding: .utf8)!
       var coinsi: String! = String(cString: [115,105,103,109,97,0], encoding: .utf8)!
       _ = coinsi
       var cancelx: String! = String(cString: [119,104,116,120,0], encoding: .utf8)!
       _ = cancelx
      if processedo || cancelx.count > 2 {
         cancelx.append("\(coinsi.count | cancelx.count)")
      }
      repeat {
         processedo = homeO == coinsi
         if processedo ? !processedo : processedo {
            break
         }
      } while (!processedo) && (processedo ? !processedo : processedo)
      repeat {
          var size_qI: Double = 0.0
         withUnsafeMutablePointer(to: &size_qI) { pointer in
                _ = pointer.pointee
         }
         coinsi.append("\(3 << (Swift.min(4, homeO.count)))")
         size_qI /= Swift.max((Double(2 - (processedo ? 3 : 3))), 3)
         if coinsi == (String(cString:[52,95,120,111,53,118,102,50,116,103,0], encoding: .utf8)!) {
            break
         }
      } while (coinsi == (String(cString:[52,95,120,111,53,118,102,50,116,103,0], encoding: .utf8)!)) && (!coinsi.hasSuffix("\(processedo)"))
         coinsi = "\(3 ^ homeO.count)"
      for _ in 0 ..< 1 {
          var creationR: String! = String(cString: [117,105,100,0], encoding: .utf8)!
          _ = creationR
          var blue_: Double = 1.0
          _ = blue_
          var size_cpe: Int = 3
          _ = size_cpe
         processedo = coinsi.count > 95
         creationR = "\(((processedo ? 1 : 1) - cancelx.count))"
         blue_ += Double(size_cpe)
         size_cpe /= Swift.max(1, 2)
      }
          var setupY: String! = String(cString: [102,114,97,109,101,98,117,102,102,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &setupY) { pointer in
    
         }
          var playA: [Any]! = [74, 29]
          _ = playA
         processedo = (22 < ((!processedo ? coinsi.count : 22) / (Swift.max(coinsi.count, 5))))
         setupY = "\(1)"
         playA.append(homeO.count - 3)
      repeat {
         cancelx = "\(((String(cString:[78,0], encoding: .utf8)!) == homeO ? (processedo ? 3 : 5) : homeO.count))"
         if 778147 == cancelx.count {
            break
         }
      } while (778147 == cancelx.count) && (coinsi.hasSuffix("\(cancelx.count)"))
      repeat {
         cancelx = "\(cancelx.count + 2)"
         if cancelx == (String(cString:[117,102,99,106,0], encoding: .utf8)!) {
            break
         }
      } while (cancelx == (String(cString:[117,102,99,106,0], encoding: .utf8)!)) && (4 <= cancelx.count)
      for _ in 0 ..< 1 {
          var timestampp: String! = String(cString: [108,111,103,111,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &timestampp) { pointer in
                _ = pointer.pointee
         }
         coinsi = "\(homeO.count)"
         timestampp.append("\(timestampp.count)")
      }
         processedo = cancelx.count == coinsi.count
       var horizontalu: [String: Any]! = [String(cString: [109,98,114,116,104,114,101,97,100,0], encoding: .utf8)!:28, String(cString: [116,117,108,115,105,0], encoding: .utf8)!:98]
         homeO.append("\(cancelx.count ^ 1)")
         horizontalu["\(cancelx)"] = cancelx.count ^ 2
      timestampF += Double(cancelx.count + 3)
      break
   }

        let label = UILabel()
        label.textColor = .black
        label.font = .systemFont(ofSize: 16, weight: .bold)
        return label
    }()

    private lazy var chatButton: UIButton = {
       var fullJ: String! = String(cString: [97,112,112,114,111,120,0], encoding: .utf8)!
    _ = fullJ
       var normalq: String! = String(cString: [103,101,110,102,105,108,101,115,0], encoding: .utf8)!
       _ = normalq
       var collectionm: String! = String(cString: [117,105,111,109,111,118,101,0], encoding: .utf8)!
       var extension_rku: Float = 1.0
       _ = extension_rku
         collectionm.append("\(normalq.count)")
      while ((extension_rku - Float(normalq.count)) < 4.37) {
          var testF: String! = String(cString: [108,105,103,104,116,101,110,105,110,103,0], encoding: .utf8)!
          var componentsF: Float = 3.0
          _ = componentsF
          var tap_: Double = 5.0
         withUnsafeMutablePointer(to: &tap_) { pointer in
    
         }
          var partsp: String! = String(cString: [102,97,115,116,102,105,114,115,116,112,97,115,115,0], encoding: .utf8)!
         normalq.append("\(partsp.count >> (Swift.min(normalq.count, 4)))")
         testF = "\(testF.count)"
         componentsF -= (Float(Int(tap_ > 225276766.0 || tap_ < -225276766.0 ? 24.0 : tap_)))
         break
      }
      if (normalq.count / (Swift.max(4, 9))) < 4 && (4 + Int(extension_rku > 236348090.0 || extension_rku < -236348090.0 ? 57.0 : extension_rku)) < 3 {
         normalq.append("\((1 * Int(extension_rku > 352479978.0 || extension_rku < -352479978.0 ? 16.0 : extension_rku)))")
      }
         extension_rku -= (Float(collectionm == (String(cString:[76,0], encoding: .utf8)!) ? collectionm.count : Int(extension_rku > 111649837.0 || extension_rku < -111649837.0 ? 72.0 : extension_rku)))
       var cost9: Int = 5
         cost9 -= 2
      repeat {
          var opacityO: Bool = false
          var index5: [Any]! = [String(cString: [112,97,100,0], encoding: .utf8)!, String(cString: [97,99,99,101,108,101,114,97,116,101,0], encoding: .utf8)!]
          var i_countb: String! = String(cString: [108,111,103,105,99,97,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &i_countb) { pointer in
    
         }
          var keysF: Double = 2.0
         cost9 -= cost9
         opacityO = keysF >= 46.29 || (String(cString:[57,0], encoding: .utf8)!) == i_countb
         index5.append((Int(keysF > 71031181.0 || keysF < -71031181.0 ? 72.0 : keysF)))
         i_countb = "\(i_countb.count)"
         if 1426960 == cost9 {
            break
         }
      } while (!normalq.hasSuffix("\(cost9)")) && (1426960 == cost9)
       var works: Double = 3.0
       var relativel: Double = 2.0
      repeat {
         extension_rku -= Float(normalq.count | 3)
         if extension_rku == 2764065.0 {
            break
         }
      } while ((collectionm.count / (Swift.max(3, 10))) >= 2 || 3 >= (collectionm.count - Int(extension_rku > 87254321.0 || extension_rku < -87254321.0 ? 91.0 : extension_rku))) && (extension_rku == 2764065.0)
         works -= (Double(collectionm == (String(cString:[119,0], encoding: .utf8)!) ? Int(works > 388737383.0 || works < -388737383.0 ? 64.0 : works) : collectionm.count))
         relativel += Double(3)
      fullJ = "\(fullJ.count % (Swift.max(1, 3)))"

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setImage(UIImage(named: "friend_chat"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapChat), for: .touchUpInside)
        return dimmingButton
    }()

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        setupUI()
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    func configure(with item: DSCoverRegisterItem) {
       var list3: Int = 5
    var postK: String! = String(cString: [97,116,114,97,99,100,97,116,97,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &postK) { pointer in
          _ = pointer.pointee
   }
      postK = "\(((String(cString:[70,0], encoding: .utf8)!) == postK ? list3 : postK.count))"

   if (postK.count % 2) <= 5 || 5 <= (2 % (Swift.max(5, list3))) {
      postK.append("\(list3)")
   }
        avatarImageView.image = UserData.image(for: item.avatarImageName)
        nameLabel.text = item.name
    }

    private func setupUI() {
       var cost9: Double = 4.0
   if 3.58 > cost9 {
      cost9 -= (Double(Int(cost9 > 133021680.0 || cost9 < -133021680.0 ? 97.0 : cost9) / 3))
   }

        selectionStyle = .none
        backgroundColor = .clear
        contentView.backgroundColor = .clear

        contentView.addSubview(infoCardView)
        contentView.addSubview(chatButton)
        infoCardView.addSubview(avatarImageView)
        infoCardView.addSubview(nameLabel)

        infoCardView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalToSuperview()
            make.height.equalTo(Layout.rowHeight)
            make.trailing.equalTo(chatButton.snp.leading).offset(-Layout.spacing)
            make.bottom.equalToSuperview().offset(-12)
        }

        chatButton.snp.makeConstraints { make in
            make.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.centerY.equalTo(infoCardView)
            make.height.equalTo(Layout.rowHeight)
            make.width.equalTo(140)
        }

        avatarImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(12)
            make.centerY.equalToSuperview()
            make.width.height.equalTo(Layout.avatarSize)
        }

        nameLabel.snp.makeConstraints { make in
            make.leading.equalTo(avatarImageView.snp.trailing).offset(12)
            make.trailing.lessThanOrEqualToSuperview().inset(12)
            make.centerY.equalToSuperview()
        }
    }

    @objc private func didTapChat() {
       var keyc: String! = String(cString: [101,120,99,105,116,97,116,105,111,110,0], encoding: .utf8)!
   if keyc == String(cString:[73,0], encoding: .utf8)! {
      keyc.append("\(keyc.count / (Swift.max(8, keyc.count)))")
   }

        onChatTapped?()
    }
}
