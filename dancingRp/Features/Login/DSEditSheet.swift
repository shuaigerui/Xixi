
import Foundation

import AuthenticationServices
import Toast_Swift
import UIKit

final class DSEditSheet: NSObject {

    private weak var presentingViewController: UIViewController?


    private static func formattedName(from components: PersonNameComponents?) -> String? {
       var messageG: Double = 2.0
    var firsta: Bool = false
   if 4.7 > messageG {
       var presentingR: Float = 4.0
       var feedP: String! = String(cString: [119,104,101,116,104,101,114,0], encoding: .utf8)!
       var userx: Double = 4.0
         feedP.append("\((feedP.count + Int(userx > 17585137.0 || userx < -17585137.0 ? 15.0 : userx)))")
      while ((presentingR * 4.85) > 3.2 && 4.32 > (4.85 * presentingR)) {
         feedP.append("\(1)")
         break
      }
      for _ in 0 ..< 1 {
         userx /= Swift.max(5, (Double(feedP == (String(cString:[111,0], encoding: .utf8)!) ? feedP.count : Int(userx > 218509376.0 || userx < -218509376.0 ? 82.0 : userx))))
      }
          var handlingA: Int = 0
          var controllersE: [Any]! = [3, 55, 71]
         withUnsafeMutablePointer(to: &controllersE) { pointer in
    
         }
         presentingR /= Swift.max(2, (Float(2 + Int(userx > 195688912.0 || userx < -195688912.0 ? 72.0 : userx))))
         handlingA %= Swift.max(handlingA + 2, 2)
         controllersE = [feedP.count ^ controllersE.count]
          var attributesL: Int = 5
          _ = attributesL
          var followingF: Float = 1.0
          var instanto: [String: Any]! = [String(cString: [109,97,99,104,0], encoding: .utf8)!:String(cString: [97,110,97,108,111,103,0], encoding: .utf8)!, String(cString: [117,110,118,111,116,101,0], encoding: .utf8)!:String(cString: [112,97,99,107,101,100,0], encoding: .utf8)!, String(cString: [102,114,101,101,116,121,112,101,0], encoding: .utf8)!:String(cString: [102,105,120,116,117,114,101,115,0], encoding: .utf8)!]
         userx += Double(1)
         attributesL /= Swift.max(3, 2)
         followingF += Float(1 >> (Swift.min(labs(attributesL), 2)))
         instanto["\(attributesL)"] = (Int(presentingR > 136037259.0 || presentingR < -136037259.0 ? 36.0 : presentingR))
          var agoq: String! = String(cString: [117,95,55,57,95,115,117,109,100,0], encoding: .utf8)!
          var docU: String! = String(cString: [103,117,97,114,100,0], encoding: .utf8)!
          var u_titley: Float = 3.0
         presentingR /= Swift.max(4, (Float((String(cString:[50,0], encoding: .utf8)!) == docU ? docU.count : Int(presentingR > 105991897.0 || presentingR < -105991897.0 ? 45.0 : presentingR))))
         agoq = "\(2)"
         u_titley -= (Float(Int(userx > 146523514.0 || userx < -146523514.0 ? 30.0 : userx) & feedP.count))
      for _ in 0 ..< 3 {
         userx -= Double(3)
      }
      if (3.98 / (Swift.max(10, presentingR))) == 3.13 && 5 == (Int(presentingR > 24557603.0 || presentingR < -24557603.0 ? 7.0 : presentingR) / 2) {
         presentingR -= (Float(Int(userx > 114400028.0 || userx < -114400028.0 ? 77.0 : userx) * 1))
      }
         feedP = "\(1)"
      messageG /= Swift.max(5, (Double((firsta ? 5 : 1) / (Swift.max(Int(messageG > 289843696.0 || messageG < -289843696.0 ? 11.0 : messageG), 4)))))
   }

        guard let components else { return nil }
        let formatter = PersonNameComponentsFormatter()
       var fieldB: [Any]! = [String(cString: [102,108,101,120,105,98,108,101,0], encoding: .utf8)!, String(cString: [101,120,105,115,116,105,110,103,0], encoding: .utf8)!]
       var docd: String! = String(cString: [105,108,98,99,100,97,116,97,0], encoding: .utf8)!
         docd = "\(1)"
          var unread2: [String: Any]! = [String(cString: [108,105,98,119,101,98,112,0], encoding: .utf8)!:15, String(cString: [105,100,99,116,120,100,99,0], encoding: .utf8)!:11]
          var y_imageu: String! = String(cString: [98,97,110,110,101,100,0], encoding: .utf8)!
         docd = "\(y_imageu.count % 2)"
         unread2 = ["\(unread2.count)": 1]
      repeat {
          var safeR: String! = String(cString: [120,105,112,104,0], encoding: .utf8)!
          var userR: String! = String(cString: [109,101,109,122,101,114,111,0], encoding: .utf8)!
          var indexZ: Float = 5.0
         withUnsafeMutablePointer(to: &indexZ) { pointer in
                _ = pointer.pointee
         }
         fieldB.append((Int(indexZ > 319532048.0 || indexZ < -319532048.0 ? 46.0 : indexZ)))
         safeR.append("\(safeR.count << (Swift.min(1, fieldB.count)))")
         userR = "\(userR.count & safeR.count)"
         if fieldB.count == 2898662 {
            break
         }
      } while ((docd.count - fieldB.count) >= 1) && (fieldB.count == 2898662)
         fieldB.append(fieldB.count * docd.count)
         fieldB = [1]
         docd.append("\((docd == (String(cString:[117,0], encoding: .utf8)!) ? docd.count : fieldB.count))")
      messageG -= (Double((firsta ? 5 : 2) * 3))
        let name = formatter.string(from: components).trimmingCharacters(in: .whitespacesAndNewlines)
        return name.isEmpty ? nil : name
    }


    private func handleAuthorization(_ authorization: ASAuthorization) {
       var gradientx: String! = String(cString: [100,98,115,105,122,101,0], encoding: .utf8)!
    var time_3c1: String! = String(cString: [101,110,118,105,114,111,110,109,101,110,116,0], encoding: .utf8)!
   if time_3c1.hasSuffix("\(gradientx.count)") {
       var usersd: [Any]! = [11, 4]
       _ = usersd
       var followE: Double = 4.0
       var urlsA: Double = 4.0
          var phraseC: String! = String(cString: [118,101,114,115,105,111,110,101,100,0], encoding: .utf8)!
         followE -= Double(1 & usersd.count)
         phraseC.append("\((Int(urlsA > 36382728.0 || urlsA < -36382728.0 ? 76.0 : urlsA) >> (Swift.min(2, labs(3)))))")
          var status6: Int = 4
         urlsA -= Double(3)
         status6 >>= Swift.min(labs((Int(urlsA > 6775288.0 || urlsA < -6775288.0 ? 19.0 : urlsA))), 1)
       var nameY: String! = String(cString: [99,111,112,121,105,110,103,0], encoding: .utf8)!
       _ = nameY
       var main_md: String! = String(cString: [108,111,103,111,115,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         nameY = "\(nameY.count)"
      }
         nameY.append("\((Int(followE > 63516633.0 || followE < -63516633.0 ? 79.0 : followE) | 2))")
         main_md = "\((Int(urlsA > 169958259.0 || urlsA < -169958259.0 ? 19.0 : urlsA) / (Swift.max(9, main_md.count))))"
         main_md.append("\(main_md.count)")
         usersd.append(3 >> (Swift.min(4, main_md.count)))
          var nextv: String! = String(cString: [105,110,116,114,111,0], encoding: .utf8)!
          _ = nextv
          var controllersD: Float = 5.0
          var n_managerX: Bool = false
         urlsA += Double(nextv.count)
         controllersD -= Float(main_md.count - 1)
         n_managerX = 99 >= main_md.count
      time_3c1.append("\((gradientx == (String(cString:[81,0], encoding: .utf8)!) ? gradientx.count : Int(urlsA > 117975458.0 || urlsA < -117975458.0 ? 54.0 : urlsA)))")
   }

        guard let credential = authorization.credential as? ASAuthorizationAppleIDCredential else {
            return
        }

   if gradientx.contains("\(time_3c1.count)") {
      gradientx = "\(gradientx.count)"
   }
        let background = credential.user
        if DSSecondaryNews.shared.signInWithAppleSubject(background) {
            return
        }

        let seconds = Self.formattedName(from: credential.fullName)
        let selController = DSCurrentScriptsController(source: .apple(subject: background, suggestedName: seconds))
        presentingViewController?.navigationController?.pushViewController(selController, animated: true)
    }


    func startSignIn(from viewController: UIViewController) {
       var keptT: Int = 0
   withUnsafeMutablePointer(to: &keptT) { pointer in
    
   }
   while (5 >= (keptT & 2) || 3 >= (keptT & 2)) {
       var celly: Double = 4.0
       var namesI: Double = 4.0
       var picker3: Double = 1.0
      withUnsafeMutablePointer(to: &picker3) { pointer in
             _ = pointer.pointee
      }
       var remote9: Double = 1.0
         namesI += (Double(Int(remote9 > 209815856.0 || remote9 < -209815856.0 ? 95.0 : remote9) - 1))
      while ((Double(3 - Int(celly))) == 4.66) {
          var auto_zzw: String! = String(cString: [97,109,117,108,116,105,0], encoding: .utf8)!
          var exitr: String! = String(cString: [100,99,116,115,117,98,0], encoding: .utf8)!
          var r_imageJ: String! = String(cString: [97,117,116,104,101,110,116,104,105,99,97,116,101,0], encoding: .utf8)!
         picker3 /= Swift.max((Double(Int(picker3 > 147696957.0 || picker3 < -147696957.0 ? 99.0 : picker3) >> (Swift.min(4, labs(3))))), 5)
         auto_zzw = "\((Int(picker3 > 33403357.0 || picker3 < -33403357.0 ? 87.0 : picker3) / 1))"
         exitr = "\((auto_zzw.count ^ Int(namesI > 278382423.0 || namesI < -278382423.0 ? 2.0 : namesI)))"
         r_imageJ = "\(2)"
         break
      }
      while (3.44 < namesI) {
          var resultY: Double = 3.0
         withUnsafeMutablePointer(to: &resultY) { pointer in
                _ = pointer.pointee
         }
          var configB: Bool = false
         celly += Double(1)
         resultY *= (Double(Int(namesI > 266697198.0 || namesI < -266697198.0 ? 47.0 : namesI) + Int(picker3 > 88557581.0 || picker3 < -88557581.0 ? 82.0 : picker3)))
         configB = 61.74 > namesI
         break
      }
         namesI /= Swift.max(1, (Double(1 / (Swift.max(Int(remote9 > 172364008.0 || remote9 < -172364008.0 ? 100.0 : remote9), 5)))))
      if (celly * namesI) >= 1.89 && 1.89 >= (namesI * celly) {
         namesI /= Swift.max(3, (Double(Int(celly > 120222402.0 || celly < -120222402.0 ? 23.0 : celly) << (Swift.min(5, labs(Int(picker3 > 267720287.0 || picker3 < -267720287.0 ? 65.0 : picker3)))))))
      }
      while (2.56 >= (2.60 + celly)) {
          var teamk: [String: Any]! = [String(cString: [115,101,99,117,114,105,116,121,0], encoding: .utf8)!:84, String(cString: [102,111,111,116,101,114,0], encoding: .utf8)!:23]
         withUnsafeMutablePointer(to: &teamk) { pointer in
                _ = pointer.pointee
         }
          var overlapV: String! = String(cString: [97,116,99,104,101,114,0], encoding: .utf8)!
          var gold5: String! = String(cString: [115,101,108,102,105,101,0], encoding: .utf8)!
         celly -= Double(3 ^ teamk.count)
         overlapV.append("\((Int(celly > 304399387.0 || celly < -304399387.0 ? 81.0 : celly) % 1))")
         gold5.append("\((Int(picker3 > 388771589.0 || picker3 < -388771589.0 ? 100.0 : picker3) | Int(remote9 > 152475958.0 || remote9 < -152475958.0 ? 94.0 : remote9)))")
         break
      }
      if 2.17 >= namesI {
         celly += Double(3)
      }
      while (remote9 > picker3) {
          var toolR: Double = 5.0
         withUnsafeMutablePointer(to: &toolR) { pointer in
                _ = pointer.pointee
         }
          var credential_: Int = 2
         picker3 -= (Double(Int(toolR > 202884480.0 || toolR < -202884480.0 ? 56.0 : toolR)))
         credential_ >>= Swift.min(3, labs((Int(picker3 > 150133512.0 || picker3 < -150133512.0 ? 56.0 : picker3))))
         break
      }
      repeat {
         picker3 += Double(3)
         if 788111.0 == picker3 {
            break
         }
      } while ((3.47 - picker3) == 2.13) && (788111.0 == picker3)
         picker3 /= Swift.max(5, (Double(Int(celly > 56811789.0 || celly < -56811789.0 ? 64.0 : celly))))
      if (3.17 + picker3) < 5.77 || (namesI + picker3) < 3.17 {
          var itemo: Int = 4
          var filesO: Float = 0.0
          var remainC: String! = String(cString: [99,104,97,116,0], encoding: .utf8)!
          var remainK: String! = String(cString: [100,101,108,101,116,105,111,110,0], encoding: .utf8)!
          var contentV: [String: Any]! = [String(cString: [99,111,109,98,105,0], encoding: .utf8)!:25, String(cString: [118,105,115,105,98,108,105,116,121,0], encoding: .utf8)!:73, String(cString: [101,110,116,105,116,121,0], encoding: .utf8)!:82]
         namesI /= Swift.max((Double(Int(celly > 290904584.0 || celly < -290904584.0 ? 93.0 : celly) * remainC.count)), 2)
         itemo %= Swift.max(4, (1 % (Swift.max(5, Int(picker3 > 44875922.0 || picker3 < -44875922.0 ? 56.0 : picker3)))))
         filesO -= (Float(Int(namesI > 254460525.0 || namesI < -254460525.0 ? 2.0 : namesI) ^ 1))
         remainK = "\(((String(cString:[104,0], encoding: .utf8)!) == remainK ? Int(namesI > 295208188.0 || namesI < -295208188.0 ? 5.0 : namesI) : remainK.count))"
         contentV["\(remote9)"] = (Int(remote9 > 335687025.0 || remote9 < -335687025.0 ? 49.0 : remote9))
      }
          var posts: Double = 2.0
          var totala: Double = 5.0
         picker3 /= Swift.max(5, (Double(2 & Int(namesI > 62852382.0 || namesI < -62852382.0 ? 50.0 : namesI))))
         posts *= (Double(Int(celly > 282168599.0 || celly < -282168599.0 ? 53.0 : celly) / (Swift.max(Int(namesI > 310877751.0 || namesI < -310877751.0 ? 96.0 : namesI), 9))))
         totala /= Swift.max(2, (Double(Int(picker3 > 245025760.0 || picker3 < -245025760.0 ? 62.0 : picker3))))
      keptT += keptT
      break
   }

        presentingViewController = viewController

        let setup = ASAuthorizationAppleIDProvider()
        let inset = setup.createRequest()
        inset.requestedScopes = [.fullName, .email]

        let previewController = ASAuthorizationController(authorizationRequests: [inset])
        previewController.delegate = self
        previewController.presentationContextProvider = self
        previewController.performRequests()
    }


    private func handleError(_ error: Error) {
       var existing_: String! = String(cString: [108,122,111,120,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &existing_) { pointer in
          _ = pointer.pointee
   }
    var friend_2R: String! = String(cString: [115,103,105,114,108,101,0], encoding: .utf8)!
      existing_.append("\(3)")

      existing_.append("\(existing_.count)")
        let preset = error as NSError
        if preset.domain == ASAuthorizationError.errorDomain,
           preset.code == ASAuthorizationError.canceled.rawValue {
            return
        }
        presentingViewController?.view.makeToast("Apple sign in failed. Please try again.")
   while (friend_2R == String(cString:[76,0], encoding: .utf8)!) {
       var followx: String! = String(cString: [101,120,116,109,97,112,0], encoding: .utf8)!
       var s_centerq: String! = String(cString: [109,112,101,103,117,116,105,108,115,0], encoding: .utf8)!
       _ = s_centerq
          var buttonE: [String: Any]! = [String(cString: [115,116,111,114,101,102,114,97,109,101,0], encoding: .utf8)!:63, String(cString: [101,118,101,114,121,111,110,101,0], encoding: .utf8)!:92]
          var componentp: Double = 3.0
          var horizontalE: Double = 3.0
         followx.append("\(followx.count)")
         buttonE["\(s_centerq)"] = 3 << (Swift.min(5, s_centerq.count))
         componentp += Double(1)
         horizontalE += Double(1 / (Swift.max(6, followx.count)))
          var j_titleD: Double = 2.0
          var profileo: Int = 3
         withUnsafeMutablePointer(to: &profileo) { pointer in
                _ = pointer.pointee
         }
          var cleanedr: Double = 3.0
          _ = cleanedr
         s_centerq = "\(followx.count)"
         j_titleD -= Double(2 * profileo)
         profileo %= Swift.max(1, profileo + 2)
         cleanedr -= Double(profileo / 2)
         s_centerq = "\(s_centerq.count)"
      repeat {
          var clipsz: [Any]! = [100, 49, 29]
          _ = clipsz
          var costR: String! = String(cString: [120,102,97,99,101,0], encoding: .utf8)!
          _ = costR
          var submitj: String! = String(cString: [97,99,115,107,105,112,0], encoding: .utf8)!
          var fittedV: Int = 1
          var date7: String! = String(cString: [101,111,109,101,116,114,121,0], encoding: .utf8)!
         followx = "\(s_centerq.count)"
         clipsz.append(costR.count)
         costR.append("\(1)")
         submitj.append("\(3 | costR.count)")
         fittedV &= (date7 == (String(cString:[74,0], encoding: .utf8)!) ? fittedV : date7.count)
         if 4279819 == followx.count {
            break
         }
      } while (s_centerq == followx) && (4279819 == followx.count)
         followx.append("\(followx.count)")
         followx.append("\(s_centerq.count * 1)")
      friend_2R.append("\(3)")
      break
   }
    }
}

extension DSEditSheet: ASAuthorizationControllerDelegate {


    func authorizationController(
        controller: ASAuthorizationController,
        didCompleteWithAuthorization authorization: ASAuthorization
    ) {
       var items9: String! = String(cString: [99,111,109,98,105,110,101,114,0], encoding: .utf8)!
    var instantV: String! = String(cString: [107,101,121,112,97,116,104,0], encoding: .utf8)!
   repeat {
      instantV.append("\(2 + items9.count)")
      if instantV.count == 4168448 {
         break
      }
   } while (items9 != String(cString:[99,0], encoding: .utf8)!) && (instantV.count == 4168448)
   while (items9 == instantV) {
      instantV.append("\(instantV.count)")
      break
   }

        handleAuthorization(authorization)
    }


    func authorizationController(
        controller: ASAuthorizationController,
        didCompleteWithError error: Error
    ) {
       var rightH: String! = String(cString: [117,110,100,101,102,105,110,101,100,0], encoding: .utf8)!
    var switch_jf: String! = String(cString: [116,104,101,116,97,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
       var roomx: String! = String(cString: [109,97,116,114,111,115,107,97,0], encoding: .utf8)!
       var sectionv: String! = String(cString: [116,105,116,108,116,101,0], encoding: .utf8)!
       var preset8: Float = 0.0
       var cleanedQ: Double = 4.0
      if roomx.count <= (Int(cleanedQ > 156566876.0 || cleanedQ < -156566876.0 ? 100.0 : cleanedQ)) {
         cleanedQ += (Double(Int(preset8 > 23267318.0 || preset8 < -23267318.0 ? 31.0 : preset8)))
      }
         sectionv = "\((1 >> (Swift.min(labs(Int(cleanedQ > 172569723.0 || cleanedQ < -172569723.0 ? 22.0 : cleanedQ)), 2))))"
       var exitv: Int = 5
       var sourceo: Int = 2
       var clipy: String! = String(cString: [115,112,114,111,112,0], encoding: .utf8)!
          var switch_4R: String! = String(cString: [105,110,105,116,105,97,108,105,122,97,116,105,111,110,115,0], encoding: .utf8)!
          _ = switch_4R
         exitv &= sourceo ^ 3
         switch_4R = "\(1 << (Swift.min(1, switch_4R.count)))"
       var homee: String! = String(cString: [99,104,97,110,110,101,108,109,97,112,0], encoding: .utf8)!
         cleanedQ /= Swift.max(4, (Double(Int(preset8 > 24320780.0 || preset8 < -24320780.0 ? 2.0 : preset8))))
         roomx = "\(clipy.count)"
         roomx.append("\(2 + homee.count)")
      if 2.53 < (2.74 - cleanedQ) || (preset8 - 2.74) < 5.48 {
         cleanedQ -= (Double(2 % (Swift.max(Int(preset8 > 254395143.0 || preset8 < -254395143.0 ? 32.0 : preset8), 7))))
      }
      while (clipy != String(cString:[122,0], encoding: .utf8)!) {
         homee = "\(roomx.count | sectionv.count)"
         break
      }
      for _ in 0 ..< 1 {
          var screenW: String! = String(cString: [116,100,108,116,0], encoding: .utf8)!
          var sandboxR: [Any]! = [String(cString: [97,102,116,101,114,0], encoding: .utf8)!, String(cString: [114,101,102,101,114,0], encoding: .utf8)!, String(cString: [108,105,98,115,119,105,102,116,111,115,0], encoding: .utf8)!]
         exitv -= 1
         screenW = "\(3 % (Swift.max(1, exitv)))"
         sandboxR.append(screenW.count / (Swift.max(1, 9)))
      }
      switch_jf.append("\(1 | sectionv.count)")
   }
   if rightH != switch_jf {
      switch_jf = "\(switch_jf.count)"
   }

   repeat {
       var submitg: String! = String(cString: [108,105,115,116,115,0], encoding: .utf8)!
       var j_height0: [String: Any]! = [String(cString: [114,101,102,0], encoding: .utf8)!:[String(cString: [112,114,101,117,112,108,111,97,100,0], encoding: .utf8)!:34, String(cString: [115,116,109,116,0], encoding: .utf8)!:33]]
          var thumbnailU: Double = 4.0
          _ = thumbnailU
          var linef: Double = 2.0
          _ = linef
         j_height0 = ["\(j_height0.count)": 1]
         thumbnailU -= (Double(Int(linef > 235365854.0 || linef < -235365854.0 ? 8.0 : linef)))
          var blueY: Int = 1
          var topI: Bool = false
         withUnsafeMutablePointer(to: &topI) { pointer in
                _ = pointer.pointee
         }
         j_height0["\(topI)"] = j_height0.keys.count
         blueY &= j_height0.keys.count | 3
      rightH.append("\((rightH == (String(cString:[71,0], encoding: .utf8)!) ? rightH.count : submitg.count))")
      if rightH.count == 3407618 {
         break
      }
   } while (rightH.hasSuffix(rightH)) && (rightH.count == 3407618)
        handleError(error)
    }
}

extension DSEditSheet: ASAuthorizationControllerPresentationContextProviding {


    func presentationAnchor(for controller: ASAuthorizationController) -> ASPresentationAnchor {
       var blueB: String! = String(cString: [117,110,100,101,114,0], encoding: .utf8)!
   repeat {
       var empty9: Double = 5.0
       var teamM: String! = String(cString: [118,97,108,105,100,97,116,111,114,115,0], encoding: .utf8)!
       _ = teamM
      if teamM.count <= (Int(empty9 > 200593736.0 || empty9 < -200593736.0 ? 44.0 : empty9)) {
         teamM.append("\(2)")
      }
      while (2 <= (5 << (Swift.min(2, teamM.count))) || (empty9 / (Swift.max(4.28, 10))) <= 4.38) {
         empty9 -= (Double((String(cString:[97,0], encoding: .utf8)!) == teamM ? teamM.count : Int(empty9 > 196048856.0 || empty9 < -196048856.0 ? 89.0 : empty9)))
         break
      }
         teamM.append("\((Int(empty9 > 6045729.0 || empty9 < -6045729.0 ? 58.0 : empty9) ^ 2))")
      repeat {
         empty9 += Double(teamM.count)
         if empty9 == 4117157.0 {
            break
         }
      } while (empty9 == 4117157.0) && (empty9 > Double(teamM.count))
         teamM = "\((Int(empty9 > 96557843.0 || empty9 < -96557843.0 ? 6.0 : empty9)))"
         teamM.append("\(3 + teamM.count)")
      blueB = "\(1 + teamM.count)"
      if 4636053 == blueB.count {
         break
      }
   } while (blueB.count > 4) && (4636053 == blueB.count)

return         presentingViewController?.view.window ?? ASPresentationAnchor()
    }
}
