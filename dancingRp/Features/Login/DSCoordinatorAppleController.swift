
import Foundation

import UIKit

class DSCoordinatorAppleController: DSSecondaryLiveController {

    private let appleSignInCoordinator = DSEditSheet()

    private let backgroundImageView: UIImageView = {
       var cached5: String! = String(cString: [104,105,110,116,115,0], encoding: .utf8)!
    _ = cached5
    var default_phM: Float = 0.0
       var formatterY: [Any]! = [76, 50, 42]
         formatterY = [1]
         formatterY.append(formatterY.count)
       var loadingM: [Any]! = [15, 23]
       _ = loadingM
         loadingM = [loadingM.count % (Swift.max(2, formatterY.count))]
      cached5.append("\((Int(default_phM > 41292997.0 || default_phM < -41292997.0 ? 61.0 : default_phM)))")

        let imageView = UIImageView(image: UIImage(named: "login_welcomeBg"))
      default_phM -= Float(cached5.count / 3)
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        return imageView
    }()

    private lazy var appleSignInButton = makeActionButton(
        title: "Sign in with Apple",
        systemImageName: "apple.logo"
    )

    private lazy var createAccountButton = makeActionButton(title: "Create Account")

    private lazy var signInButton = makeActionButton(title: "Sign in")

    private lazy var actionStackView: UIStackView = {
       var componentsZ: Double = 3.0
   if (componentsZ * componentsZ) == 1.6 || 3.74 == (1.6 * componentsZ) {
      componentsZ -= (Double(Int(componentsZ > 317038312.0 || componentsZ < -317038312.0 ? 92.0 : componentsZ) * 1))
   }

        let follow = UIStackView(arrangedSubviews: [
            appleSignInButton,
            createAccountButton,
            signInButton
        ])
        follow.axis = .vertical
        follow.spacing = 12
        follow.distribution = .fillEqually
        return follow
    }()


    @objc private func didTapCreateAccount() {
       var main_me: [String: Any]! = [String(cString: [102,97,108,115,101,0], encoding: .utf8)!:76, String(cString: [116,114,117,101,109,111,116,105,111,110,114,116,0], encoding: .utf8)!:80]
    var tableW: Int = 1
       var blacklisted_: String! = String(cString: [119,114,105,116,101,120,0], encoding: .utf8)!
       _ = blacklisted_
         blacklisted_.append("\(2)")
       var delegate_iR: String! = String(cString: [114,101,115,112,111,110,100,0], encoding: .utf8)!
       var configz: Bool = true
       var tracks1: Bool = false
       _ = tracks1
         delegate_iR.append("\(delegate_iR.count - blacklisted_.count)")
         configz = ((delegate_iR.count * (!tracks1 ? delegate_iR.count : 40)) > 40)
         tracks1 = delegate_iR.hasSuffix("\(configz)")
      main_me = ["\(main_me.values.count)": main_me.values.count]
   if 2 == (2 * main_me.values.count) && 2 == (main_me.values.count * 2) {
       var permissionsS: String! = String(cString: [111,100,100,97,118,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &permissionsS) { pointer in
             _ = pointer.pointee
      }
       var apple0: [Any]! = [String(cString: [116,105,109,101,115,116,97,109,112,101,100,0], encoding: .utf8)!, String(cString: [106,115,105,109,100,100,99,116,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &apple0) { pointer in
    
      }
       var bundledK: [Any]! = [78, 62, 96]
         apple0.append(apple0.count / 2)
      for _ in 0 ..< 2 {
          var sectionY: String! = String(cString: [114,101,100,117,110,100,97,110,116,0], encoding: .utf8)!
          var identifiero: String! = String(cString: [114,101,109,97,116,114,105,120,105,110,103,0], encoding: .utf8)!
          var celly: Double = 0.0
          var pathsK: String! = String(cString: [105,109,112,111,115,115,105,98,108,101,0], encoding: .utf8)!
         permissionsS = "\(((String(cString:[115,0], encoding: .utf8)!) == permissionsS ? apple0.count : permissionsS.count))"
         sectionY.append("\(1)")
         identifiero.append("\(apple0.count)")
         celly -= Double(identifiero.count << (Swift.min(3, sectionY.count)))
         pathsK = "\(((String(cString:[68,0], encoding: .utf8)!) == sectionY ? identifiero.count : sectionY.count))"
      }
      if 5 <= (permissionsS.count - bundledK.count) {
         permissionsS = "\(apple0.count)"
      }
          var delegate_7ax: String! = String(cString: [115,104,105,109,109,101,114,105,110,103,0], encoding: .utf8)!
          var applea: String! = String(cString: [115,99,114,101,101,110,112,114,101,115,115,111,0], encoding: .utf8)!
         bundledK.append(applea.count & 3)
         delegate_7ax = "\(permissionsS.count | 2)"
      while ((bundledK.count / (Swift.max(1, 2))) == 3 && (1 / (Swift.max(4, bundledK.count))) == 1) {
          var urlZ: String! = String(cString: [105,109,112,111,115,116,101,114,0], encoding: .utf8)!
         bundledK.append(1 ^ urlZ.count)
         break
      }
      tableW |= tableW
   }

        navigationController?.pushViewController(DSCenterSetupController(mode: .register), animated: true)
    }


    private func makeActionButton(title: String, systemImageName: String? = nil) -> UIButton {
       var random4: Float = 4.0
    _ = random4
       var fillersR: Bool = false
       var segmentz: Double = 1.0
      if 5.93 <= (segmentz * 4.99) && 4.99 <= segmentz {
         fillersR = !fillersR
      }
       var durationb: Bool = false
       var subject6: Bool = true
      while (subject6) {
          var rede: String! = String(cString: [111,119,110,101,114,115,0], encoding: .utf8)!
         segmentz /= Swift.max(5, Double(3))
         rede = "\(((durationb ? 2 : 2)))"
         break
      }
      while (!durationb) {
         segmentz += (Double(1 % (Swift.max(4, Int(segmentz > 100503270.0 || segmentz < -100503270.0 ? 20.0 : segmentz)))))
         break
      }
         segmentz -= Double(1)
      while (durationb && 4.21 < (segmentz - 5.78)) {
         segmentz += (Double((subject6 ? 3 : 1) >> (Swift.min(3, labs(3)))))
         break
      }
      random4 /= Swift.max((Float((fillersR ? 3 : 3) << (Swift.min(labs(Int(segmentz > 302428186.0 || segmentz < -302428186.0 ? 77.0 : segmentz)), 1)))), 4)

        let dimmingButton = UIButton(type: .custom)

        var tab = UIButton.Configuration.filled()
        tab.baseBackgroundColor = .white
        tab.baseForegroundColor = .black
        tab.cornerStyle = .capsule
        tab.title = title
        tab.titleTextAttributesTransformer = UIConfigurationTextAttributesTransformer { incoming in
            var layout = incoming
            layout.font = .systemFont(ofSize: 17, weight: .semibold)
            return layout
        }

        if let systemImageName {
            let last = UIImage.SymbolConfiguration(pointSize: 18, weight: .medium)
            tab.image = UIImage(systemName: systemImageName, withConfiguration: last)
            tab.imagePadding = 8
            tab.imagePlacement = .leading
        }

        dimmingButton.configuration = tab
        return dimmingButton
    }


    @objc private func didTapSignIn() {
       var loggedK: String! = String(cString: [105,109,97,103,101,121,117,118,99,111,110,102,105,103,0], encoding: .utf8)!
       var targetr: String! = String(cString: [109,111,116,105,111,110,115,101,97,114,99,104,0], encoding: .utf8)!
         targetr.append("\(targetr.count)")
      while (targetr != String(cString:[107,0], encoding: .utf8)!) {
         targetr.append("\(targetr.count >> (Swift.min(labs(2), 2)))")
         break
      }
       var lasto: String! = String(cString: [115,101,114,105,97,108,105,115,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &lasto) { pointer in
    
      }
       var removedR: String! = String(cString: [97,110,97,103,108,121,112,104,0], encoding: .utf8)!
       _ = removedR
         lasto = "\(targetr.count & 2)"
         removedR.append("\(targetr.count)")
      loggedK = "\(3)"

        navigationController?.pushViewController(DSCenterSetupController(mode: .login), animated: true)
    }


    private func setupUI() {
       var confirmo: [String: Any]! = [String(cString: [98,97,99,107,0], encoding: .utf8)!:String(cString: [110,101,116,115,0], encoding: .utf8)!, String(cString: [113,117,97,100,115,0], encoding: .utf8)!:String(cString: [97,112,112,108,101,0], encoding: .utf8)!]
    _ = confirmo
    var peerF: String! = String(cString: [112,114,101,115,101,110,116,101,100,0], encoding: .utf8)!
      confirmo = ["\(confirmo.values.count)": 3 + peerF.count]

       var configurationD: String! = String(cString: [117,110,115,105,103,110,101,100,0], encoding: .utf8)!
       var footerl: Bool = true
       _ = footerl
         footerl = (((!footerl ? 78 : configurationD.count) - configurationD.count) <= 78)
         footerl = configurationD.hasPrefix("\(footerl)")
         footerl = !footerl
      while (footerl) {
         footerl = (55 == (configurationD.count & (footerl ? configurationD.count : 55)))
         break
      }
       var q_viewY: Double = 2.0
         q_viewY -= (Double((footerl ? 5 : 4) + Int(q_viewY > 146278935.0 || q_viewY < -146278935.0 ? 76.0 : q_viewY)))
      peerF.append("\(confirmo.values.count)")
        view.backgroundColor = UIColor(red: 0.35, green: 0.22, blue: 0.75, alpha: 1)

        view.addSubview(backgroundImageView)
        view.addSubview(actionStackView)

        backgroundImageView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        actionStackView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(32)
            make.bottom.equalTo(view.safeAreaLayoutGuide).inset(48)
        }

        [appleSignInButton, createAccountButton, signInButton].forEach { button in
            button.snp.makeConstraints { make in
                make.height.equalTo(52)
            }
        }
    }


    private func setupActions() {
       var readyU: Int = 5
    _ = readyU
      readyU &= readyU - 1

        appleSignInButton.addTarget(self, action: #selector(didTapAppleSignIn), for: .touchUpInside)
        createAccountButton.addTarget(self, action: #selector(didTapCreateAccount), for: .touchUpInside)
        signInButton.addTarget(self, action: #selector(didTapSignIn), for: .touchUpInside)
    }


    @objc private func didTapAppleSignIn() {
       var cost5: String! = String(cString: [111,110,103,111,105,110,103,0], encoding: .utf8)!
    var maskp: Float = 0.0
      cost5 = "\(cost5.count)"
      maskp -= Float(1)

        appleSignInCoordinator.startSignIn(from: self)
    }


    override func viewDidLoad() {
       var color5: String! = String(cString: [111,114,105,103,105,110,115,0], encoding: .utf8)!
   while (color5.hasSuffix(color5)) {
      color5.append("\(color5.count + 2)")
      break
   }

        super.viewDidLoad()
        setupUI()
        setupActions()
    }
}
