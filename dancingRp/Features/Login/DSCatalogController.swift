
import Foundation

import UIKit

class DSCatalogController: DSSecondaryLiveController {

    private enum Layout {
        static let backgroundImageNames = ["guide_1", "guide_2", "guide_3"]
        static let nextButtonWidth: CGFloat = 267
        static let nextButtonHeight: CGFloat = 64
    }

    private var currentPageIndex = 0

    private let backgroundImageView: UIImageView = {
       var bubble9: Double = 3.0
    var romm7: Int = 3
   if 5 <= (5 - romm7) && 2 <= (5 & romm7) {
      romm7 -= romm7 - 3
   }

        let imageView = UIImageView()
       var valueH: String! = String(cString: [98,101,103,105,110,0], encoding: .utf8)!
       _ = valueH
       var toggleN: String! = String(cString: [117,114,97,110,100,111,109,0], encoding: .utf8)!
       var pushn: String! = String(cString: [109,117,108,116,105,100,101,115,99,114,105,112,116,111,114,0], encoding: .utf8)!
         toggleN = "\(pushn.count ^ valueH.count)"
      while (pushn.count > 3) {
         pushn = "\(valueH.count / 1)"
         break
      }
      while (pushn != toggleN) {
          var device1: Double = 3.0
          var controlv: String! = String(cString: [99,114,111,108,108,0], encoding: .utf8)!
          var media8: Double = 4.0
          var topX: [Any]! = [36.0]
          var networkl: [String: Any]! = [String(cString: [114,101,115,97,109,112,108,101,107,104,122,0], encoding: .utf8)!:[String(cString: [115,108,101,101,112,0], encoding: .utf8)!]]
         withUnsafeMutablePointer(to: &networkl) { pointer in
                _ = pointer.pointee
         }
         toggleN = "\((3 * Int(media8 > 115515928.0 || media8 < -115515928.0 ? 92.0 : media8)))"
         device1 /= Swift.max(Double(valueH.count), 1)
         controlv.append("\(toggleN.count)")
         topX.append(topX.count & valueH.count)
         networkl[toggleN] = valueH.count
         break
      }
      repeat {
         pushn.append("\(valueH.count / (Swift.max(toggleN.count, 4)))")
         if pushn == (String(cString:[109,112,48,119,0], encoding: .utf8)!) {
            break
         }
      } while (pushn != String(cString:[84,0], encoding: .utf8)! || 3 == toggleN.count) && (pushn == (String(cString:[109,112,48,119,0], encoding: .utf8)!))
      for _ in 0 ..< 2 {
         pushn = "\(1 & pushn.count)"
      }
      bubble9 += (Double(Int(bubble9 > 48291419.0 || bubble9 < -48291419.0 ? 22.0 : bubble9)))
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        return imageView
    }()

    private lazy var nextButton: UIButton = {
       var audioK: Float = 0.0
   withUnsafeMutablePointer(to: &audioK) { pointer in
    
   }
   repeat {
      audioK /= Swift.max(3, (Float(Int(audioK > 165057604.0 || audioK < -165057604.0 ? 60.0 : audioK))))
      if 4049172.0 == audioK {
         break
      }
   } while (4049172.0 == audioK) && (audioK == audioK)

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setBackgroundImage(UIImage(named: "login_next"), for: .normal)
        dimmingButton.adjustsImageWhenHighlighted = false
        dimmingButton.accessibilityLabel = "Next"
        dimmingButton.addTarget(self, action: #selector(didTapNext), for: .touchUpInside)
        return dimmingButton
    }()

    override func viewDidLoad() {
       var stored5: String! = String(cString: [111,112,116,105,111,110,97,108,108,121,0], encoding: .utf8)!
    var switch_hsv: String! = String(cString: [116,101,108,101,112,104,111,116,111,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      switch_hsv.append("\(3)")
   }

   repeat {
      stored5.append("\(3)")
      if stored5.count == 1033288 {
         break
      }
   } while (stored5.count == 1033288) && (switch_hsv.contains("\(stored5.count)"))
        super.viewDidLoad()
        setupUI()
        showPage(at: currentPageIndex, animated: false)
    }

    private func setupUI() {
       var httpl: String! = String(cString: [117,110,101,109,98,101,100,0], encoding: .utf8)!
   if !httpl.contains("\(httpl.count)") {
       var endw: Double = 2.0
       _ = endw
       var h_heightq: String! = String(cString: [102,100,99,116,120,0], encoding: .utf8)!
       var hoursz: String! = String(cString: [103,101,111,99,111,100,101,114,0], encoding: .utf8)!
         h_heightq.append("\(((String(cString:[55,0], encoding: .utf8)!) == hoursz ? hoursz.count : h_heightq.count))")
          var spacingh: [Any]! = [46, 31, 9]
         withUnsafeMutablePointer(to: &spacingh) { pointer in
                _ = pointer.pointee
         }
          var bordert: [Any]! = [81, 59]
         h_heightq.append("\(1 ^ bordert.count)")
         spacingh = [(hoursz == (String(cString:[105,0], encoding: .utf8)!) ? spacingh.count : hoursz.count)]
         hoursz = "\(hoursz.count)"
         endw /= Swift.max(Double(hoursz.count), 5)
          var page6: [String: Any]! = [String(cString: [111,118,101,114,114,105,100,101,115,0], encoding: .utf8)!:String(cString: [112,117,116,110,117,109,112,97,115,115,101,115,0], encoding: .utf8)!, String(cString: [105,110,118,105,116,101,114,0], encoding: .utf8)!:String(cString: [114,105,112,101,109,100,0], encoding: .utf8)!]
          var lcopy_ea: String! = String(cString: [99,97,114,116,0], encoding: .utf8)!
         h_heightq.append("\(lcopy_ea.count << (Swift.min(4, page6.values.count)))")
      for _ in 0 ..< 1 {
         h_heightq = "\((Int(endw > 95416712.0 || endw < -95416712.0 ? 45.0 : endw)))"
      }
         endw += (Double(h_heightq == (String(cString:[89,0], encoding: .utf8)!) ? Int(endw > 230102184.0 || endw < -230102184.0 ? 72.0 : endw) : h_heightq.count))
         h_heightq.append("\(hoursz.count << (Swift.min(labs(1), 4)))")
         h_heightq.append("\(hoursz.count)")
      httpl = "\(1)"
   }

        view.backgroundColor = .black

        view.addSubview(backgroundImageView)
        view.addSubview(nextButton)

        backgroundImageView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        nextButton.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.bottom.equalTo(view.safeAreaLayoutGuide).inset(24)
            make.width.equalTo(Layout.nextButtonWidth)
            make.height.equalTo(Layout.nextButtonHeight)
        }
    }

    private func showPage(at index: Int, animated: Bool) {
       var nameD: Double = 1.0
    var resolved4: [Any]! = [1, 65]
       var accentM: Int = 3
      while ((5 & accentM) > 4 || (5 & accentM) > 1) {
         accentM += accentM % 1
         break
      }
      if (accentM * accentM) > 5 {
         accentM ^= accentM + accentM
      }
          var directj: String! = String(cString: [100,101,116,101,99,116,99,108,111,115,101,0], encoding: .utf8)!
          var buttonv: [String: Any]! = [String(cString: [98,108,111,119,102,105,115,104,0], encoding: .utf8)!:33, String(cString: [116,104,101,114,101,117,109,0], encoding: .utf8)!:53]
          var currentw: [String: Any]! = [String(cString: [98,117,114,115,116,0], encoding: .utf8)!:44, String(cString: [116,104,114,101,115,104,0], encoding: .utf8)!:38, String(cString: [114,101,108,101,97,115,101,100,0], encoding: .utf8)!:34]
         accentM /= Swift.max(accentM % 2, 5)
         directj.append("\(accentM)")
         buttonv = ["\(accentM)": ((String(cString:[90,0], encoding: .utf8)!) == directj ? accentM : directj.count)]
         currentw = [directj: (directj == (String(cString:[115,0], encoding: .utf8)!) ? accentM : directj.count)]
      resolved4 = [(accentM >> (Swift.min(labs(Int(nameD > 96187569.0 || nameD < -96187569.0 ? 25.0 : nameD)), 3)))]

        guard Layout.backgroundImageNames.indices.contains(index) else { return }

       var createl: Int = 5
       var loadingC: String! = String(cString: [103,105,103,97,103,114,111,117,112,0], encoding: .utf8)!
          var directoryw: [String: Any]! = [String(cString: [109,105,110,105,109,97,0], encoding: .utf8)!:String(cString: [97,117,116,111,102,111,114,109,97,116,116,105,110,103,0], encoding: .utf8)!, String(cString: [116,114,97,110,115,102,111,114,109,101,114,0], encoding: .utf8)!:String(cString: [114,101,115,111,117,114,99,101,0], encoding: .utf8)!, String(cString: [102,97,99,116,111,114,105,122,97,116,105,111,110,0], encoding: .utf8)!:String(cString: [112,101,114,105,111,100,105,99,0], encoding: .utf8)!]
         loadingC.append("\(((String(cString:[115,0], encoding: .utf8)!) == loadingC ? loadingC.count : directoryw.count))")
          var endJ: Double = 0.0
          var configuredI: String! = String(cString: [98,108,97,99,107,115,0], encoding: .utf8)!
          _ = configuredI
          var spacingw: Float = 4.0
         withUnsafeMutablePointer(to: &spacingw) { pointer in
                _ = pointer.pointee
         }
         loadingC = "\((Int(endJ > 130166131.0 || endJ < -130166131.0 ? 38.0 : endJ)))"
         configuredI.append("\(1 >> (Swift.min(2, loadingC.count)))")
         spacingw += (Float(3 ^ Int(endJ > 171371977.0 || endJ < -171371977.0 ? 14.0 : endJ)))
          var rightJ: String! = String(cString: [114,101,110,100,101,114,101,100,0], encoding: .utf8)!
          var coordinator4: Double = 1.0
         withUnsafeMutablePointer(to: &coordinator4) { pointer in
                _ = pointer.pointee
         }
         createl ^= (Int(coordinator4 > 363133613.0 || coordinator4 < -363133613.0 ? 88.0 : coordinator4) % (Swift.max(10, rightJ.count)))
         loadingC.append("\(2 << (Swift.min(2, loadingC.count)))")
       var previousl: String! = String(cString: [100,105,118,105,100,105,110,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &previousl) { pointer in
             _ = pointer.pointee
      }
       var cover6: String! = String(cString: [107,108,97,121,116,110,0], encoding: .utf8)!
      while (createl <= 2) {
          var line1: Bool = false
         withUnsafeMutablePointer(to: &line1) { pointer in
    
         }
          var blur0: [Any]! = [95]
          var profileC: Double = 0.0
         createl |= ((String(cString:[117,0], encoding: .utf8)!) == loadingC ? loadingC.count : createl)
         line1 = 72 > cover6.count
         blur0.append(createl | cover6.count)
         profileC += Double(cover6.count << (Swift.min(3, labs(createl))))
         break
      }
         previousl.append("\(1 - cover6.count)")
      resolved4 = [loadingC.count | 3]
        let greenImage = UIImage(named: Layout.backgroundImageNames[index])
        guard animated else {
            backgroundImageView.image = greenImage
      nameD += Double(1)
            return
        }

        UIView.transition(
            with: backgroundImageView,
            duration: 0.25,
            options: .transitionCrossDissolve
        ) {
            self.backgroundImageView.image = greenImage
        }
    }

    @objc private func didTapNext() {
       var type_9gu: String! = String(cString: [105,110,116,114,101,97,100,119,114,105,116,101,0], encoding: .utf8)!
   repeat {
       var commentL: Int = 5
       var spacingu: Bool = true
       var normalB: [Any]! = [String(cString: [99,97,110,99,101,108,101,100,0], encoding: .utf8)!]
       var thumbnailj: String! = String(cString: [104,105,103,104,112,97,115,115,0], encoding: .utf8)!
       var page_: Int = 0
         thumbnailj = "\(3)"
      if 4 >= (1 - thumbnailj.count) {
         thumbnailj = "\(thumbnailj.count)"
      }
          var posts0: [Any]! = [String(cString: [109,101,116,114,105,99,115,0], encoding: .utf8)!, String(cString: [105,110,118,111,107,101,0], encoding: .utf8)!]
          var symbolc: String! = String(cString: [112,101,114,105,111,100,115,0], encoding: .utf8)!
         spacingu = (98 > (normalB.count - (spacingu ? 98 : normalB.count)))
         posts0.append(1 << (Swift.min(5, symbolc.count)))
         symbolc.append("\(posts0.count / (Swift.max(3, 2)))")
      repeat {
         page_ %= Swift.max(page_ >> (Swift.min(thumbnailj.count, 2)), 5)
         if page_ == 2916241 {
            break
         }
      } while (3 <= commentL) && (page_ == 2916241)
          var remoter: String! = String(cString: [99,104,97,110,103,105,110,103,0], encoding: .utf8)!
          _ = remoter
          var bar6: Double = 0.0
          _ = bar6
         normalB = [thumbnailj.count & commentL]
         remoter.append("\((Int(bar6 > 87912091.0 || bar6 < -87912091.0 ? 45.0 : bar6) + 2))")
         bar6 += Double(normalB.count)
      while (4 < (normalB.count ^ 1) || !spacingu) {
         spacingu = 69 <= thumbnailj.count
         break
      }
         commentL %= Swift.max(1, commentL)
      for _ in 0 ..< 2 {
         normalB = [page_]
      }
         thumbnailj.append("\(((String(cString:[74,0], encoding: .utf8)!) == thumbnailj ? commentL : thumbnailj.count))")
         normalB.append(commentL + 2)
          var blur6: Bool = true
         normalB.append(normalB.count ^ 1)
         blur6 = spacingu
      while (1 < (normalB.count >> (Swift.min(labs(5), 3))) || (normalB.count >> (Swift.min(labs(5), 3))) < 1) {
         page_ <<= Swift.min(labs(thumbnailj.count - page_), 2)
         break
      }
      while ((normalB.count / (Swift.max(3, 10))) > 5) {
          var ids7: String! = String(cString: [119,104,105,108,101,0], encoding: .utf8)!
          _ = ids7
          var resolvedV: [String: Any]! = [String(cString: [115,107,101,121,108,105,115,116,0], encoding: .utf8)!:55, String(cString: [118,100,112,97,117,99,111,110,116,101,120,116,0], encoding: .utf8)!:86, String(cString: [108,111,116,116,105,101,118,105,101,119,0], encoding: .utf8)!:48]
         withUnsafeMutablePointer(to: &resolvedV) { pointer in
    
         }
         page_ -= ((spacingu ? 4 : 4) * ids7.count)
         resolvedV = [ids7: ids7.count]
         break
      }
      for _ in 0 ..< 2 {
          var messagesV: String! = String(cString: [107,102,109,111,100,101,115,0], encoding: .utf8)!
          var semaphoreB: String! = String(cString: [100,97,116,97,99,101,110,116,101,114,0], encoding: .utf8)!
         page_ &= commentL | 3
         messagesV.append("\((normalB.count * (spacingu ? 2 : 2)))")
         semaphoreB = "\(3)"
      }
      repeat {
         spacingu = 73 == page_
         if spacingu ? !spacingu : spacingu {
            break
         }
      } while (spacingu) && (spacingu ? !spacingu : spacingu)
      type_9gu.append("\(2 >> (Swift.min(2, type_9gu.count)))")
      if type_9gu == (String(cString:[110,110,122,97,48,48,51,116,121,102,0], encoding: .utf8)!) {
         break
      }
   } while (type_9gu != String(cString:[85,0], encoding: .utf8)!) && (type_9gu == (String(cString:[110,110,122,97,48,48,51,116,121,102,0], encoding: .utf8)!))

        if currentPageIndex < Layout.backgroundImageNames.count - 1 {
            currentPageIndex += 1
            showPage(at: currentPageIndex, animated: true)
            return
        }
        finishGuide()
    }

    private func finishGuide() {
       var normalP: String! = String(cString: [112,97,114,0], encoding: .utf8)!
    var reusabley: [Any]! = [28, 27]
   if 1 >= normalP.count {
      reusabley = [normalP.count]
   }
      reusabley = [(normalP == (String(cString:[71,0], encoding: .utf8)!) ? reusabley.count : normalP.count)]

        DSRoomReviseController.switchToMainInterface()
    }
}
