
import Foundation

import UIKit

class DSEditController: DSEmptyController {

    private enum Layout {
        static let backButtonSize: CGFloat = 44
        static let topInset: CGFloat = 8
        static let bottomInset: CGFloat = 24
        static let horizontalInset: CGFloat = 16
        static let avatarSize: CGFloat = 40
        static let bottomGradientHeight: CGFloat = 200
    }

    private let post: DSWelcomeCurrent

    private lazy var backButton: UIButton = {
       var inputJ: Bool = true
   while (inputJ) {
      inputJ = (inputJ ? inputJ : !inputJ)
      break
   }

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setImage(UIImage(named: "common_back"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapBack), for: .touchUpInside)
        return dimmingButton
    }()

    private let mediaImageView: UIImageView = {
       var partsc: Int = 5
    var time_pbS: String! = String(cString: [113,112,98,105,116,115,0], encoding: .utf8)!
       var suggestedF: Float = 5.0
       var primarye: [String: Any]! = [String(cString: [112,114,111,98,115,0], encoding: .utf8)!:[String(cString: [97,100,115,0], encoding: .utf8)!:47]]
      while (1 == primarye.count) {
          var names4: Float = 4.0
          var completionJ: String! = String(cString: [105,110,116,101,114,120,121,0], encoding: .utf8)!
         primarye = ["\(suggestedF)": completionJ.count]
         names4 -= Float(2 << (Swift.min(5, completionJ.count)))
         break
      }
      repeat {
          var scriptJ: Double = 0.0
          var presentingY: String! = String(cString: [116,104,114,101,97,100,112,111,111,108,0], encoding: .utf8)!
          _ = presentingY
          var backC: [String: Any]! = [String(cString: [115,117,98,106,101,99,116,115,0], encoding: .utf8)!:54, String(cString: [104,97,108,108,0], encoding: .utf8)!:18, String(cString: [115,121,110,99,104,114,111,110,111,117,115,0], encoding: .utf8)!:100]
          var loaderw: Float = 4.0
          var selectedg: String! = String(cString: [100,101,102,114,97,103,109,101,110,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &selectedg) { pointer in
    
         }
         suggestedF -= Float(3)
         scriptJ -= Double(2)
         presentingY.append("\(((String(cString:[86,0], encoding: .utf8)!) == presentingY ? backC.count : presentingY.count))")
         backC["\(suggestedF)"] = (Int(suggestedF > 205136343.0 || suggestedF < -205136343.0 ? 3.0 : suggestedF))
         loaderw -= (Float(primarye.values.count - Int(scriptJ > 301312842.0 || scriptJ < -301312842.0 ? 83.0 : scriptJ)))
         selectedg = "\((Int(scriptJ > 44941802.0 || scriptJ < -44941802.0 ? 18.0 : scriptJ) * primarye.keys.count))"
         if 3153043.0 == suggestedF {
            break
         }
      } while (3153043.0 == suggestedF) && (3.25 >= suggestedF)
         suggestedF -= (Float(Int(suggestedF > 311293847.0 || suggestedF < -311293847.0 ? 67.0 : suggestedF) / 1))
         primarye["\(suggestedF)"] = (primarye.count << (Swift.min(1, labs(Int(suggestedF > 60671160.0 || suggestedF < -60671160.0 ? 30.0 : suggestedF)))))
          var backgroundI: Bool = false
          var sectiont: String! = String(cString: [103,101,115,116,117,114,101,115,0], encoding: .utf8)!
          var mergedK: String! = String(cString: [110,111,116,103,101,116,0], encoding: .utf8)!
          _ = mergedK
         suggestedF /= Swift.max(1, Float(primarye.count ^ 1))
         sectiont = "\(1 - mergedK.count)"
         mergedK = "\((mergedK == (String(cString:[67,0], encoding: .utf8)!) ? (backgroundI ? 2 : 5) : mergedK.count))"
      for _ in 0 ..< 3 {
          var request2: Float = 5.0
         withUnsafeMutablePointer(to: &request2) { pointer in
    
         }
          var modeg: String! = String(cString: [105,112,97,100,100,0], encoding: .utf8)!
          var seedg: Double = 4.0
         withUnsafeMutablePointer(to: &seedg) { pointer in
    
         }
          var delegate_45: [Any]! = [45, 40, 96]
          _ = delegate_45
          var blur2: Float = 4.0
         primarye = ["\(primarye.count)": 3 ^ primarye.keys.count]
         request2 *= (Float(Int(seedg > 42779819.0 || seedg < -42779819.0 ? 23.0 : seedg)))
         modeg = "\(modeg.count)"
         seedg += Double(primarye.count * 3)
         delegate_45 = [primarye.count]
         blur2 *= Float(2 ^ modeg.count)
      }
      time_pbS = "\(time_pbS.count / 2)"

        let imageView = UIImageView()
   while ((time_pbS.count - 4) < 4 && (time_pbS.count - 4) < 3) {
      time_pbS.append("\(partsc >> (Swift.min(time_pbS.count, 4)))")
      break
   }
        imageView.contentMode = .scaleAspectFill
   for _ in 0 ..< 1 {
      partsc -= 3
   }
        imageView.clipsToBounds = true
        imageView.backgroundColor = .black
        return imageView
    }()

    private let bottomGradientView: UIView = {
       var candidateT: String! = String(cString: [99,104,114,111,109,97,107,101,121,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &candidateT) { pointer in
          _ = pointer.pointee
   }
    var sharedG: String! = String(cString: [103,108,97,115,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &sharedG) { pointer in
    
   }
      sharedG.append("\(sharedG.count ^ 2)")

        let view = UIView()
   if 1 == candidateT.count {
       var taskn: Float = 2.0
      withUnsafeMutablePointer(to: &taskn) { pointer in
             _ = pointer.pointee
      }
         taskn /= Swift.max((Float(Int(taskn > 170463712.0 || taskn < -170463712.0 ? 17.0 : taskn) * 3)), 1)
          var formatterO: Double = 1.0
          var r_titleU: Double = 5.0
          _ = r_titleU
         taskn -= Float(1)
         formatterO -= (Double(Int(taskn > 160021450.0 || taskn < -160021450.0 ? 30.0 : taskn)))
         r_titleU -= Double(3)
      repeat {
         taskn -= (Float(1 & Int(taskn > 86958187.0 || taskn < -86958187.0 ? 98.0 : taskn)))
         if 126020.0 == taskn {
            break
         }
      } while ((taskn - taskn) == 5.62 || (taskn - 5.62) == 4.2) && (126020.0 == taskn)
      sharedG.append("\((Int(taskn > 93533107.0 || taskn < -93533107.0 ? 16.0 : taskn)))")
   }
        view.isUserInteractionEnabled = false
   repeat {
      candidateT.append("\(candidateT.count + candidateT.count)")
      if candidateT == (String(cString:[120,108,110,56,119,50,49,0], encoding: .utf8)!) {
         break
      }
   } while (candidateT == candidateT) && (candidateT == (String(cString:[120,108,110,56,119,50,49,0], encoding: .utf8)!))
        return view
    }()

    private let avatarImageView: UIImageView = {
       var safeU: Int = 0
   while (safeU <= 1) {
       var footerl: String! = String(cString: [115,97,116,105,115,102,105,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &footerl) { pointer in
             _ = pointer.pointee
      }
       var urls9: String! = String(cString: [98,97,110,100,115,0], encoding: .utf8)!
       _ = urls9
       var messagest: Bool = true
      while (messagest && footerl.count > 5) {
         footerl.append("\(footerl.count >> (Swift.min(urls9.count, 1)))")
         break
      }
       var symbolt: Float = 2.0
       var mergedk: Float = 2.0
      repeat {
         urls9 = "\((Int(mergedk > 315083739.0 || mergedk < -315083739.0 ? 65.0 : mergedk) % 3))"
         if urls9 == (String(cString:[121,108,114,110,53,0], encoding: .utf8)!) {
            break
         }
      } while (urls9 == (String(cString:[121,108,114,110,53,0], encoding: .utf8)!)) && (footerl.count == urls9.count)
      for _ in 0 ..< 1 {
          var footerC: [String: Any]! = [String(cString: [110,105,103,104,116,115,104,111,116,0], encoding: .utf8)!:59, String(cString: [97,108,103,115,0], encoding: .utf8)!:28]
          _ = footerC
         messagest = !messagest
         footerC[urls9] = (2 << (Swift.min(2, labs(Int(symbolt > 241282789.0 || symbolt < -241282789.0 ? 5.0 : symbolt)))))
      }
      repeat {
         urls9 = "\(((String(cString:[81,0], encoding: .utf8)!) == urls9 ? (messagest ? 3 : 1) : urls9.count))"
         if 4121316 == urls9.count {
            break
         }
      } while (urls9.hasPrefix("\(messagest)")) && (4121316 == urls9.count)
      for _ in 0 ..< 1 {
         messagest = !messagest
      }
       var indicesg: Double = 1.0
       _ = indicesg
       var visibilityf: Double = 3.0
       var post6: String! = String(cString: [98,117,102,114,101,102,0], encoding: .utf8)!
       _ = post6
      for _ in 0 ..< 1 {
          var editz: Bool = false
          var linesI: String! = String(cString: [116,101,115,116,98,105,116,0], encoding: .utf8)!
          var tabbarU: String! = String(cString: [100,117,114,103,101,114,107,105,110,103,0], encoding: .utf8)!
          var allk: Double = 1.0
         visibilityf += Double(3 & footerl.count)
         editz = linesI.count == 55
         linesI = "\(post6.count)"
         tabbarU.append("\(footerl.count)")
         allk += (Double((editz ? 1 : 4) / (Swift.max(Int(symbolt > 20629477.0 || symbolt < -20629477.0 ? 51.0 : symbolt), 1))))
      }
         indicesg -= Double(footerl.count % (Swift.max(3, 9)))
      safeU %= Swift.max(3, (urls9 == (String(cString:[103,0], encoding: .utf8)!) ? safeU : urls9.count))
      break
   }

        let imageView = UIImageView()
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        imageView.backgroundColor = UIColor.hex("#444444")
        imageView.layer.cornerRadius = Layout.avatarSize / 2
        imageView.layer.borderColor = UIColor.white.cgColor
        imageView.layer.borderWidth = 1
        return imageView
    }()

    private let userNameLabel: UILabel = {
       var subdirectoryI: String! = String(cString: [109,107,118,109,117,120,101,114,0], encoding: .utf8)!
       var fulla: String! = String(cString: [115,119,114,105,0], encoding: .utf8)!
       _ = fulla
       var indicesL: String! = String(cString: [115,116,114,108,99,97,116,0], encoding: .utf8)!
          var radius2: Int = 2
         indicesL = "\((indicesL == (String(cString:[120,0], encoding: .utf8)!) ? indicesL.count : radius2))"
      while (indicesL != fulla) {
         fulla = "\(indicesL.count + 3)"
         break
      }
      repeat {
         indicesL.append("\(1)")
         if (String(cString:[108,103,48,102,53,49,0], encoding: .utf8)!) == indicesL {
            break
         }
      } while (fulla == String(cString:[118,0], encoding: .utf8)!) && ((String(cString:[108,103,48,102,53,49,0], encoding: .utf8)!) == indicesL)
         fulla = "\(fulla.count)"
         indicesL.append("\(fulla.count)")
         fulla.append("\(fulla.count - indicesL.count)")
      subdirectoryI = "\(fulla.count - 3)"

        let label = UILabel()
        label.textColor = .white
        label.font = .systemFont(ofSize: 16, weight: .semibold)
        return label
    }()

    private let contentLabel: UILabel = {
       var preview2: Int = 2
       var parameters6: Int = 5
       var audio8: String! = String(cString: [99,114,101,97,116,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &audio8) { pointer in
    
      }
         audio8 = "\(1)"
      for _ in 0 ..< 2 {
         parameters6 -= parameters6 >> (Swift.min(labs(2), 4))
      }
       var endv: Float = 3.0
         audio8 = "\(((String(cString:[55,0], encoding: .utf8)!) == audio8 ? audio8.count : parameters6))"
      if (audio8.count * 2) >= 1 {
         audio8.append("\(3 + audio8.count)")
      }
      while (Int(endv) >= parameters6) {
         parameters6 >>= Swift.min(labs((audio8.count & Int(endv > 288818184.0 || endv < -288818184.0 ? 53.0 : endv))), 4)
         break
      }
      preview2 *= preview2 + 1

        let label = UILabel()
        label.textColor = .white
        label.font = .systemFont(ofSize: 14, weight: .regular)
        label.numberOfLines = 0
        return label
    }()

    init(post: DSWelcomeCurrent) {
        self.post = post
        super.init(nibName: nil, bundle: nil)
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    override func viewDidLoad() {
       var identifier1: String! = String(cString: [97,115,105,110,107,0], encoding: .utf8)!
   while (identifier1.count >= 2 && identifier1 != String(cString:[53,0], encoding: .utf8)!) {
      identifier1.append("\(identifier1.count % (Swift.max(identifier1.count, 7)))")
      break
   }

        super.viewDidLoad()
        setupUI()
        bindPost()
    }

    override func viewDidLayoutSubviews() {
       var rowi: Double = 0.0
    var completionp: Double = 2.0
   if (rowi - completionp) <= 1.4 {
       var blacklistedN: String! = String(cString: [100,101,99,97,121,0], encoding: .utf8)!
       _ = blacklistedN
       var editW: String! = String(cString: [114,101,99,101,105,118,101,0], encoding: .utf8)!
       var encodedp: String! = String(cString: [111,98,106,110,105,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &encodedp) { pointer in
    
      }
       var accentR: Float = 3.0
       var symbolS: String! = String(cString: [111,112,116,105,109,105,115,109,0], encoding: .utf8)!
      repeat {
         encodedp = "\(editW.count)"
         if 3290234 == encodedp.count {
            break
         }
      } while (3290234 == encodedp.count) && (1 <= symbolS.count)
      for _ in 0 ..< 1 {
         symbolS = "\(((String(cString:[69,0], encoding: .utf8)!) == encodedp ? encodedp.count : Int(accentR > 195845701.0 || accentR < -195845701.0 ? 8.0 : accentR)))"
      }
         accentR /= Swift.max(5, Float(1))
      for _ in 0 ..< 3 {
          var dnewsL: String! = String(cString: [102,111,114,99,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &dnewsL) { pointer in
    
         }
          var basel: String! = String(cString: [112,111,119,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &basel) { pointer in
                _ = pointer.pointee
         }
          var linkl: Double = 4.0
         editW.append("\(symbolS.count % (Swift.max(3, editW.count)))")
         dnewsL = "\(editW.count)"
         basel.append("\(basel.count + 3)")
         linkl += (Double(Int(linkl > 31647852.0 || linkl < -31647852.0 ? 60.0 : linkl)))
      }
       var usedG: Double = 0.0
       var aspectX: Double = 2.0
      withUnsafeMutablePointer(to: &aspectX) { pointer in
    
      }
         encodedp = "\(encodedp.count)"
         encodedp.append("\((1 - Int(aspectX > 270393397.0 || aspectX < -270393397.0 ? 83.0 : aspectX)))")
      for _ in 0 ..< 1 {
         symbolS = "\((2 + Int(usedG > 54637960.0 || usedG < -54637960.0 ? 89.0 : usedG)))"
      }
      repeat {
         blacklistedN.append("\(encodedp.count)")
         if blacklistedN.count == 1415362 {
            break
         }
      } while (3.66 < (aspectX * 1.24)) && (blacklistedN.count == 1415362)
      repeat {
          var capturec: String! = String(cString: [105,99,111,110,0], encoding: .utf8)!
          var inset2: String! = String(cString: [102,111,114,119,97,114,100,101,100,0], encoding: .utf8)!
          _ = inset2
         usedG /= Swift.max(Double(blacklistedN.count % 2), 4)
         capturec.append("\(3 & editW.count)")
         inset2 = "\((Int(accentR > 154917107.0 || accentR < -154917107.0 ? 80.0 : accentR)))"
         if usedG == 4459818.0 {
            break
         }
      } while (!editW.hasSuffix("\(usedG)")) && (usedG == 4459818.0)
      while (4 < symbolS.count) {
          var toolJ: String! = String(cString: [114,101,103,117,108,97,114,0], encoding: .utf8)!
          _ = toolJ
         editW.append("\(2)")
         toolJ = "\(1)"
         break
      }
         symbolS = "\(3 ^ blacklistedN.count)"
      for _ in 0 ..< 1 {
          var trailingY: Double = 4.0
          var type_2J: String! = String(cString: [97,117,116,111,100,111,119,110,108,111,97,100,0], encoding: .utf8)!
          var bottomW: String! = String(cString: [112,97,114,101,110,0], encoding: .utf8)!
          _ = bottomW
         aspectX -= Double(editW.count)
         trailingY -= Double(bottomW.count)
         type_2J = "\(editW.count % 3)"
         bottomW.append("\((Int(accentR > 283355789.0 || accentR < -283355789.0 ? 14.0 : accentR)))")
      }
         editW = "\(3)"
         symbolS.append("\((encodedp == (String(cString:[114,0], encoding: .utf8)!) ? encodedp.count : blacklistedN.count))")
      completionp += (Double(Int(completionp > 244083685.0 || completionp < -244083685.0 ? 6.0 : completionp) ^ 1))
   }

      completionp -= (Double(1 - Int(rowi > 266588500.0 || rowi < -266588500.0 ? 17.0 : rowi)))
        super.viewDidLayoutSubviews()
        updateBottomGradient()
    }

    private func setupUI() {
       var partsN: String! = String(cString: [108,97,112,110,100,122,0], encoding: .utf8)!
    _ = partsN
      partsN.append("\(partsN.count - 3)")

        view.addSubview(mediaImageView)
        view.addSubview(bottomGradientView)
        view.addSubview(backButton)
        view.addSubview(avatarImageView)
        view.addSubview(userNameLabel)
        view.addSubview(contentLabel)

        mediaImageView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalTo(view.safeAreaLayoutGuide).offset(Layout.topInset)
            make.width.height.equalTo(Layout.backButtonSize)
        }

        avatarImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
            make.bottom.equalTo(contentLabel.snp.top).offset(-8)
            make.width.height.equalTo(Layout.avatarSize)
        }

        userNameLabel.snp.makeConstraints { make in
            make.leading.equalTo(avatarImageView.snp.trailing).offset(10)
            make.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.centerY.equalTo(avatarImageView)
        }

        contentLabel.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.bottom.equalTo(view.safeAreaLayoutGuide).inset(Layout.bottomInset)
        }

        bottomGradientView.snp.makeConstraints { make in
            make.leading.trailing.bottom.equalToSuperview()
            make.height.equalTo(Layout.bottomGradientHeight)
        }
    }

    private func bindPost() {
       var hangC: [Any]! = [85, 17]
    _ = hangC
   if 5 <= hangC.count {
      hangC = [hangC.count]
   }

        avatarImageView.image = UserData.image(for: post.avatarUrl)
        userNameLabel.text = post.userName
        contentLabel.text = post.content
        mediaImageView.image = UserData.image(for: post.mediaUrl)
    }

    private func updateBottomGradient() {
       var editM: String! = String(cString: [105,115,111,112,101,110,113,117,111,116,101,0], encoding: .utf8)!
    var reviewx: String! = String(cString: [109,101,97,115,117,114,101,0], encoding: .utf8)!
      reviewx.append("\(2)")

      editM.append("\(editM.count)")
        bottomGradientView.layer.sublayers?
            .filter { $0 is CAGradientLayer }
            .forEach { $0.removeFromSuperlayer() }

        let field = CAGradientLayer()
        field.frame = bottomGradientView.bounds
        field.colors = [
            UIColor.clear.cgColor,
            UIColor.black.withAlphaComponent(0.75).cgColor
        ]
        field.locations = [0, 1]
        bottomGradientView.layer.insertSublayer(field, at: 0)
    }

    @objc private func didTapBack() {
       var borderT: Double = 0.0
      borderT /= Swift.max((Double(Int(borderT > 65951980.0 || borderT < -65951980.0 ? 32.0 : borderT) + 2)), 3)

        navigationController?.popViewController(animated: true)
    }
}
