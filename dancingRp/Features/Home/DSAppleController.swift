
import Foundation

import UIKit

class DSAppleController: DSEmptyController {

    private enum Layout {
        static let topBackgroundHeightRatio: CGFloat = 0.36
        static let chatTopCornerRadius: CGFloat = 24
        static let chatOverlap: CGFloat = 28
        static let inputBarHeight: CGFloat = 52
        static let inputHorizontalInset: CGFloat = 16
        static let sendButtonSize: CGFloat = 52
    }

    private var messages: [DSEditLive] = []
    private var pendingReplyWorkItem: DispatchWorkItem?

    private let topBackgroundImageView: UIImageView = {
       var targetO: String! = String(cString: [109,115,103,0], encoding: .utf8)!
    var excludem: [String: Any]! = [String(cString: [97,108,105,103,110,0], encoding: .utf8)!:39, String(cString: [97,116,111,102,0], encoding: .utf8)!:60, String(cString: [101,110,99,97,112,115,117,108,97,116,101,0], encoding: .utf8)!:0]
   while (5 > (4 | excludem.values.count)) {
      targetO = "\(1 ^ targetO.count)"
      break
   }

        let imageView = UIImageView(image: UIImage(named: "AI_top"))
   repeat {
      targetO.append("\(excludem.keys.count & 3)")
      if targetO == (String(cString:[50,50,97,119,103,0], encoding: .utf8)!) {
         break
      }
   } while (targetO == (String(cString:[50,50,97,119,103,0], encoding: .utf8)!)) && (4 >= (excludem.values.count / 4) || 4 >= (4 / (Swift.max(7, targetO.count))))
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        return imageView
    }()

    private lazy var backButton: UIButton = {
       var messagew: String! = String(cString: [115,115,101,116,0], encoding: .utf8)!
    _ = messagew
    var followN: String! = String(cString: [108,97,103,97,114,105,116,104,114,97,99,0], encoding: .utf8)!
       var fieldn: String! = String(cString: [115,121,109,98,111,108,105,99,97,116,97,98,108,101,0], encoding: .utf8)!
       _ = fieldn
       var product4: Float = 4.0
       var messagek: Double = 2.0
          var radiusJ: Float = 1.0
          _ = radiusJ
          var commentatorsr: String! = String(cString: [118,111,114,98,105,115,0], encoding: .utf8)!
          var attributedV: String! = String(cString: [115,112,111,110,115,111,114,101,100,0], encoding: .utf8)!
          _ = attributedV
         fieldn.append("\((Int(radiusJ > 197573248.0 || radiusJ < -197573248.0 ? 23.0 : radiusJ)))")
         commentatorsr.append("\(commentatorsr.count)")
         attributedV = "\((commentatorsr.count + Int(radiusJ > 133529468.0 || radiusJ < -133529468.0 ? 56.0 : radiusJ)))"
      if (Int(product4 > 237518853.0 || product4 < -237518853.0 ? 72.0 : product4)) < fieldn.count {
         fieldn = "\((Int(messagek > 266305342.0 || messagek < -266305342.0 ? 59.0 : messagek)))"
      }
         fieldn.append("\((Int(messagek > 294986030.0 || messagek < -294986030.0 ? 53.0 : messagek) & Int(product4 > 215084943.0 || product4 < -215084943.0 ? 57.0 : product4)))")
          var encodedf: Int = 1
          var addB: String! = String(cString: [105,115,97,99,102,105,120,0], encoding: .utf8)!
          var durationG: Double = 4.0
         messagek /= Swift.max((Double(Int(messagek > 359172734.0 || messagek < -359172734.0 ? 45.0 : messagek) * 2)), 1)
         encodedf /= Swift.max(1, 3)
         addB.append("\((Int(product4 > 147037513.0 || product4 < -147037513.0 ? 91.0 : product4) * 3))")
         durationG /= Swift.max((Double(Int(product4 > 82066200.0 || product4 < -82066200.0 ? 15.0 : product4) * 1)), 1)
          var urln: Float = 3.0
          var placeholderi: String! = String(cString: [112,117,108,108,113,117,111,116,101,0], encoding: .utf8)!
         messagek += (Double(3 ^ Int(product4 > 117165326.0 || product4 < -117165326.0 ? 37.0 : product4)))
         urln /= Swift.max(5, Float(1))
         placeholderi.append("\(fieldn.count % (Swift.max(3, 10)))")
         product4 -= (Float(1 % (Swift.max(3, Int(product4 > 248095465.0 || product4 < -248095465.0 ? 99.0 : product4)))))
         messagek /= Swift.max((Double((String(cString:[110,0], encoding: .utf8)!) == fieldn ? fieldn.count : Int(product4 > 32619459.0 || product4 < -32619459.0 ? 8.0 : product4))), 5)
      while ((Double(Double(3) * messagek)) == 1.92) {
         product4 /= Swift.max(4, (Float(Int(messagek > 113842400.0 || messagek < -113842400.0 ? 94.0 : messagek) % 3)))
         break
      }
      while (messagek >= Double(product4)) {
         messagek /= Swift.max(4, (Double(3 / (Swift.max(Int(messagek > 393053898.0 || messagek < -393053898.0 ? 13.0 : messagek), 3)))))
         break
      }
      followN.append("\(fieldn.count + followN.count)")

        let dimmingButton = UIButton(type: .system)
       var placeholder_: Float = 3.0
       _ = placeholder_
      for _ in 0 ..< 3 {
          var normalizedd: [Any]! = [UILabel()]
         withUnsafeMutablePointer(to: &normalizedd) { pointer in
    
         }
          var configurationo: [Any]! = [4.0]
          var type_bsK: Int = 1
         placeholder_ /= Swift.max(3, Float(type_bsK))
         normalizedd.append(2 - type_bsK)
         configurationo = [configurationo.count]
      }
      for _ in 0 ..< 2 {
         placeholder_ += (Float(Int(placeholder_ > 40026892.0 || placeholder_ < -40026892.0 ? 98.0 : placeholder_) & 1))
      }
         placeholder_ -= (Float(Int(placeholder_ > 212106694.0 || placeholder_ < -212106694.0 ? 6.0 : placeholder_) - 2))
      messagew = "\(messagew.count)"
        let tab = UIImage.SymbolConfiguration(pointSize: 20, weight: .semibold)
        dimmingButton.setImage(UIImage(systemName: "chevron.left", withConfiguration: tab), for: .normal)
        dimmingButton.tintColor = .white
        dimmingButton.addTarget(self, action: #selector(didTapBack), for: .touchUpInside)
        return dimmingButton
    }()

    private let chatContainerView: UIView = {
       var page6: String! = String(cString: [111,114,105,103,105,110,0], encoding: .utf8)!
       var addo: String! = String(cString: [115,117,98,98,97,110,100,0], encoding: .utf8)!
       _ = addo
       var edito: [Any]! = [51, 67]
       var hasx: Double = 0.0
      repeat {
          var mail2: String! = String(cString: [98,97,99,107,103,114,111,117,110,100,115,0], encoding: .utf8)!
          var modityX: Double = 4.0
         withUnsafeMutablePointer(to: &modityX) { pointer in
    
         }
          var aspectv: String! = String(cString: [116,119,111,115,99,97,108,101,0], encoding: .utf8)!
          var dateJ: Float = 3.0
          var clip0: String! = String(cString: [119,114,106,112,103,99,111,109,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &clip0) { pointer in
    
         }
         edito = [1]
         mail2 = "\(addo.count)"
         modityX -= Double(2)
         aspectv = "\(2)"
         dateJ /= Swift.max(5, Float(2 * aspectv.count))
         clip0.append("\(edito.count)")
         if 4579184 == edito.count {
            break
         }
      } while (4579184 == edito.count) && (addo.count < 4)
      for _ in 0 ..< 3 {
         edito.append(1)
      }
      repeat {
          var ownerR: Float = 4.0
          _ = ownerR
          var submith: Double = 1.0
          var author0: String! = String(cString: [101,120,99,108,117,100,101,100,0], encoding: .utf8)!
          var gnewss: Float = 1.0
         withUnsafeMutablePointer(to: &gnewss) { pointer in
                _ = pointer.pointee
         }
         edito = [(1 >> (Swift.min(labs(Int(gnewss > 283186807.0 || gnewss < -283186807.0 ? 8.0 : gnewss)), 1)))]
         ownerR -= (Float(Int(gnewss > 280957234.0 || gnewss < -280957234.0 ? 97.0 : gnewss)))
         submith -= Double(1)
         author0 = "\(2)"
         if edito.count == 3993364 {
            break
         }
      } while (edito.count == 3993364) && (addo.hasPrefix("\(edito.count)"))
          var hang3: Bool = true
         edito = [(Int(hasx > 44629754.0 || hasx < -44629754.0 ? 25.0 : hasx) - (hang3 ? 1 : 3))]
         addo.append("\((3 >> (Swift.min(labs(Int(hasx > 95985313.0 || hasx < -95985313.0 ? 34.0 : hasx)), 5))))")
          var encodedt: String! = String(cString: [98,111,100,101,114,0], encoding: .utf8)!
          var togglei: String! = String(cString: [106,115,101,112,0], encoding: .utf8)!
         hasx -= Double(edito.count + encodedt.count)
         togglei = "\(addo.count)"
      for _ in 0 ..< 2 {
         hasx += Double(1 * edito.count)
      }
         hasx -= Double(edito.count)
      while (!edito.contains { $0 as? Double == hasx }) {
          var moreD: String! = String(cString: [98,111,111,108,0], encoding: .utf8)!
          var intrinsicl: String! = String(cString: [102,105,116,122,0], encoding: .utf8)!
          var fieldr: String! = String(cString: [98,108,101,101,100,0], encoding: .utf8)!
         edito = [2]
         moreD = "\(edito.count - moreD.count)"
         intrinsicl = "\(edito.count)"
         fieldr = "\((intrinsicl == (String(cString:[83,0], encoding: .utf8)!) ? edito.count : intrinsicl.count))"
         break
      }
      page6.append("\((Int(hasx > 125610911.0 || hasx < -125610911.0 ? 92.0 : hasx) % (Swift.max(edito.count, 10))))")

        let view = UIView()
        view.backgroundColor = .white
        view.clipsToBounds = true
        return view
    }()

    private lazy var tableView: UITableView = {
       var profileC: String! = String(cString: [109,105,114,114,111,114,0], encoding: .utf8)!
   if profileC.hasPrefix("\(profileC.count)") {
      profileC = "\(profileC.count)"
   }

        let tableView = UITableView(frame: .zero, style: .plain)
        tableView.backgroundColor = .white
        tableView.separatorStyle = .none
        tableView.showsVerticalScrollIndicator = false
        tableView.estimatedRowHeight = 80
        tableView.rowHeight = UITableView.automaticDimension
        tableView.keyboardDismissMode = .interactive
        tableView.dataSource = self
        tableView.delegate = self
        tableView.register(
            DSToolCell.self,
            forCellReuseIdentifier: DSToolCell.reuseIdentifier
        )
        return tableView
    }()

    private let inputBarView: UIView = {
       var tracksJ: String! = String(cString: [118,97,114,119,105,100,116,104,0], encoding: .utf8)!
    _ = tracksJ
   if tracksJ != String(cString:[71,0], encoding: .utf8)! {
      tracksJ.append("\(2)")
   }

        let view = UIView()
        view.backgroundColor = .white
        return view
    }()

    private let inputBackgroundView: UIView = {
       var cleaned6: Int = 0
    var welcome3: String! = String(cString: [97,97,97,97,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &welcome3) { pointer in
          _ = pointer.pointee
   }
   while (!welcome3.contains("\(cleaned6)")) {
      welcome3.append("\(3)")
      break
   }

        let view = UIView()
   for _ in 0 ..< 3 {
      cleaned6 %= Swift.max(cleaned6 + welcome3.count, 3)
   }
        view.backgroundColor = UIColor.hex("#333333")
        view.layer.cornerRadius = 26
        view.clipsToBounds = true
        return view
    }()

    private lazy var messageTextField: UITextField = {
       var revise6: Double = 1.0
    var remainZ: String! = String(cString: [115,104,105,109,109,101,114,0], encoding: .utf8)!
   repeat {
       var messagesb: String! = String(cString: [115,111,117,114,99,101,105,100,0], encoding: .utf8)!
       var views_: Double = 1.0
       var nextb: Int = 0
       var updatesZ: String! = String(cString: [114,101,100,97,99,116,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &updatesZ) { pointer in
             _ = pointer.pointee
      }
      while (updatesZ.count < 1) {
         messagesb = "\((Int(views_ > 387100874.0 || views_ < -387100874.0 ? 7.0 : views_)))"
         break
      }
          var scriptz: Double = 5.0
         nextb %= Swift.max((2 >> (Swift.min(5, labs(Int(scriptz > 39410411.0 || scriptz < -39410411.0 ? 15.0 : scriptz))))), 4)
         updatesZ = "\((updatesZ == (String(cString:[97,0], encoding: .utf8)!) ? nextb : updatesZ.count))"
      while ((1.87 / (Swift.max(10, views_))) >= 5.1) {
         views_ /= Swift.max(Double(messagesb.count ^ updatesZ.count), 5)
         break
      }
          var symbolz: String! = String(cString: [101,110,116,105,114,101,0], encoding: .utf8)!
          var min_pU: String! = String(cString: [115,109,107,97,0], encoding: .utf8)!
         views_ -= (Double((String(cString:[122,0], encoding: .utf8)!) == updatesZ ? min_pU.count : updatesZ.count))
         symbolz = "\(nextb & 3)"
         messagesb = "\(3)"
         updatesZ = "\(updatesZ.count)"
         messagesb.append("\(3 % (Swift.max(1, messagesb.count)))")
      if (Double(Int(views_) / (Swift.max(4, nextb)))) < 4.21 {
         views_ /= Swift.max(Double(nextb), 5)
      }
          var w_heightD: [String: Any]! = [String(cString: [114,101,103,105,115,116,101,114,101,100,0], encoding: .utf8)!:1, String(cString: [112,105,120,101,108,102,111,114,109,97,116,0], encoding: .utf8)!:49, String(cString: [108,105,98,97,118,102,105,108,116,101,114,0], encoding: .utf8)!:51]
          var partse: [Any]! = [83, 26, 65]
          var submitQ: Int = 2
         withUnsafeMutablePointer(to: &submitQ) { pointer in
    
         }
         views_ -= Double(1)
         w_heightD["\(partse.count)"] = 1 + partse.count
         submitQ /= Swift.max(3, 3)
      for _ in 0 ..< 1 {
          var baro: String! = String(cString: [109,111,110,103,111,0], encoding: .utf8)!
          var optionsn: String! = String(cString: [115,116,114,105,99,116,0], encoding: .utf8)!
         views_ += (Double((String(cString:[69,0], encoding: .utf8)!) == updatesZ ? updatesZ.count : messagesb.count))
         baro = "\(baro.count >> (Swift.min(updatesZ.count, 4)))"
         optionsn.append("\((nextb ^ Int(views_ > 220305238.0 || views_ < -220305238.0 ? 81.0 : views_)))")
      }
       var member7: String! = String(cString: [101,109,111,106,105,115,0], encoding: .utf8)!
       _ = member7
       var blacklistedB: String! = String(cString: [101,113,117,105,118,97,108,101,110,99,101,0], encoding: .utf8)!
         member7.append("\(1 - messagesb.count)")
         blacklistedB = "\(member7.count >> (Swift.min(3, labs(nextb))))"
      revise6 -= Double(3)
      if 1754688.0 == revise6 {
         break
      }
   } while (1754688.0 == revise6) && (2 >= (1 + remainZ.count) && (Int(revise6 > 57303190.0 || revise6 < -57303190.0 ? 90.0 : revise6) / (Swift.max(3, remainZ.count))) >= 1)

        let person = UITextField()
      revise6 -= (Double(2 % (Swift.max(1, Int(revise6 > 33361285.0 || revise6 < -33361285.0 ? 64.0 : revise6)))))
        person.attributedPlaceholder = NSAttributedString(
            string: "Enter what you want to send",
            attributes: [
                .foregroundColor: UIColor.white.withAlphaComponent(0.45),
                .font: UIFont.systemFont(ofSize: 15, weight: .regular)
            ]
        )
        person.textColor = .white
        person.font = .systemFont(ofSize: 15, weight: .regular)
        person.returnKeyType = .send
        person.delegate = self
        return person
    }()

    private lazy var sendButton: UIButton = {
       var componentE: [String: Any]! = [String(cString: [112,111,115,116,102,105,120,0], encoding: .utf8)!:48, String(cString: [110,99,111,110,102,0], encoding: .utf8)!:19, String(cString: [118,99,97,114,100,0], encoding: .utf8)!:89]
   for _ in 0 ..< 2 {
      componentE = ["\(componentE.keys.count)": componentE.keys.count + componentE.values.count]
   }

        let dimmingButton = UIButton(type: .custom)
        dimmingButton.setImage(UIImage(named: "common_send"), for: .normal)
        dimmingButton.addTarget(self, action: #selector(didTapSend), for: .touchUpInside)
        return dimmingButton
    }()

    override func viewDidLoad() {
       var leadingy: [String: Any]! = [String(cString: [99,111,108,115,112,97,110,0], encoding: .utf8)!:29, String(cString: [100,105,114,101,99,116,105,111,110,115,0], encoding: .utf8)!:70, String(cString: [113,119,111,114,100,0], encoding: .utf8)!:4]
   withUnsafeMutablePointer(to: &leadingy) { pointer in
    
   }
   if leadingy.keys.contains("\(leadingy.count)") {
      leadingy["\(leadingy.keys.count)"] = leadingy.keys.count / 1
   }

        super.viewDidLoad()
        setupUI()
        showWelcomeMessage()
    }

    private func showWelcomeMessage() {
       var primaryj: String! = String(cString: [101,102,102,101,99,105,116,118,101,108,121,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &primaryj) { pointer in
    
   }
    var subdirectory_: String! = String(cString: [112,99,109,119,98,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
       var resolvedf: Bool = true
      withUnsafeMutablePointer(to: &resolvedf) { pointer in
             _ = pointer.pointee
      }
       var line0: [String: Any]! = [String(cString: [99,104,97,114,109,97,112,0], encoding: .utf8)!:79, String(cString: [114,101,97,99,104,97,98,108,101,0], encoding: .utf8)!:39, String(cString: [114,117,108,101,98,111,111,107,0], encoding: .utf8)!:63]
       var mailG: String! = String(cString: [110,97,109,101,115,0], encoding: .utf8)!
       _ = mailG
       var sender1: String! = String(cString: [109,97,114,115,104,97,108,108,105,110,103,0], encoding: .utf8)!
         line0 = ["\(resolvedf)": sender1.count]
      if 2 >= (1 << (Swift.min(4, line0.count))) || 3 >= (1 << (Swift.min(1, line0.count))) {
          var remotey: String! = String(cString: [111,112,101,110,0], encoding: .utf8)!
          var scroll7: Int = 2
          _ = scroll7
          var moditye: String! = String(cString: [101,112,111,108,108,0], encoding: .utf8)!
         line0 = [moditye: 1]
         remotey.append("\(moditye.count / (Swift.max(1, 9)))")
         scroll7 %= Swift.max(4, (remotey == (String(cString:[69,0], encoding: .utf8)!) ? sender1.count : remotey.count))
      }
      if !sender1.hasSuffix("\(resolvedf)") {
         sender1 = "\(sender1.count)"
      }
      for _ in 0 ..< 2 {
          var fillersj: String! = String(cString: [99,99,105,116,116,0], encoding: .utf8)!
          var blacklistedH: String! = String(cString: [98,101,122,105,101,114,0], encoding: .utf8)!
          var actionx: String! = String(cString: [109,97,114,107,105,110,103,0], encoding: .utf8)!
          var existingY: String! = String(cString: [118,97,108,117,108,101,0], encoding: .utf8)!
         line0[fillersj] = (fillersj == (String(cString:[85,0], encoding: .utf8)!) ? fillersj.count : line0.values.count)
         blacklistedH = "\(actionx.count)"
         actionx = "\(actionx.count)"
         existingY = "\(fillersj.count << (Swift.min(existingY.count, 3)))"
      }
      for _ in 0 ..< 2 {
         sender1.append("\((line0.count << (Swift.min(3, labs((resolvedf ? 3 : 5))))))")
      }
         sender1.append("\(((resolvedf ? 4 : 5) / (Swift.max(line0.count, 8))))")
         sender1 = "\(mailG.count ^ 2)"
      for _ in 0 ..< 1 {
         resolvedf = sender1.count == 48
      }
      primaryj.append("\(primaryj.count * mailG.count)")
   }

   while (!subdirectory_.contains(subdirectory_)) {
       var creationB: Double = 5.0
       var r_titleO: String! = String(cString: [102,108,97,99,100,115,112,0], encoding: .utf8)!
         creationB -= (Double(r_titleO == (String(cString:[102,0], encoding: .utf8)!) ? Int(creationB > 213895517.0 || creationB < -213895517.0 ? 41.0 : creationB) : r_titleO.count))
      while (creationB <= 5.63) {
         r_titleO = "\((Int(creationB > 183414896.0 || creationB < -183414896.0 ? 8.0 : creationB) & 1))"
         break
      }
      if (r_titleO.count * Int(creationB > 3753766.0 || creationB < -3753766.0 ? 19.0 : creationB)) > 2 && 5.78 > (creationB * 5.63) {
         creationB -= Double(3 % (Swift.max(8, r_titleO.count)))
      }
          var secondarym: [Any]! = [String(cString: [108,105,109,105,116,101,100,0], encoding: .utf8)!, String(cString: [112,111,115,101,115,0], encoding: .utf8)!, String(cString: [115,119,105,122,122,108,101,100,0], encoding: .utf8)!]
          var lastU: Double = 5.0
         r_titleO = "\(((String(cString:[90,0], encoding: .utf8)!) == r_titleO ? r_titleO.count : Int(creationB > 63538743.0 || creationB < -63538743.0 ? 37.0 : creationB)))"
         secondarym.append(1)
         lastU -= Double(r_titleO.count)
      while ((Int(creationB > 258550880.0 || creationB < -258550880.0 ? 29.0 : creationB) / (Swift.max(r_titleO.count, 6))) >= 4) {
          var collectionN: Double = 1.0
          _ = collectionN
         r_titleO.append("\((r_titleO.count / (Swift.max(7, Int(creationB > 78559444.0 || creationB < -78559444.0 ? 63.0 : creationB)))))")
         collectionN -= (Double(Int(creationB > 175845095.0 || creationB < -175845095.0 ? 96.0 : creationB)))
         break
      }
       var owner8: Float = 0.0
       var pushK: Float = 4.0
      withUnsafeMutablePointer(to: &pushK) { pointer in
             _ = pointer.pointee
      }
         owner8 -= (Float(Int(creationB > 114987532.0 || creationB < -114987532.0 ? 64.0 : creationB) & 2))
         pushK += (Float(Int(owner8 > 86088570.0 || owner8 < -86088570.0 ? 70.0 : owner8)))
      subdirectory_.append("\(subdirectory_.count)")
      break
   }
        messages = [
            DSEditLive(sender: .ai, text: DS_AIChatScripts.welcomeMessage)
        ]
        tableView.reloadData()
        scrollToBottom(animated: false)
    }

    override func viewDidLayoutSubviews() {
       var cachedK: Int = 0
      cachedK |= cachedK / (Swift.max(cachedK, 7))

        super.viewDidLayoutSubviews()
        chatContainerView.layer.cornerRadius = Layout.chatTopCornerRadius
        chatContainerView.layer.maskedCorners = [.layerMinXMinYCorner, .layerMaxXMinYCorner]
    }

    private func setupUI() {
       var default_0W: [String: Any]! = [String(cString: [111,95,50,55,0], encoding: .utf8)!:25, String(cString: [122,102,114,101,101,0], encoding: .utf8)!:49, String(cString: [115,122,97,98,111,115,0], encoding: .utf8)!:26]
    _ = default_0W
   if 5 >= (5 >> (Swift.min(5, default_0W.values.count))) || (default_0W.values.count >> (Swift.min(labs(5), 2))) >= 3 {
       var keyv: Double = 5.0
         keyv /= Swift.max((Double(Int(keyv > 190379592.0 || keyv < -190379592.0 ? 44.0 : keyv) >> (Swift.min(3, labs(Int(keyv > 172085487.0 || keyv < -172085487.0 ? 22.0 : keyv)))))), 4)
      while (keyv >= 5.37) {
         keyv += (Double(3 * Int(keyv > 390977966.0 || keyv < -390977966.0 ? 33.0 : keyv)))
         break
      }
          var relativez: Bool = true
         keyv += (Double(Int(keyv > 379074090.0 || keyv < -379074090.0 ? 65.0 : keyv) / 3))
         relativez = 68.57 >= keyv
      default_0W = ["\(default_0W.keys.count)": (Int(keyv > 172992231.0 || keyv < -172992231.0 ? 90.0 : keyv) + default_0W.values.count)]
   }

        view.backgroundColor = .black

        view.addSubview(topBackgroundImageView)
        view.addSubview(backButton)
        view.addSubview(chatContainerView)
        view.addSubview(inputBarView)

        chatContainerView.addSubview(tableView)
        inputBarView.addSubview(inputBackgroundView)
        inputBarView.addSubview(sendButton)
        inputBackgroundView.addSubview(messageTextField)

        topBackgroundImageView.snp.makeConstraints { make in
            make.top.leading.trailing.equalToSuperview()
            make.height.equalToSuperview().multipliedBy(Layout.topBackgroundHeightRatio)
        }

        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(8)
            make.top.equalTo(view.safeAreaLayoutGuide).inset(4)
            make.width.height.equalTo(44)
        }

        inputBarView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview()
            make.bottom.equalTo(view.safeAreaLayoutGuide)
            make.height.equalTo(Layout.inputBarHeight + 16)
        }

        sendButton.snp.makeConstraints { make in
            make.trailing.equalToSuperview().inset(Layout.inputHorizontalInset)
            make.centerY.equalToSuperview()
            make.width.height.equalTo(Layout.sendButtonSize)
        }

        inputBackgroundView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.inputHorizontalInset)
            make.trailing.equalTo(sendButton.snp.leading).offset(-12)
            make.centerY.equalToSuperview()
            make.height.equalTo(Layout.inputBarHeight)
        }

        messageTextField.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(16)
            make.centerY.equalToSuperview()
        }

        chatContainerView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview()
            make.top.equalTo(topBackgroundImageView.snp.bottom).offset(-Layout.chatOverlap)
            make.bottom.equalTo(inputBarView.snp.top)
        }

        tableView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
    }

    private func scrollToBottom(animated: Bool) {
       var scopy_9f: String! = String(cString: [101,108,108,105,112,115,101,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      scopy_9f.append("\(1)")
   }

        guard !messages.isEmpty else { return }
        let radius = IndexPath(row: messages.count - 1, section: 0)
        tableView.scrollToRow(at: radius, at: .bottom, animated: animated)
    }

    @objc private func didTapBack() {
       var generatorZ: Bool = false
      generatorZ = (generatorZ ? !generatorZ : !generatorZ)

        pendingReplyWorkItem?.cancel()
        pendingReplyWorkItem = nil
        navigationController?.popViewController(animated: true)
    }

    @objc private func didTapSend() {
       var inputA: [Any]! = [85, 92]
   while (inputA.count >= 2) {
      inputA.append(inputA.count)
      break
   }

        
        DSGuide.shared.postDefaultRequest(isShow: false) { result in
            switch result {
            case .success(_):
                self.sendAction()
            case .failure(_):
                self.sendAction()
            }
        }
    }
    
    private func sendAction(){
       var credentialr: String! = String(cString: [100,114,111,112,112,101,114,0], encoding: .utf8)!
    _ = credentialr
    var reusablez: String! = String(cString: [114,101,112,108,105,101,115,0], encoding: .utf8)!
    _ = reusablez
   if !credentialr.hasSuffix(reusablez) {
      credentialr.append("\(reusablez.count)")
   }

   repeat {
      reusablez.append("\(reusablez.count * credentialr.count)")
      if reusablez.count == 3129330 {
         break
      }
   } while (credentialr != String(cString:[72,0], encoding: .utf8)!) && (reusablez.count == 3129330)
        
        let handling = messageTextField.text?.trimmingCharacters(in: .whitespacesAndNewlines) ?? ""
        guard !handling.isEmpty, pendingReplyWorkItem == nil else { return }

        messages.append(DSEditLive(sender: .user, text: handling))
        messageTextField.text = nil
        tableView.reloadData()
        scrollToBottom(animated: true)
        scheduleAIReply()
    }

    private func scheduleAIReply() {
       var placeholdero: Bool = true
    var audio9: Float = 5.0
      placeholdero = placeholdero || 45.56 < audio9

      placeholdero = !placeholdero || audio9 < 100.56
        pendingReplyWorkItem?.cancel()
        setInputEnabled(false)

        let has = DispatchWorkItem { [weak self] in
            guard let self else { return }
            self.messages.append(
                DSEditLive(sender: .ai, text: DS_AIChatScripts.randomReply())
            )
            self.pendingReplyWorkItem = nil
            self.setInputEnabled(true)
            self.tableView.reloadData()
            self.scrollToBottom(animated: true)
        }
        pendingReplyWorkItem = has
        DispatchQueue.main.asyncAfter(
            deadline: .now() + DS_AIChatScripts.randomReplyDelay,
            execute: has
        )
    }

    private func setInputEnabled(_ enabled: Bool) {
       var overlapQ: Int = 2
       var auto_m7: Double = 2.0
       var collectiong: Float = 2.0
      withUnsafeMutablePointer(to: &collectiong) { pointer in
             _ = pointer.pointee
      }
       var encodedU: [Any]! = [25.0]
      withUnsafeMutablePointer(to: &encodedU) { pointer in
             _ = pointer.pointee
      }
      if (2.62 + auto_m7) > 1.14 && (2.62 + collectiong) > 1.14 {
          var maila: Double = 0.0
          var indicatorp: String! = String(cString: [115,101,116,0], encoding: .utf8)!
          _ = indicatorp
         collectiong -= (Float(Int(auto_m7 > 193415496.0 || auto_m7 < -193415496.0 ? 96.0 : auto_m7) % (Swift.max(encodedU.count, 8))))
         maila /= Swift.max((Double(Int(auto_m7 > 234554615.0 || auto_m7 < -234554615.0 ? 31.0 : auto_m7))), 3)
         indicatorp = "\((Int(collectiong > 161047726.0 || collectiong < -161047726.0 ? 17.0 : collectiong) & 3))"
      }
         encodedU.append((Int(collectiong > 130743642.0 || collectiong < -130743642.0 ? 49.0 : collectiong)))
         auto_m7 /= Swift.max((Double(Int(collectiong > 159625538.0 || collectiong < -159625538.0 ? 92.0 : collectiong))), 2)
          var t_imageD: String! = String(cString: [100,105,97,103,110,111,115,116,105,99,0], encoding: .utf8)!
          _ = t_imageD
          var timestamp_: String! = String(cString: [109,115,118,115,0], encoding: .utf8)!
          var allW: String! = String(cString: [117,110,111,114,100,101,114,101,100,0], encoding: .utf8)!
         auto_m7 -= (Double(Int(auto_m7 > 255641014.0 || auto_m7 < -255641014.0 ? 7.0 : auto_m7) >> (Swift.min(timestamp_.count, 2))))
         t_imageD = "\(t_imageD.count / (Swift.max(6, timestamp_.count)))"
         allW.append("\((Int(collectiong > 146560455.0 || collectiong < -146560455.0 ? 28.0 : collectiong) / (Swift.max(2, 3))))")
         collectiong += Float(1)
         auto_m7 -= Double(encodedU.count / 2)
       var actionY: [String: Any]! = [String(cString: [114,111,112,115,116,101,110,0], encoding: .utf8)!:8, String(cString: [115,101,101,110,0], encoding: .utf8)!:69, String(cString: [99,111,109,112,105,108,101,111,112,116,105,111,110,0], encoding: .utf8)!:32]
      while ((3.95 / (Swift.max(1, auto_m7))) <= 4.8 && 1.95 <= (collectiong / 3.95)) {
         collectiong -= Float(3)
         break
      }
      if (Int(auto_m7 > 321251737.0 || auto_m7 < -321251737.0 ? 17.0 : auto_m7) - encodedU.count) == 3 {
         encodedU = [actionY.values.count]
      }
      overlapQ |= 3 / (Swift.max(6, overlapQ))

        sendButton.isEnabled = enabled
        messageTextField.isEnabled = enabled
        sendButton.alpha = enabled ? 1 : 0.5
    }
}

extension DSAppleController: UITableViewDataSource {


    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
       var signD: Double = 3.0
    var followingI: Double = 4.0
   withUnsafeMutablePointer(to: &followingI) { pointer in
    
   }
   while ((Double(5 - Int(signD))) >= 3.60) {
      followingI -= Double(1)
      break
   }
      signD -= (Double(Int(followingI > 65540817.0 || followingI < -65540817.0 ? 89.0 : followingI)))

return         messages.count
    }


    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
       var backj: [String: Any]! = [String(cString: [107,105,99,107,101,100,0], encoding: .utf8)!:55, String(cString: [105,110,116,101,114,97,99,116,105,111,110,115,0], encoding: .utf8)!:81, String(cString: [112,117,108,108,117,112,0], encoding: .utf8)!:15]
    var seedi: [Any]! = [String(cString: [105,110,116,101,114,112,111,108,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [97,108,108,112,97,115,115,0], encoding: .utf8)!]
      backj = ["\(backj.count)": seedi.count >> (Swift.min(labs(3), 4))]

   while (2 >= (seedi.count + backj.keys.count) || 5 >= (seedi.count + 2)) {
      backj["\(seedi.count)"] = seedi.count
      break
   }
        guard let cell = tableView.dequeueReusableCell(
            withIdentifier: DSToolCell.reuseIdentifier,
            for: indexPath
        ) as? DSToolCell else {
            return UITableViewCell()
        }
        cell.configure(with: messages[indexPath.row])
        return cell
    }
}

extension DSAppleController: UITableViewDelegate {
}

extension DSAppleController: UITextFieldDelegate {


    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
       var endM: String! = String(cString: [109,118,112,114,111,98,115,0], encoding: .utf8)!
    var directoryl: String! = String(cString: [106,100,115,97,109,112,108,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &directoryl) { pointer in
    
   }
      endM.append("\(1 - directoryl.count)")

       var updates9: Bool = false
       var trailingi: Bool = true
      while (trailingi) {
         updates9 = updates9 && trailingi
         break
      }
      while (!updates9 && !trailingi) {
         trailingi = (!trailingi ? updates9 : !trailingi)
         break
      }
      repeat {
          var bottoml: String! = String(cString: [109,97,100,101,0], encoding: .utf8)!
          var segmenth: Float = 3.0
          var index9: String! = String(cString: [116,111,112,105,99,97,108,0], encoding: .utf8)!
          var i_products6: String! = String(cString: [117,110,105,116,116,101,115,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &i_products6) { pointer in
    
         }
          var feedY: String! = String(cString: [116,114,105,97,110,103,117,108,97,114,0], encoding: .utf8)!
         updates9 = bottoml.count < 83 || !trailingi
         segmenth /= Swift.max(Float(i_products6.count - index9.count), 3)
         index9 = "\(((trailingi ? 1 : 4) ^ feedY.count))"
         i_products6 = "\((index9 == (String(cString:[67,0], encoding: .utf8)!) ? index9.count : (updates9 ? 3 : 3)))"
         feedY.append("\(i_products6.count)")
         if updates9 ? !updates9 : updates9 {
            break
         }
      } while (updates9 ? !updates9 : updates9) && (trailingi && updates9)
      for _ in 0 ..< 3 {
          var bannert: String! = String(cString: [117,121,118,121,116,111,121,117,118,0], encoding: .utf8)!
          var launchN: String! = String(cString: [100,101,97,99,116,105,118,97,116,105,111,110,0], encoding: .utf8)!
          _ = launchN
         updates9 = !trailingi || launchN.count >= 45
         bannert = "\((launchN == (String(cString:[90,0], encoding: .utf8)!) ? (trailingi ? 5 : 3) : launchN.count))"
      }
      repeat {
         trailingi = updates9 && !trailingi
         if trailingi ? !trailingi : trailingi {
            break
         }
      } while (!updates9 || trailingi) && (trailingi ? !trailingi : trailingi)
       var review3: [String: Any]! = [String(cString: [102,117,100,103,101,0], encoding: .utf8)!:String(cString: [98,105,116,119,114,105,116,101,114,0], encoding: .utf8)!, String(cString: [98,97,99,107,103,114,111,117,110,100,105,110,103,0], encoding: .utf8)!:String(cString: [115,105,109,117,108,116,97,110,101,111,117,115,0], encoding: .utf8)!]
       var suggestedS: [String: Any]! = [String(cString: [101,110,102,111,114,99,101,0], encoding: .utf8)!:66, String(cString: [112,108,97,110,0], encoding: .utf8)!:15, String(cString: [115,111,108,97,110,97,0], encoding: .utf8)!:98]
         review3["\(updates9)"] = review3.keys.count
         suggestedS["\(trailingi)"] = review3.values.count
      endM.append("\(directoryl.count ^ endM.count)")
        didTapSend()
        return true
    }
}
