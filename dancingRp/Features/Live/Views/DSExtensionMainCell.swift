
import Foundation

import UIKit

struct DSSetupClip {
    let userName: String?
    let avatarPath: String?
    let text: String
    let isSystem: Bool


    static func system(_ text: String) -> DSSetupClip {
       var migratedy: Int = 0
   while (migratedy >= 3) {
      migratedy |= 2
      break
   }

return         DSSetupClip(userName: nil, avatarPath: nil, text: text, isSystem: true)
    }


    static func member(userName: String, avatarPath: String?, text: String) -> DSSetupClip {
       var completionh: String! = String(cString: [101,114,114,108,111,103,0], encoding: .utf8)!
    _ = completionh
      completionh = "\(((String(cString:[118,0], encoding: .utf8)!) == completionh ? completionh.count : completionh.count))"

return         DSSetupClip(userName: userName, avatarPath: avatarPath, text: text, isSystem: false)
    }
}

final class DSExtensionMainCell: UITableViewCell {

    static let reuseIdentifier = "DSExtensionMainCell"

    private enum Layout {
        static let horizontalInset: CGFloat = 16
        static let avatarSize: CGFloat = 36
        static let systemCornerRadius: CGFloat = 12
    }

    private let systemContainerView: UIView = {
       var panel6: String! = String(cString: [116,111,107,101,110,116,120,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
       var excludeK: Double = 1.0
       var insetw: String! = String(cString: [119,101,108,115,100,101,99,100,101,109,111,0], encoding: .utf8)!
       _ = insetw
      for _ in 0 ..< 3 {
         insetw.append("\(3)")
      }
         insetw = "\((Int(excludeK > 93399267.0 || excludeK < -93399267.0 ? 7.0 : excludeK)))"
          var visibility6: [String: Any]! = [String(cString: [101,115,116,114,111,121,0], encoding: .utf8)!:70, String(cString: [101,120,97,99,116,108,121,0], encoding: .utf8)!:86, String(cString: [115,116,111,112,101,100,0], encoding: .utf8)!:58]
          var secondaryR: Double = 4.0
         excludeK -= (Double(insetw.count + Int(excludeK > 197097850.0 || excludeK < -197097850.0 ? 55.0 : excludeK)))
         visibility6 = ["\(visibility6.keys.count)": (insetw == (String(cString:[106,0], encoding: .utf8)!) ? insetw.count : visibility6.count)]
         secondaryR /= Swift.max(1, Double(insetw.count % (Swift.max(1, 8))))
      repeat {
         excludeK -= (Double(1 ^ Int(excludeK > 166351256.0 || excludeK < -166351256.0 ? 47.0 : excludeK)))
         if excludeK == 326806.0 {
            break
         }
      } while (excludeK == 326806.0) && (4 < (Int(excludeK > 277259737.0 || excludeK < -277259737.0 ? 26.0 : excludeK) / (Swift.max(insetw.count, 1))) && 5.43 < (excludeK / (Swift.max(Double(insetw.count), 4))))
      while (4.11 == excludeK) {
         insetw = "\(insetw.count)"
         break
      }
         insetw = "\(insetw.count << (Swift.min(labs(1), 3)))"
      panel6 = "\(insetw.count >> (Swift.min(labs(2), 1)))"
   }

        let view = UIView()
        view.backgroundColor = UIColor.hex("#2C2C2E")
        view.layer.cornerRadius = Layout.systemCornerRadius
        view.clipsToBounds = true
        return view
    }()

    private let systemLabel: UILabel = {
       var totall: String! = String(cString: [99,121,99,108,101,115,0], encoding: .utf8)!
       var docV: [Any]! = [String(cString: [102,116,101,108,108,0], encoding: .utf8)!, String(cString: [99,104,114,111,109,97,104,111,108,100,0], encoding: .utf8)!, String(cString: [101,120,112,101,99,116,0], encoding: .utf8)!]
       var bottomJ: Double = 1.0
       var providerk: String! = String(cString: [102,111,99,117,115,97,98,108,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &providerk) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 1 {
          var icond: Double = 5.0
          var supportU: [String: Any]! = [String(cString: [97,117,116,104,101,110,116,105,99,97,116,105,111,110,0], encoding: .utf8)!:43, String(cString: [101,108,101,118,97,116,101,100,0], encoding: .utf8)!:21, String(cString: [115,97,109,101,0], encoding: .utf8)!:42]
         providerk.append("\(supportU.count + 3)")
         icond /= Swift.max(1, (Double(Int(icond > 76064459.0 || icond < -76064459.0 ? 83.0 : icond))))
      }
         providerk.append("\((Int(bottomJ > 206459843.0 || bottomJ < -206459843.0 ? 50.0 : bottomJ) & 1))")
      if (docV.count ^ providerk.count) < 1 || (providerk.count ^ 1) < 4 {
         docV = [(Int(bottomJ > 289244909.0 || bottomJ < -289244909.0 ? 21.0 : bottomJ))]
      }
          var bubblez: String! = String(cString: [115,99,114,97,112,101,0], encoding: .utf8)!
          var person5: [Any]! = [String(cString: [99,111,110,102,108,105,99,116,0], encoding: .utf8)!, String(cString: [100,101,99,111,100,101,112,108,97,110,101,0], encoding: .utf8)!, String(cString: [112,105,110,103,0], encoding: .utf8)!]
          var emailf: [String: Any]! = [String(cString: [109,97,108,101,0], encoding: .utf8)!:8, String(cString: [121,117,118,110,118,99,0], encoding: .utf8)!:81, String(cString: [115,112,101,110,100,101,114,0], encoding: .utf8)!:51]
         withUnsafeMutablePointer(to: &emailf) { pointer in
                _ = pointer.pointee
         }
         bottomJ -= Double(bubblez.count + 2)
         person5.append(docV.count)
         emailf["\(providerk)"] = emailf.count / (Swift.max(providerk.count, 10))
       var recommendm: String! = String(cString: [112,97,114,116,105,99,105,112,97,110,116,115,0], encoding: .utf8)!
      if 2 > (docV.count + 4) {
         bottomJ -= Double(recommendm.count)
      }
      repeat {
          var greenO: String! = String(cString: [115,101,115,115,105,111,110,105,100,0], encoding: .utf8)!
          _ = greenO
          var sheetK: String! = String(cString: [97,114,116,105,115,116,0], encoding: .utf8)!
          var asku: String! = String(cString: [118,109,115,108,0], encoding: .utf8)!
          var enabledD: String! = String(cString: [100,105,115,112,108,97,121,105,110,103,0], encoding: .utf8)!
          var spacingn: String! = String(cString: [114,101,97,115,115,109,0], encoding: .utf8)!
         bottomJ *= Double(2 / (Swift.max(4, asku.count)))
         greenO.append("\(greenO.count * 1)")
         sheetK.append("\(greenO.count)")
         enabledD = "\((Int(bottomJ > 359249378.0 || bottomJ < -359249378.0 ? 49.0 : bottomJ) / (Swift.max(docV.count, 6))))"
         spacingn.append("\((Int(bottomJ > 192592094.0 || bottomJ < -192592094.0 ? 91.0 : bottomJ) - greenO.count))")
         if 1232436.0 == bottomJ {
            break
         }
      } while ((Int(bottomJ > 392658598.0 || bottomJ < -392658598.0 ? 36.0 : bottomJ)) > docV.count) && (1232436.0 == bottomJ)
         providerk = "\((Int(bottomJ > 38050855.0 || bottomJ < -38050855.0 ? 76.0 : bottomJ) % (Swift.max(docV.count, 2))))"
         docV.append(3)
      totall = "\((Int(bottomJ > 21978358.0 || bottomJ < -21978358.0 ? 15.0 : bottomJ)))"

        let label = UILabel()
        label.textColor = UIColor.hex("#CCCCCC")
        label.font = .systemFont(ofSize: 13, weight: .regular)
        label.numberOfLines = 0
        return label
    }()

    private let memberContainerView = UIView()

    private let avatarImageView: UIImageView = {
       var observero: Int = 3
    var raw8: Double = 1.0
       var phrasesf: String! = String(cString: [118,105,101,119,0], encoding: .utf8)!
       _ = phrasesf
      while (phrasesf.hasPrefix(phrasesf)) {
         phrasesf = "\((phrasesf == (String(cString:[83,0], encoding: .utf8)!) ? phrasesf.count : phrasesf.count))"
         break
      }
      if phrasesf.count >= phrasesf.count {
         phrasesf = "\(2)"
      }
       var memberM: String! = String(cString: [105,110,105,116,105,97,116,111,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &memberM) { pointer in
    
      }
       var b_manager6: String! = String(cString: [99,117,114,114,114,101,110,116,0], encoding: .utf8)!
         memberM = "\((b_manager6 == (String(cString:[84,0], encoding: .utf8)!) ? b_manager6.count : memberM.count))"
      observero |= 2

        let imageView = UIImageView()
   while (2.58 < (2.47 * raw8)) {
       var control7: Int = 1
       var modep: Bool = false
      withUnsafeMutablePointer(to: &modep) { pointer in
             _ = pointer.pointee
      }
       var avatarN: String! = String(cString: [100,95,56,49,0], encoding: .utf8)!
          var cameraa: [Any]! = [16, 35, 86]
         modep = 99 <= avatarN.count && control7 <= 99
         cameraa = [1]
      repeat {
         modep = avatarN.contains("\(modep)")
         if modep ? !modep : modep {
            break
         }
      } while (!modep) && (modep ? !modep : modep)
      while (4 < (avatarN.count & control7) || (4 & avatarN.count) < 3) {
         avatarN = "\(3)"
         break
      }
      if avatarN.count > 1 {
         avatarN = "\(avatarN.count)"
      }
       var buttonsU: Float = 3.0
       var mailQ: Float = 4.0
      if !modep {
          var safe_: Int = 0
          var shared1: Int = 3
          var normalized6: String! = String(cString: [115,116,105,99,107,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &normalized6) { pointer in
    
         }
          var inseth: String! = String(cString: [115,117,109,102,0], encoding: .utf8)!
          var reportC: Bool = false
         control7 %= Swift.max((Int(buttonsU > 194747044.0 || buttonsU < -194747044.0 ? 54.0 : buttonsU)), 5)
         safe_ += (1 / (Swift.max(2, Int(buttonsU > 28903344.0 || buttonsU < -28903344.0 ? 71.0 : buttonsU))))
         shared1 /= Swift.max(safe_, 1)
         normalized6.append("\(2 | safe_)")
         inseth.append("\(((String(cString:[89,0], encoding: .utf8)!) == inseth ? avatarN.count : inseth.count))")
         reportC = !modep
      }
      repeat {
         buttonsU += Float(1)
         if buttonsU == 3492407.0 {
            break
         }
      } while (4 > avatarN.count) && (buttonsU == 3492407.0)
         avatarN = "\((control7 & Int(buttonsU > 240610238.0 || buttonsU < -240610238.0 ? 28.0 : buttonsU)))"
       var docsW: [Any]! = [44, 87, 36]
       var decodedY: [Any]! = [47, 16]
         mailQ /= Swift.max(3, Float(docsW.count))
         docsW = [((String(cString:[88,0], encoding: .utf8)!) == avatarN ? (modep ? 5 : 4) : avatarN.count)]
         decodedY.append(docsW.count ^ decodedY.count)
      raw8 /= Swift.max(Double(1), 2)
      break
   }
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        imageView.backgroundColor = UIColor.hex("#444444")
        imageView.layer.cornerRadius = Layout.avatarSize / 2
        return imageView
    }()

    private let nameLabel: UILabel = {
       var componentc: [String: Any]! = [String(cString: [112,97,114,101,110,116,97,103,101,0], encoding: .utf8)!:89, String(cString: [115,108,105,99,101,115,0], encoding: .utf8)!:34]
   while (componentc.values.count < 1) {
       var first0: String! = String(cString: [102,116,118,98,108,97,110,107,0], encoding: .utf8)!
       var targetn: Double = 2.0
      withUnsafeMutablePointer(to: &targetn) { pointer in
             _ = pointer.pointee
      }
       var scripth: Float = 1.0
       var valueB: [Any]! = [87]
       var bundled3: Double = 0.0
       _ = bundled3
         bundled3 -= (Double(1 % (Swift.max(6, Int(scripth > 212427077.0 || scripth < -212427077.0 ? 11.0 : scripth)))))
      if !first0.contains("\(targetn)") {
         targetn += Double(2 % (Swift.max(8, valueB.count)))
      }
      for _ in 0 ..< 2 {
         bundled3 /= Swift.max(5, (Double(2 * Int(scripth > 221880719.0 || scripth < -221880719.0 ? 7.0 : scripth))))
      }
      while ((Double(targetn * Double(3))) >= 4.82) {
         scripth -= (Float((String(cString:[122,0], encoding: .utf8)!) == first0 ? Int(scripth > 1102554.0 || scripth < -1102554.0 ? 39.0 : scripth) : first0.count))
         break
      }
          var confirms: String! = String(cString: [111,118,101,114,104,101,97,100,0], encoding: .utf8)!
          var link5: String! = String(cString: [98,117,102,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &link5) { pointer in
                _ = pointer.pointee
         }
          var review1: Double = 5.0
         withUnsafeMutablePointer(to: &review1) { pointer in
                _ = pointer.pointee
         }
         scripth -= Float(3)
         confirms.append("\((confirms == (String(cString:[48,0], encoding: .utf8)!) ? first0.count : confirms.count))")
         link5 = "\(((String(cString:[114,0], encoding: .utf8)!) == link5 ? Int(bundled3 > 282358419.0 || bundled3 < -282358419.0 ? 65.0 : bundled3) : link5.count))"
         review1 += Double(first0.count)
         first0 = "\((valueB.count - Int(bundled3 > 295172949.0 || bundled3 < -295172949.0 ? 52.0 : bundled3)))"
       var teamH: Double = 1.0
      for _ in 0 ..< 2 {
         targetn -= (Double(Int(scripth > 168642743.0 || scripth < -168642743.0 ? 65.0 : scripth)))
      }
          var shuffled_: String! = String(cString: [112,97,116,116,101,114,110,115,0], encoding: .utf8)!
          _ = shuffled_
         teamH -= Double(1)
         shuffled_.append("\(first0.count)")
       var fileP: Double = 2.0
       var linex: Double = 1.0
         bundled3 += Double(2)
         linex /= Swift.max(4, Double(1))
       var switch_oO: [String: Any]! = [String(cString: [116,116,97,100,115,112,0], encoding: .utf8)!:23, String(cString: [99,110,111,110,99,101,0], encoding: .utf8)!:73, String(cString: [100,97,115,104,101,115,0], encoding: .utf8)!:1]
       var url_: [String: Any]! = [String(cString: [97,99,102,105,108,116,101,114,0], encoding: .utf8)!:15, String(cString: [115,97,118,101,105,0], encoding: .utf8)!:24, String(cString: [103,101,110,100,101,114,0], encoding: .utf8)!:93]
      withUnsafeMutablePointer(to: &url_) { pointer in
             _ = pointer.pointee
      }
      repeat {
         url_ = ["\(linex)": (Int(linex > 165447936.0 || linex < -165447936.0 ? 53.0 : linex))]
         if url_.count == 1539537 {
            break
         }
      } while (bundled3 == 3.97) && (url_.count == 1539537)
      while (Double(switch_oO.values.count) <= fileP) {
          var messagel: Float = 2.0
         switch_oO["\(linex)"] = (Int(linex > 336222995.0 || linex < -336222995.0 ? 67.0 : linex))
         messagel += Float(2 >> (Swift.min(4, switch_oO.count)))
         break
      }
      componentc["\(scripth)"] = (Int(scripth > 100214819.0 || scripth < -100214819.0 ? 59.0 : scripth) % 3)
      break
   }

        let label = UILabel()
        label.textColor = .white
        label.font = .systemFont(ofSize: 14, weight: .semibold)
        return label
    }()

    private let messageLabel: UILabel = {
       var memberL: String! = String(cString: [115,116,114,117,99,116,117,114,97,108,0], encoding: .utf8)!
      memberL.append("\(memberL.count)")

        let label = UILabel()
        label.textColor = UIColor.hex("#E5E5E5")
        label.font = .systemFont(ofSize: 14, weight: .regular)
        label.numberOfLines = 0
        return label
    }()

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        setupUI()
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    func configure(with message: DSSetupClip) {
       var buttona: String! = String(cString: [120,117,118,109,118,115,0], encoding: .utf8)!
   repeat {
      buttona.append("\(buttona.count)")
      if buttona.count == 3403512 {
         break
      }
   } while (!buttona.hasPrefix(buttona)) && (buttona.count == 3403512)

        systemContainerView.isHidden = !message.isSystem
        memberContainerView.isHidden = message.isSystem

        if message.isSystem {
            systemLabel.text = message.text
            return
        }

        nameLabel.text = message.userName
        messageLabel.text = message.text
        avatarImageView.image = UserData.image(for: message.avatarPath)
    }

    private func setupUI() {
       var urlE: String! = String(cString: [100,105,97,108,111,103,117,101,115,0], encoding: .utf8)!
    var phrases2: String! = String(cString: [116,104,97,119,101,100,0], encoding: .utf8)!
       var timerc: String! = String(cString: [108,115,104,105,102,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &timerc) { pointer in
    
      }
      if !timerc.hasPrefix("\(timerc.count)") {
          var subjectr: [Any]! = [22, 93]
          var reusableH: Double = 2.0
          var doc7: Double = 4.0
          var tabZ: Bool = false
          _ = tabZ
          var avatarse: Float = 2.0
         timerc.append("\((3 * Int(doc7 > 146643210.0 || doc7 < -146643210.0 ? 27.0 : doc7)))")
         subjectr.append(2)
         reusableH -= Double(2 >> (Swift.min(5, timerc.count)))
         tabZ = !tabZ
         avatarse += (Float(Int(doc7 > 55634360.0 || doc7 < -55634360.0 ? 6.0 : doc7) << (Swift.min(2, labs((tabZ ? 4 : 3))))))
      }
      for _ in 0 ..< 3 {
         timerc = "\(timerc.count % 1)"
      }
      repeat {
          var secondsm: String! = String(cString: [103,117,105,100,101,0], encoding: .utf8)!
          var durationg: String! = String(cString: [101,120,116,101,114,110,0], encoding: .utf8)!
          var attributedH: Float = 0.0
         timerc = "\((Int(attributedH > 359015176.0 || attributedH < -359015176.0 ? 88.0 : attributedH) << (Swift.min(timerc.count, 4))))"
         secondsm.append("\((Int(attributedH > 239154583.0 || attributedH < -239154583.0 ? 87.0 : attributedH) | durationg.count))")
         durationg = "\(1 * timerc.count)"
         if timerc == (String(cString:[101,50,121,108,0], encoding: .utf8)!) {
            break
         }
      } while (timerc == (String(cString:[101,50,121,108,0], encoding: .utf8)!)) && (timerc.contains(timerc))
      phrases2.append("\(2 / (Swift.max(3, urlE.count)))")

       var barS: Double = 4.0
       var createdU: String! = String(cString: [115,116,114,116,121,112,101,0], encoding: .utf8)!
       var pathsU: String! = String(cString: [97,104,101,97,100,0], encoding: .utf8)!
         barS += (Double((String(cString:[111,0], encoding: .utf8)!) == createdU ? createdU.count : pathsU.count))
      if (barS / 1.100) >= 5.78 {
          var emailM: [Any]! = [UILabel()]
          var fillers7: Double = 1.0
         createdU.append("\((2 >> (Swift.min(1, labs(Int(barS > 313044420.0 || barS < -313044420.0 ? 89.0 : barS))))))")
         emailM = [3 + emailM.count]
         fillers7 /= Swift.max((Double(Int(barS > 176170477.0 || barS < -176170477.0 ? 78.0 : barS))), 1)
      }
      if pathsU == createdU {
          var create4: Float = 4.0
         withUnsafeMutablePointer(to: &create4) { pointer in
    
         }
          var postj: String! = String(cString: [110,99,104,117,110,107,0], encoding: .utf8)!
          var sendw: String! = String(cString: [117,110,99,111,109,112,114,101,115,115,101,100,0], encoding: .utf8)!
          var indices9: Int = 3
         createdU = "\(1 >> (Swift.min(5, sendw.count)))"
         create4 -= (Float((String(cString:[78,0], encoding: .utf8)!) == postj ? sendw.count : postj.count))
         indices9 %= Swift.max(sendw.count * createdU.count, 1)
      }
         createdU.append("\(createdU.count - pathsU.count)")
         createdU = "\(1 >> (Swift.min(5, pathsU.count)))"
      repeat {
         pathsU.append("\((Int(barS > 43132573.0 || barS < -43132573.0 ? 27.0 : barS) & pathsU.count))")
         if pathsU.count == 3984353 {
            break
         }
      } while (pathsU.count == 3984353) && (!createdU.contains("\(pathsU.count)"))
         pathsU = "\(((String(cString:[117,0], encoding: .utf8)!) == createdU ? createdU.count : Int(barS > 173334907.0 || barS < -173334907.0 ? 54.0 : barS)))"
          var playingq: Int = 2
          var randomn: String! = String(cString: [114,97,110,107,105,110,103,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &randomn) { pointer in
                _ = pointer.pointee
         }
         barS /= Swift.max(Double(2 % (Swift.max(7, createdU.count))), 2)
         playingq += pathsU.count
         randomn.append("\(playingq)")
         barS += Double(createdU.count >> (Swift.min(labs(3), 2)))
      urlE = "\(2 << (Swift.min(3, createdU.count)))"
        selectionStyle = .none
        backgroundColor = .clear
        contentView.backgroundColor = .clear

        contentView.addSubview(systemContainerView)
        contentView.addSubview(memberContainerView)
        systemContainerView.addSubview(systemLabel)
        memberContainerView.addSubview(avatarImageView)
        memberContainerView.addSubview(nameLabel)
        memberContainerView.addSubview(messageLabel)

        systemContainerView.snp.makeConstraints { make in
            make.top.equalToSuperview().inset(8)
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.bottom.equalToSuperview().inset(8)
        }

        systemLabel.snp.makeConstraints { make in
            make.edges.equalToSuperview().inset(14)
        }

        memberContainerView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }

        avatarImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalToSuperview().inset(10)
            make.width.height.equalTo(Layout.avatarSize)
        }

        nameLabel.snp.makeConstraints { make in
            make.leading.equalTo(avatarImageView.snp.trailing).offset(10)
            make.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalTo(avatarImageView)
        }

        messageLabel.snp.makeConstraints { make in
            make.leading.trailing.equalTo(nameLabel)
            make.top.equalTo(nameLabel.snp.bottom).offset(4)
            make.bottom.equalToSuperview().inset(10)
        }
    }
}
